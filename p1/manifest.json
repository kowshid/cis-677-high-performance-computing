{
  "format": "hpcbench/task/1",
  "requested_by": "kowshid",
  "key_kind": "personal",
  "source_included": true,
  "ingest_path": "/api/public/ingest",
  "input_path": "/api/public/task/cis677-p1/input",
  "task": "cis677-p1",
  "title": "Dense \u00d7 sparse matrix multiply",
  "status": "open",
  "kill_factor": 2,
  "config": {
    "sweep_arg": "k",
    "sweep_values": [
      8,
      16,
      32,
      64,
      128,
      256
    ],
    "canon_sig_digits": 8,
    "sanity_timeout_s": 120,
    "threads": 4,
    "mem_limit_mb": 32768,
    "full_credit_at": 8,
    "metric_label": "Geometric-mean speedup",
    "correctness_mode": "tolerance",
    "correctness_rel_tol": 0.01,
    "verify_seed": "ba7c26882c39d5dae13d9dfd420c279f",
    "audit_enabled": true,
    "audit_path": "/api/public/task/cis677-p1/audit"
  },
  "input": {
    "mode": "url",
    "url": "https://raw.githubusercontent.com/zdebruine/cis677-hpcbench/main/data/p1-kernel/pbmc3k.npz",
    "sha256": "840600d40f18954912cda4fe2a6bdbe6b11836750879d1f3e3cc5395770ee9c2",
    "prepare_cmd": "curl -fsSL https://raw.githubusercontent.com/zdebruine/cis677-hpcbench/main/tools/make_p1_input.py -o make_p1_input.py && python3 make_p1_input.py --packed \"$HPCBENCH_INPUT\" --variant public --out \"$HPCBENCH_INPUT_OUT\"",
    "storage": false,
    "notes": "u64 n_cells, u64 n_genes, then n_cells*n_genes float64 row-major (cells x genes). Rebuilt locally from the 4.4 MB packed pbmc3k matrix; requires python3 with numpy.",
    "bytes": 296236816
  },
  "contract": {
    "build_cmd": "sh ./build.sh",
    "prepare_cmd": "./build/task prepare {input} {work}",
    "run_cmd": "./build/task solve {work} {result} {sweep}"
  },
  "harness": {
    "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
    "files": {
      "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
      "include/task.hpp": "// task.hpp -- HOST-OWNED. Read-only for competitors.\n//\n// This header is the whole contract between the harness and an entry. An entry\n// is two files, each containing exactly one function and nothing else:\n//\n//     // src/prepare.cpp    UNTIMED\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n//     // src/solution.cpp   TIMED, and only this\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Everything else -- main, argv, reading the raw corpus, generating W,\n// allocating and zeroing the output, writing result.bin, computing the digest --\n// lives in harness.cpp and is not the competitor's problem.\n//\n// WHAT IS DELIBERATELY STILL YOURS. The host owns the *mechanism* of reading the\n// workdir; you own *when and how much* you read, because you call these helpers\n// yourself from inside `solve`, inside the timed region. Streaming versus\n// mapping versus slurping, lazy versus eager, one file versus shards, are real\n// levers and they stay with you.\n//\n// WHAT prepare CANNOT SEE. `k` is not in `Input` and not in `Sink`, on purpose.\n// The sweep value does not exist while your representation is being built, so it\n// cannot be tabled.\n//\n// Failures here are fatal and say why, on stderr, with the path. Nothing in the\n// harness throws: a browser tab runs these binaries against a libc++abi with no\n// unwinder, where a throw becomes an unexplained abort.\n#ifndef HPCBENCH_TASK_HPP\n#define HPCBENCH_TASK_HPP\n\n#include <fcntl.h>\n#include <sys/stat.h>\n#include <unistd.h>\n\n#include <cstddef>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <string>\n#include <vector>\n\n// `read_exact_fd`, `read_raw_input` and `gen_W` are host-owned too and live next\n// door; they are included here so an entry needs exactly one #include.\n#include \"task_io.hpp\"\n\n// mmap exists natively and does not exist in the wasm sysroot the in-tab runner\n// uses, so `Work::map` is a real mapping on one platform and a read into memory\n// on the other. Either way the bytes and the lifetime are the same.\n#if !defined(__wasm__) && !defined(__EMSCRIPTEN__) && !defined(_WIN32)\n#define HPCBENCH_HAVE_MMAP 1\n#include <sys/mman.h>\n#endif\n\nnamespace hpcbench {\n\n[[noreturn]] inline void fatal(const std::string& what) {\n  std::fprintf(stderr, \"hpcbench: %s\\n\", what.c_str());\n  std::fflush(stderr);\n  std::exit(1);\n}\n\n// The write-side twin of `read_exact_fd`: bounded chunks, same memfs reason.\ninline bool write_exact_fd(int fd, const void* src, std::size_t n) {\n  const unsigned char* p = static_cast<const unsigned char*>(src);\n  const std::size_t chunk = std::size_t(1) << 20;\n  while (n > 0) {\n    const std::size_t want = n < chunk ? n : chunk;\n    const ssize_t put = ::write(fd, p, want);\n    if (put <= 0) return false;\n    p += put;\n    n -= static_cast<std::size_t>(put);\n  }\n  return true;\n}\n\n// ---------------------------------------------------------------------------\n// prepare side: untimed\n// ---------------------------------------------------------------------------\n\n/** One file being written into the workdir by `prepare`. Untimed. */\nclass Writer {\n public:\n  explicit Writer(std::string path) : path_(std::move(path)) {\n    fd_ = ::open(path_.c_str(), O_WRONLY | O_CREAT | O_TRUNC, 0644);\n    if (fd_ < 0) fatal(\"cannot create \" + path_);\n  }\n  Writer(const Writer&) = delete;\n  Writer& operator=(const Writer&) = delete;\n  Writer(Writer&& other) noexcept : fd_(other.fd_), path_(std::move(other.path_)) {\n    other.fd_ = -1;\n  }\n  ~Writer() { close(); }\n\n  /** Writes n bytes, or ends the process saying which file failed. */\n  void write(const void* data, std::size_t bytes) {\n    if (fd_ < 0) fatal(\"write after close on \" + path_);\n    if (bytes && !write_exact_fd(fd_, data, bytes)) fatal(\"short write to \" + path_);\n  }\n  template <typename T>\n  void write_value(const T& value) {\n    write(&value, sizeof(T));\n  }\n  void close() {\n    if (fd_ >= 0) {\n      if (::close(fd_) != 0) {\n        fd_ = -1;\n        fatal(\"failed to close \" + path_);\n      }\n      fd_ = -1;\n    }\n  }\n  const std::string& path() const { return path_; }\n\n private:\n  int fd_ = -1;\n  std::string path_;\n};\n\n/** The workdir, as `prepare` sees it: a place to create files. Untimed. */\nstruct Sink {\n  std::string dir;\n  Writer create(const char* name) const { return Writer(dir + \"/\" + name); }\n  const char* path() const { return dir.c_str(); }\n};\n\n/** The raw corpus, already read for you by the harness. Untimed, and `k` is\n *  deliberately absent. */\nstruct Input {\n  std::uint64_t n_cells;\n  std::uint64_t n_genes;\n  const double* X;  // row-major n_cells x n_genes\n};\n\n// ---------------------------------------------------------------------------\n// solve side: every call below happens INSIDE the timed region\n// ---------------------------------------------------------------------------\n\n/** A bounded streaming read of one file you wrote in prepare. */\nclass Reader {\n public:\n  explicit Reader(std::string path) : path_(std::move(path)) {\n    fd_ = ::open(path_.c_str(), O_RDONLY);\n    if (fd_ < 0) fatal(\"cannot open \" + path_);\n    struct stat st{};\n    size_ = ::fstat(fd_, &st) == 0 ? static_cast<std::uint64_t>(st.st_size) : 0;\n  }\n  Reader(const Reader&) = delete;\n  Reader& operator=(const Reader&) = delete;\n  Reader(Reader&& other) noexcept : fd_(other.fd_), size_(other.size_), path_(std::move(other.path_)) {\n    other.fd_ = -1;\n  }\n  ~Reader() { close(); }\n\n  /** Reads exactly n bytes; a short read is fatal and names the file. */\n  void read(void* dst, std::size_t bytes) {\n    if (fd_ < 0) fatal(\"read after close on \" + path_);\n    if (bytes && !read_exact_fd(fd_, dst, bytes)) fatal(\"truncated \" + path_);\n  }\n  /** Reads exactly n bytes, returning false instead of exiting at end of file. */\n  bool try_read(void* dst, std::size_t bytes) {\n    return fd_ >= 0 && (bytes == 0 || read_exact_fd(fd_, dst, bytes));\n  }\n  template <typename T>\n  T read_value() {\n    T value{};\n    read(&value, sizeof(T));\n    return value;\n  }\n  std::uint64_t size() const { return size_; }\n  void close() {\n    if (fd_ >= 0) ::close(fd_);\n    fd_ = -1;\n  }\n\n private:\n  int fd_ = -1;\n  std::uint64_t size_ = 0;\n  std::string path_;\n};\n\n/** A whole file addressable as bytes: mmap where the platform has it, a read\n *  into memory where it does not. Owns whatever it took to get there. */\nclass Mapped {\n public:\n  Mapped() = default;\n  explicit Mapped(const std::string& path) {\n    const int fd = ::open(path.c_str(), O_RDONLY);\n    if (fd < 0) fatal(\"cannot open \" + path);\n    struct stat st{};\n    if (::fstat(fd, &st) != 0) {\n      ::close(fd);\n      fatal(\"cannot size \" + path);\n    }\n    bytes_ = static_cast<std::size_t>(st.st_size);\n#ifdef HPCBENCH_HAVE_MMAP\n    if (bytes_ > 0) {\n      void* p = ::mmap(nullptr, bytes_, PROT_READ, MAP_PRIVATE, fd, 0);\n      if (p != MAP_FAILED) {\n        mapped_ = p;\n        data_ = static_cast<const std::uint8_t*>(p);\n        ::close(fd);\n        return;\n      }\n    }\n#endif\n    owned_.resize(bytes_);\n    if (bytes_ && !read_exact_fd(fd, owned_.data(), bytes_)) {\n      ::close(fd);\n      fatal(\"truncated \" + path);\n    }\n    ::close(fd);\n    data_ = owned_.data();\n  }\n  Mapped(const Mapped&) = delete;\n  Mapped& operator=(const Mapped&) = delete;\n  Mapped(Mapped&& o) noexcept\n      : data_(nullptr), bytes_(o.bytes_), owned_(std::move(o.owned_)), mapped_(o.mapped_) {\n    data_ = o.mapped_ ? static_cast<const std::uint8_t*>(o.mapped_) : owned_.data();\n    o.mapped_ = nullptr;\n    o.data_ = nullptr;\n    o.bytes_ = 0;\n  }\n  ~Mapped() {\n#ifdef HPCBENCH_HAVE_MMAP\n    if (mapped_) ::munmap(mapped_, bytes_);\n#endif\n  }\n  const std::uint8_t* data() const { return data_; }\n  std::size_t bytes() const { return bytes_; }\n  /** The mapping reinterpreted, for a file you wrote as fixed-width records. */\n  template <typename T>\n  const T* as(std::size_t byte_offset = 0) const {\n    return reinterpret_cast<const T*>(data_ + byte_offset);\n  }\n\n private:\n  const std::uint8_t* data_ = nullptr;\n  std::size_t bytes_ = 0;\n  std::vector<std::uint8_t> owned_;\n  void* mapped_ = nullptr;\n};\n\n/** The workdir as `solve` sees it. Everything you call on it is on the clock:\n *  reading your own representation is part of your entry. */\nstruct Work {\n  std::string dir_path;\n\n  Reader open(const char* name) const { return Reader(dir_path + \"/\" + name); }\n  Mapped map(const char* name) const { return Mapped(dir_path + \"/\" + name); }\n  std::vector<std::uint8_t> read_all(const char* name) const {\n    Reader r = open(name);\n    std::vector<std::uint8_t> bytes(static_cast<std::size_t>(r.size()));\n    r.read(bytes.data(), bytes.size());\n    return bytes;\n  }\n  const char* dir() const { return dir_path.c_str(); }\n};\n\n/** The host's definition of the problem for this run. W is already generated:\n *  there is no gen_W in an entry any more, and no time to be spent on it. */\nstruct Params {\n  std::uint64_t k;\n  const double* W;  // row-major n_genes x k\n  std::uint64_t n_cells;\n  std::uint64_t n_genes;\n};\n\n/** The output, preallocated and zero-filled by the harness. Row-major\n *  n_cells x k. Writing it to disk is the harness's job, after the clock stops. */\nstruct Result {\n  double* data;\n  std::uint64_t n_cells;\n  std::uint64_t k;\n};\n\n// The two functions an entry provides. Nothing else is linked from an entry.\nvoid prepare(const Input& in, Sink& out);\nvoid solve(const Work& w, const Params& p, Result& y);\n\n}  // namespace hpcbench\n\n// An entry file must not define main(): the harness owns the process. Defining\n// one would either collide with the harness or, worse, replace it and skip the\n// timing boundary entirely. `main` is renamed onto an object, so a definition\n// becomes a compile error whose text says which rule it broke. (Renaming it\n// onto a *type* is not enough: C++ lets a function hide a class name, so the\n// definition would compile and sit there dead.)\nnamespace hpcbench {\nnamespace {\nconst int hpcbench_error_entry_files_must_not_define_main_the_harness_owns_it = 0;\n}\n}  // namespace hpcbench\nusing hpcbench::hpcbench_error_entry_files_must_not_define_main_the_harness_owns_it;\n#ifndef HPCBENCH_HARNESS_TU\n#define main hpcbench_error_entry_files_must_not_define_main_the_harness_owns_it\n#endif\n\n\n#endif  // HPCBENCH_TASK_HPP\n",
      "include/canon.hpp": "// ============================================================================\n// hpcbench canonical result writer.\n//\n// Emit your answer with write_result(). The grader hashes the file after\n// quantizing to the task's declared significant digits, so a vectorized or\n// threaded reduction that reorders floating-point additions still produces the\n// same checksum as the scalar reference -- provided it agrees to that\n// precision. Bit-exactness is NOT required (and would be unachievable).\n//\n// Usage:\n//     #include \"canon.hpp\"\n//     std::vector<double> out = ...;\n//     hpcbench::write_result(argv[2], out, {n_rows, n_cols});\n//\n// DO NOT MODIFY. The grader has its own copy.\n// ============================================================================\n#pragma once\n\n#include <cstdint>\n#include <cstdio>\n#include <cstring>\n#include <initializer_list>\n#include <stdexcept>\n#include <string>\n#include <vector>\n\nnamespace hpcbench {\n\nconstexpr std::uint32_t kCanonVersion = 1;\n\ninline void write_result(const std::string& path,\n                         const double* data,\n                         std::uint64_t n,\n                         std::initializer_list<std::uint64_t> dims = {}) {\n  std::FILE* f = std::fopen(path.c_str(), \"wb\");\n  if (!f) throw std::runtime_error(\"cannot open result file: \" + path);\n\n  std::fwrite(\"HPCBENCH\", 1, 8, f);\n  std::uint32_t version = kCanonVersion;\n  std::uint32_t ndim = static_cast<std::uint32_t>(dims.size());\n  std::fwrite(&version, sizeof(version), 1, f);\n  std::fwrite(&ndim, sizeof(ndim), 1, f);\n  for (std::uint64_t d : dims) std::fwrite(&d, sizeof(d), 1, f);\n  std::fwrite(&n, sizeof(n), 1, f);\n  std::fwrite(data, sizeof(double), n, f);\n\n  if (std::fclose(f) != 0)\n    throw std::runtime_error(\"failed to close result file: \" + path);\n}\n\ninline void write_result(const std::string& path,\n                         const std::vector<double>& v,\n                         std::initializer_list<std::uint64_t> dims = {}) {\n  write_result(path, v.data(), static_cast<std::uint64_t>(v.size()), dims);\n}\n\n}  // namespace hpcbench\n",
      "include/task_io.hpp": "// task_io.hpp \u2014 HOST-OWNED. Read-only for competitors.\n//\n// This header holds the two pieces of the task that are the host's definition of\n// the problem rather than anyone's optimisation:\n//\n//   hpcbench::gen_W          the weights every entry must multiply by\n//   hpcbench::read_raw_input reading the raw corpus in prepare\n//\n// Both bodies are lifted BYTE FOR BYTE from the baseline they replace, and the\n// refactored baseline is gated on producing bit-identical results at every\n// published sweep value. Nothing about the answer changed.\n//\n// Why it is here instead of copied into every entry: while `gen_W` lives in each\n// entry's own solution.cpp, it is a pure function of `k`, and a pure function of\n// a small published set of `k` can be precomputed into a lookup table. The fix\n// is to make W depend on a value that does not exist when `prepare` runs, and\n// that is only possible if there is ONE `gen_W` that the host controls.\n//\n// ANTICIPATED CHANGE \u2014 the session nonce. `gen_W` already takes a trailing\n// `session_nonce` with a default of 0, and today it is RESERVED: nothing reads\n// it, so the weights are exactly the historical ones. When the host turns the nonce on, the runner will pass the session's\n// value and nothing at any call site has to change. Do not add parameters before\n// that one, and do not change what `session_nonce == 0` computes: every stored\n// reference digest depends on it.\n//\n// The runners stage this file LAST, after an entry's own sources, so an entry\n// that ships a file at this path cannot replace it.\n#ifndef HPCBENCH_TASK_IO_HPP\n#define HPCBENCH_TASK_IO_HPP\n\n#include <fcntl.h>\n#include <unistd.h>\n\n#include <cstddef>\n#include <cstdint>\n#include <cstdio>\n#include <vector>\n\nnamespace hpcbench {\n\n// A bounded open()/read() loop. REGRESSION GUARD -- do NOT replace this with\n// buffered stdio (std::ifstream/fread). hpcbench also runs entries in a browser\n// tab, and the vendored memfs there (public/wasm-clang/shared.js) has a broken\n// fd_read: its copy_out helper builds a typed-array view over the memfs\n// WebAssembly.Memory using the caller's full requested length, past the end of\n// the file region, and throws \"Invalid typed array length: <file size>\".\n// Buffered stdio asks for large readahead chunks and trips it on this ~296 MB\n// input.\ninline bool read_exact_fd(int fd, void* dst, std::size_t n) {\n  unsigned char* p = static_cast<unsigned char*>(dst);\n  const std::size_t chunk = std::size_t(1) << 20;\n  while (n > 0) {\n    const std::size_t want = n < chunk ? n : chunk;\n    const ssize_t got = ::read(fd, p, want);\n    if (got <= 0) return false;\n    p += got;\n    n -= static_cast<std::size_t>(got);\n  }\n  return true;\n}\n\n/** Reads the raw corpus: two u64 dimensions, then n_cells * n_genes doubles,\n *  row-major. Returns false and leaves a message on stderr on any failure. */\ninline bool read_raw_input(const char* path, std::uint64_t& n_cells, std::uint64_t& n_genes,\n                           std::vector<double>& X) {\n  const int fd = ::open(path, O_RDONLY);\n  if (fd < 0) {\n    std::fprintf(stderr, \"cannot open %s\\n\", path);\n    return false;\n  }\n  n_cells = 0;\n  n_genes = 0;\n  if (!read_exact_fd(fd, &n_cells, 8) || !read_exact_fd(fd, &n_genes, 8)) {\n    std::fprintf(stderr, \"truncated input\\n\");\n    ::close(fd);\n    return false;\n  }\n  X.assign(n_cells * n_genes, 0.0);\n  if (!read_exact_fd(fd, X.data(), X.size() * sizeof(double))) {\n    std::fprintf(stderr, \"truncated input\\n\");\n    ::close(fd);\n    return false;\n  }\n  ::close(fd);\n  return true;\n}\n\n/** The weights, identical for every entry. Body lifted byte for byte from the\n *  baseline's `gen_W`. `session_nonce == 0` is the historical definition and is\n *  what every stored reference digest was produced with; see the note above. */\ninline std::vector<double> gen_W(std::uint64_t n_genes, std::uint64_t k,\n                                 std::uint64_t session_nonce = 0) {\n  std::vector<double> W(n_genes * k);\n  std::uint64_t s = 0x9E3779B97F4A7C15ull ^ (k * 0xBF58476D1CE4E5B9ull);\n  // The nonce is RESERVED and deliberately unused: turning it on changes the\n  // answer for every existing entry, so it is the host's call and his timing.\n  // When he makes it, this is the one line that changes:\n  //     if (session_nonce != 0) s ^= session_nonce * 0x94D049BB133111EBull;\n  (void)session_nonce;\n  for (std::uint64_t i = 0; i < W.size(); ++i) {\n    s ^= s >> 30; s *= 0xBF58476D1CE4E5B9ull;\n    s ^= s >> 27; s *= 0x94D049BB133111EBull;\n    s ^= s >> 31;\n    W[i] = (double(s >> 11) * (1.0 / 9007199254740992.0)) * 2.0 - 1.0;\n  }\n  return W;\n}\n\n}  // namespace hpcbench\n\n#endif  // HPCBENCH_TASK_IO_HPP\n"
    },
    "notes": "The harness owns main and dispatches on argv: `task prepare <raw_input> <workdir>` and `task solve <workdir> <result.bin> <k>`. Only your hpcbench::solve is timed; the weights, the output buffer and writing result.bin are outside the clock. Y is row-major n_cells x k and arrives zero-filled. Natively the harness prints HPCBENCH_TIME_MS; in a browser tab, where this sysroot has no clock, it brackets the call with HPCBENCH_SOLVE_BEGIN/END and the runner timestamps those.",
    "solution_signature": "void hpcbench::solve(const Work& w, const Params& p, Result& y)"
  },
  "reference_digests": [
    {
      "sweep_key": "8",
      "digest": "6bf177b4204b2ad3dd3a9f88d03d1d986385b66db0189c3c2df7e704e813f32b"
    },
    {
      "sweep_key": "16",
      "digest": "707f7472caa28ae902b5310a2f561b4f3da3348224f33965acf71f98929752f7"
    },
    {
      "sweep_key": "32",
      "digest": "34517c020ca70113404a0536c7b7458abd71875543de4b3936e4188fee591793"
    },
    {
      "sweep_key": "64",
      "digest": "d9bd1307cf2133a2e56fde3b0eb92663a35919e6d98c61dd41726b602b0d3842"
    },
    {
      "sweep_key": "128",
      "digest": "3488abe1a4b5f588395ee504f6ea546ef2acedbdc0378aaf648619c96d68d173"
    },
    {
      "sweep_key": "256",
      "digest": "fe504678b9ec1cfadd6e48fa80aade9549d15481b6e6d0fb53a0c38a2d6d7664"
    }
  ],
  "submissions": [
    {
      "id": "deb449d5-f7de-4409-82fe-1d2b90ecf9f0",
      "name": "ahsan-09",
      "is_baseline": false,
      "version": 3,
      "owner_handle": "kowshid",
      "updated_at": "2026-09-23T00:59:48.26613+00:00",
      "reason": "no run on your account yet",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- RUNG 9: unchanged from rung 5. UNTIMED.\n//\n// Rungs 6, 7 and 9 change only solution.cpp. Rung 7 blocks W by gene without\n// any new offsets in this file: blocks are visited in ascending gene order, so\n// each row's decoder simply stops at a block boundary and resumes there.\n//\n// ---- rung 5 notes, unchanged ----\n//\n// RUNG 5: a data structure designed from the data.\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// The ONE change from rung 4: how each non-zero is encoded. Rung 2 spent 4\n// bytes on every non-zero (u16 gene + u16 count). The data says we can do with\n// about 1.3, by using what Lecture 3 (slides 28-34) points out about it:\n//\n//   * 69.8% of the counts are exactly 1.          -> say \"1\" with a single bit\n//   * genes are sorted within a row, gaps are small -> store the gap, not the\n//     (median 10, 99.8% under 128)                   gene (delta encoding)\n//   * counts other than 1 are small (max 419)      -> one byte each, rarely more\n//\n// Encoding, per row, in gene order:\n//\n//   code stream, one byte per non-zero:\n//     bit 7     1 if the count is 1, else 0 (count is in the value stream)\n//     bits 0-6  gap to the previous gene, 1..127 (the first gap is gene + 1)\n//     0 in bits 0-6 is an escape: the gap follows as two bytes (u16)\n//   value stream, one byte per non-zero whose count is not 1:\n//     2..255    the count itself\n//     0         escape: the count follows as two bytes (u16)\n//\n// Unlike the lecture's grouped-by-value layout, this keeps every row in gene\n// order, so the kernel adds the terms of each Y element in exactly the same\n// order as before: the compression costs no exactness.\n//\n// Each row starts at a recorded byte offset in both streams, so any row can be\n// decoded on its own -- which is what lets rung 3's threads keep splitting the\n// rows freely. A per-row count of non-zeros is kept for the thread balancing.\n//\n// csr_packed.bin layout, little-endian:\n//\n//     u64 header[16]:\n//       [0] n_cells  [1] n_genes  [2] nnz  [3] format  [4] max_row_nnz\n//       [5] off_row_nnz  [6] off_a  [7] off_b  [8] off_c  [9] off_d\n//       [10] total_bytes\n//     format 3 (packed): a = u32 code_ptr[n_cells + 1]  (byte offset of each row)\n//                        b = u32 val_ptr[n_cells + 1]   (byte offset of each row)\n//                        c = u8 code stream, d = u8 value stream (+64 pad bytes)\n//     format 2 (wide fallback, when a gene index or count does not fit):\n//                        a = u32 gene index[nnz], b = f64 value[nnz]\n//     u32 row_nnz[n_cells + 1] at off_row_nnz: prefix count of non-zeros\n//\n// Every section starts on a 64-byte boundary.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <vector>\n\n#if defined(_OPENMP)\n#include <omp.h>\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kFormatWide = 2;\nconstexpr std::uint64_t kFormatPacked = 3;\n\n// Diagnostic only (unchanged from rung 3): mirrors threads_granted() in solve.\nvoid report_threads() {\n  const char* env = std::getenv(\"OMP_NUM_THREADS\");\n#if defined(_OPENMP)\n  std::fprintf(stderr, \"prepare: solve will be granted %d threads (OpenMP; OMP_NUM_THREADS=%s)\\n\",\n               omp_get_max_threads(), env ? env : \"unset\");\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n  unsigned n = std::thread::hardware_concurrency();\n  const char* source = \"hardware threads\";\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0) {\n    n = static_cast<unsigned>(CPU_COUNT(&set));\n    source = \"CPUs in the affinity mask\";\n  }\n#endif\n  if (env && std::strtol(env, nullptr, 10) > 0) {\n    n = static_cast<unsigned>(std::strtol(env, nullptr, 10));\n    source = \"OMP_NUM_THREADS\";\n  }\n  std::fprintf(stderr, \"prepare: solve will be granted %u threads (std::thread; from %s)\\n\", n,\n               source);\n#else\n  std::fprintf(stderr, \"prepare: solve will run serially (browser build, no threads)\\n\");\n#endif\n}\n\nstd::uint64_t pad64(std::uint64_t x) { return (x + 63) & ~std::uint64_t(63); }\n\nvoid pad_to(hpcbench::Writer& w, std::uint64_t& pos, std::uint64_t target) {\n  static const unsigned char zeros[64] = {};\n  while (pos < target) {\n    const std::uint64_t n = std::min<std::uint64_t>(64, target - pos);\n    w.write(zeros, static_cast<std::size_t>(n));\n    pos += n;\n  }\n}\n\nvoid put(hpcbench::Writer& w, std::uint64_t& pos, const void* data, std::uint64_t bytes) {\n  w.write(data, static_cast<std::size_t>(bytes));\n  pos += bytes;\n}\n\n}  // namespace\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  const std::uint64_t n_cells = in.n_cells;\n  const std::uint64_t n_genes = in.n_genes;\n\n  // Pass 1: can the packed encoding represent this input exactly?\n  // Gaps must fit u16 (gene index <= 65,534) and counts must be integers\n  // 1..65,535. Otherwise fall back to u32 index + f64 value.\n  bool packable = n_genes <= 65535;\n  std::uint64_t nnz = 0, max_row = 0;\n  std::vector<std::uint32_t> row_nnz(n_cells + 1, 0);\n  for (std::uint64_t i = 0; i < n_cells; ++i) {\n    const double* xi = in.X + i * n_genes;\n    std::uint64_t row = 0;\n    for (std::uint64_t g = 0; g < n_genes; ++g) {\n      const double x = xi[g];\n      if (x == 0.0) continue;\n      ++row;\n      if (!(x >= 1.0 && x <= 65535.0 && x == std::floor(x))) packable = false;\n    }\n    nnz += row;\n    if (nnz > UINT32_MAX) fatal(\"more than 2^32 non-zeros: u32 offsets overflow\");\n    max_row = std::max(max_row, row);\n    row_nnz[i + 1] = static_cast<std::uint32_t>(nnz);\n  }\n\n  std::uint64_t header[16] = {};\n  header[0] = n_cells;\n  header[1] = n_genes;\n  header[2] = nnz;\n  header[4] = max_row;\n  header[5] = 128;  // off_row_nnz\n  const std::uint64_t after_row_nnz = pad64(header[5] + 4 * (n_cells + 1));\n\n  Writer w = out.create(\"csr_packed.bin\");\n  std::uint64_t pos = 0;\n\n  if (packable) {\n    // Pass 2: encode.\n    std::vector<std::uint8_t> code, vals;\n    std::vector<std::uint32_t> code_ptr(n_cells + 1, 0), val_ptr(n_cells + 1, 0);\n    code.reserve(static_cast<std::size_t>(nnz + nnz / 64));\n    vals.reserve(static_cast<std::size_t>(nnz / 2));\n    std::uint64_t gap_escapes = 0, val_escapes = 0;\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      std::uint64_t prev = ~std::uint64_t(0);  // \"gene -1\": the first gap is gene + 1\n      for (std::uint64_t g = 0; g < n_genes; ++g) {\n        const double x = xi[g];\n        if (x == 0.0) continue;\n        const std::uint64_t gap = g - prev;  // 1..65,535\n        prev = g;\n        const std::uint32_t v = static_cast<std::uint32_t>(x);\n        const std::uint8_t one = (v == 1) ? 0x80 : 0x00;\n        if (gap < 128) {\n          code.push_back(static_cast<std::uint8_t>(one | gap));\n        } else {\n          code.push_back(one);  // gap field 0 = escape\n          code.push_back(static_cast<std::uint8_t>(gap & 0xFF));\n          code.push_back(static_cast<std::uint8_t>(gap >> 8));\n          ++gap_escapes;\n        }\n        if (v != 1) {\n          if (v < 256) {\n            vals.push_back(static_cast<std::uint8_t>(v));\n          } else {\n            vals.push_back(0);  // escape\n            vals.push_back(static_cast<std::uint8_t>(v & 0xFF));\n            vals.push_back(static_cast<std::uint8_t>(v >> 8));\n            ++val_escapes;\n          }\n        }\n      }\n      if (code.size() > UINT32_MAX || vals.size() > UINT32_MAX)\n        fatal(\"packed streams exceed 4 GB\");\n      code_ptr[i + 1] = static_cast<std::uint32_t>(code.size());\n      val_ptr[i + 1] = static_cast<std::uint32_t>(vals.size());\n    }\n\n    header[3] = kFormatPacked;\n    header[6] = after_row_nnz;                               // code_ptr\n    header[7] = pad64(header[6] + 4 * (n_cells + 1));        // val_ptr\n    header[8] = pad64(header[7] + 4 * (n_cells + 1));        // code stream\n    header[9] = pad64(header[8] + code.size());              // value stream\n    header[10] = header[9] + vals.size() + 64;               // + pad: the decoder\n                                                             // may peek one byte ahead\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, code_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[7]);\n    put(w, pos, val_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[8]);\n    put(w, pos, code.data(), code.size());\n    pad_to(w, pos, header[9]);\n    put(w, pos, vals.data(), vals.size());\n    pad_to(w, pos, header[10]);\n    w.close();\n\n    std::fprintf(stderr,\n                 \"prepare: packed CSR, %llu nonzeros, %.2f MB (%.2f bytes/nnz); \"\n                 \"code %.2f MB, values %.2f MB, %llu gap and %llu value escapes\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6,\n                 double(header[10]) / double(nnz), double(code.size()) / 1e6,\n                 double(vals.size()) / 1e6, (unsigned long long)gap_escapes,\n                 (unsigned long long)val_escapes);\n  } else {\n    std::vector<std::uint32_t> col;\n    std::vector<double> val;\n    col.reserve(static_cast<std::size_t>(nnz));\n    val.reserve(static_cast<std::size_t>(nnz));\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      for (std::uint64_t g = 0; g < n_genes; ++g)\n        if (xi[g] != 0.0) {\n          col.push_back(static_cast<std::uint32_t>(g));\n          val.push_back(xi[g]);\n        }\n    }\n    header[3] = kFormatWide;\n    header[6] = after_row_nnz;                      // u32 gene index\n    header[7] = pad64(header[6] + 4 * nnz);         // f64 value\n    header[10] = header[7] + 8 * nnz;\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, col.data(), 4 * nnz);\n    pad_to(w, pos, header[7]);\n    put(w, pos, val.data(), 8 * nnz);\n    w.close();\n    std::fprintf(stderr, \"prepare: wide fallback CSR (u32 + f64), %llu nonzeros, %.2f MB\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6);\n  }\n  report_threads();\n}",
        "src/solution.cpp": "// src/solution.cpp -- RUNG 9: narrower types in the hot loop.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Stacked on rung 7. The ONE change: what a decoded row stores, and therefore\n// what the kernel re-reads once per output panel.\n//\n// Rung 7 decoded each non-zero into a u32 gene index and an f64 count, 12 bytes\n// each, and the kernel turned the gene into an address with a 64-bit multiply\n//     w = W + col[q] * k\n// EVERY time it swept the row -- once per panel, so eight times at k = 256.\n//\n// Rung 9 stores instead:\n//   * the element offset of the gene's W row, gene * k, in the same u32. The\n//     multiply happens once per non-zero while decoding rather than once per\n//     non-zero per panel; the kernel just adds. Used only when n_genes * k fits\n//     in 32 bits (3.5 M at k = 256, so always here); otherwise the rung 7 path\n//     runs unchanged.\n//   * the count as f32 instead of f64. Exact, and not by luck: the counts are\n//     integers below 2^24 (the data's largest is 419), every one of which is\n//     representable in f32, and f32 -> f64 widening is exact. Eight bytes per\n//     non-zero become four, so a decoded row is 8 bytes per non-zero instead of\n//     12 -- the scratch a thread re-reads per panel shrinks by a third.\n//\n// What it does NOT do: narrow W or Y. Both are f64 by the harness's interface,\n// and W's values are arbitrary doubles in [-1, 1), so no narrower type holds\n// them exactly. Narrowing W to f32 would be about 1e-7 relative error -- far\n// inside the task's 1% tolerance, but no longer the baseline's answer, so it is\n// not part of a bitwise-exact rung.\n//\n// Correctness: same terms, same ascending gene order, same fused multiply-adds\n// on the same double values. Bit-for-bit the baseline's.\n//\n// Knobs, as in rung 7: P1_W_BLOCK_BYTES, P1_PREFETCH_DIST.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cstdint>\n#include <cstdlib>\n#include <cstring>\n#include <memory>\n#include <vector>\n\n#if defined(__APPLE__)\n#include <sys/sysctl.h>\n#elif defined(__linux__)\n#include <unistd.h>\n#endif\n\n#if defined(_OPENMP)\n#include <omp.h>\n#define P1_OPENMP 1\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#define P1_STD_THREAD 1\n#include <atomic>\n#include <chrono>\n#include <condition_variable>\n#include <mutex>\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\n#ifndef P1_PANEL  // override with -DP1_PANEL=... (up to 64) in CXXFLAGS to experiment\n#define P1_PANEL 32\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kMinWorkPerThread = std::uint64_t(1) << 19;  // multiply-adds\nconstexpr std::uint64_t kChunksPerThread = 32;\nconstexpr std::uint64_t kMaxBlocks = 64;  // each block costs a barrier\n\n// ---- rung 4's kernel, unchanged -------------------------------------------\n\ntypedef double v4d __attribute__((vector_size(32)));  // four doubles\n\nconstexpr int kPanel = P1_PANEL;\n\n// acc + x * w, rounded exactly once, the way the baseline's `y += x * w` is.\n//\n// Where the target has a hardware fused multiply-add (__FP_FAST_FMA: every\n// arm64, x86 with FMA), the compiler fuses the baseline's statement into one\n// instruction. It does not reliably fuse ours: GCC's tuning for recent Intel\n// cores (\"avoid FMA chains\") splits a loop-carried accumulation into a multiply\n// and an add when only one or two accumulator vectors carry it -- exactly our\n// narrow panels -- which rounds twice. So the fusion is spelled out here.\n// Without hardware FMA the baseline rounds twice, and so does this.\ninline double madd(double x, double w, double acc) {\n#if defined(__FP_FAST_FMA)\n  return __builtin_fma(x, w, acc);\n#else\n  return x * w + acc;\n#endif\n}\n\ninline v4d vmadd(v4d x, v4d w, v4d acc) {\n#if defined(__FP_FAST_FMA)\n  // Four scalar fused multiply-adds; every compiler tested turns them into one\n  // vector FMA (vfmadd231pd on x86, two fmla on arm64).\n  return v4d{__builtin_fma(x[0], w[0], acc[0]), __builtin_fma(x[1], w[1], acc[1]),\n             __builtin_fma(x[2], w[2], acc[2]), __builtin_fma(x[3], w[3], acc[3])};\n#else\n  return x * w + acc;\n#endif\n}\n\n// One row, one panel of P output columns:  yc[0:P] = sum over the row's\n// non-zeros of x * Wc[g*k + 0:P], accumulated in registers. With `accumulate`\n// the sums start from what yc already holds -- the partial sums left there by\n// the previous gene blocks -- instead of from zero.\n//\n// noinline: each width is compiled as its own function. Measured necessity,\n// not style: GCC 13 inlined all 32 prefetching widths into the switch below and\n// the result ran k = 16 at 18.1 ms instead of 10.1 ms (k = 13: 15.1 vs 8.5) at\n// every prefetch distance from 1 to 64 -- a code-generation effect, not a\n// memory one. The same attribute on rungs 4 and 5 measured no difference.\n// PRE: col[] already holds gene * k (rung 9's decoded rows), so the kernel adds\n// instead of multiplying. PRE = false keeps rung 7's behaviour for plain CSR.\ntemplate <int P, bool PF, bool PRE, class Idx, class Val>\n__attribute__((noinline)) void row_panel(const Idx* col, const Val* val, std::uint32_t n,\n                                         const double* Wc, std::uint64_t k, double* yc,\n                                         std::uint32_t dist, bool accumulate) {\n  constexpr int V = P / 4;  // full 4-wide vectors\n  constexpr int R = P % 4;  // 1-3 leftover columns, scalar\n  v4d acc[V > 0 ? V : 1];\n  double tail[R > 0 ? R : 1];\n  if (accumulate) {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) std::memcpy(&acc[v], yc + 4 * v, sizeof(v4d));\n    for (int r = 0; r < R; ++r) tail[r] = yc[4 * V + r];\n  } else {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) acc[v] = v4d{0.0, 0.0, 0.0, 0.0};\n    for (int r = 0; r < R; ++r) tail[r] = 0.0;\n  }\n\n  for (std::uint32_t q = 0; q < n; ++q) {\n    if (PF && q + dist < n) {\n      // The W segment this row will need `dist` non-zeros from now: one\n      // prefetch per 64 bytes (a cache line on x86, half of one on Apple).\n      const std::uint64_t ahead_off = PRE ? static_cast<std::uint64_t>(col[q + dist])\n                                          : static_cast<std::uint64_t>(col[q + dist]) * k;\n      const char* ahead = reinterpret_cast<const char*>(Wc + ahead_off);\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n    const double x = static_cast<double>(val[q]);\n    const v4d xv = v4d{x, x, x, x};\n    const double* w = Wc + (PRE ? static_cast<std::uint64_t>(col[q])\n                                : static_cast<std::uint64_t>(col[q]) * k);\n    // Fully unrolled: GCC stops at 8 vectors on its own and would otherwise\n    // keep a 64-wide panel's accumulators on the stack.\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) {\n      v4d wv;\n      std::memcpy(&wv, w + 4 * v, sizeof(wv));  // unaligned vector load\n      acc[v] = vmadd(xv, wv, acc[v]);\n    }\n    for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n  }\n\n#pragma GCC unroll 16\n  for (int v = 0; v < V; ++v) std::memcpy(yc + 4 * v, &acc[v], sizeof(v4d));\n  for (int r = 0; r < R; ++r) yc[4 * V + r] = tail[r];\n}\n\n// The switch: a run-time width selects a compile-time kernel.\ntemplate <bool PF, bool PRE, class Idx, class Val>\nvoid row_panel_any(int width, const Idx* col, const Val* val, std::uint32_t n,\n                   const double* Wc, std::uint64_t k, double* yc, std::uint32_t dist,\n                   bool accumulate) {\n  switch (width) {\n#define P1_CASE(P)                                                  \\\n  case P:                                                           \\\n    row_panel<P, PF, PRE>(col, val, n, Wc, k, yc, dist, accumulate);     \\\n    return;\n    P1_CASE(1)\n    P1_CASE(2)\n    P1_CASE(3)\n    P1_CASE(4)\n    P1_CASE(5)\n    P1_CASE(6)\n    P1_CASE(7)\n    P1_CASE(8)\n    P1_CASE(9)\n    P1_CASE(10)\n    P1_CASE(11)\n    P1_CASE(12)\n    P1_CASE(13)\n    P1_CASE(14)\n    P1_CASE(15)\n    P1_CASE(16)\n    P1_CASE(17)\n    P1_CASE(18)\n    P1_CASE(19)\n    P1_CASE(20)\n    P1_CASE(21)\n    P1_CASE(22)\n    P1_CASE(23)\n    P1_CASE(24)\n    P1_CASE(25)\n    P1_CASE(26)\n    P1_CASE(27)\n    P1_CASE(28)\n    P1_CASE(29)\n    P1_CASE(30)\n    P1_CASE(31)\n    P1_CASE(32)\n#if P1_PANEL > 32\n    P1_CASE(33)\n    P1_CASE(34)\n    P1_CASE(35)\n    P1_CASE(36)\n    P1_CASE(37)\n    P1_CASE(38)\n    P1_CASE(39)\n    P1_CASE(40)\n    P1_CASE(41)\n    P1_CASE(42)\n    P1_CASE(43)\n    P1_CASE(44)\n    P1_CASE(45)\n    P1_CASE(46)\n    P1_CASE(47)\n    P1_CASE(48)\n    P1_CASE(49)\n    P1_CASE(50)\n    P1_CASE(51)\n    P1_CASE(52)\n    P1_CASE(53)\n    P1_CASE(54)\n    P1_CASE(55)\n    P1_CASE(56)\n    P1_CASE(57)\n    P1_CASE(58)\n    P1_CASE(59)\n    P1_CASE(60)\n    P1_CASE(61)\n    P1_CASE(62)\n    P1_CASE(63)\n    P1_CASE(64)\n#endif\n#undef P1_CASE\n    default:\n      hpcbench::fatal(\"panel width out of range\");\n  }\n}\n\n// ---- rung 5: the packed representation -------------------------------------\n\n// One row's worth of decoded non-zeros, owned by one thread.\nstruct Scratch {\n  std::unique_ptr<std::uint32_t[]> genes;  // rung 9: gene * k when it fits in u32\n  std::unique_ptr<float[]> counts;         // rung 9: f32, exact for integer counts\n  explicit Scratch(std::uint64_t cap)\n      : genes(new std::uint32_t[cap > 0 ? cap : 1]), counts(new float[cap > 0 ? cap : 1]) {}\n};\n\n// ---- rung 7: gene blocks --------------------------------------------------\n\n// The genes [b * genes, min((b + 1) * genes, n_genes)) form block b of W.\nstruct Blocks {\n  std::uint32_t genes;  // genes per block (the last one may be smaller)\n  std::uint32_t count;  // number of blocks; 1 = W fits, no blocking\n};\n\n// Where row i's decoding stopped at the end of the previous block.\nstruct Cursor {\n  std::uint32_t code;  // next code byte (packed) / next non-zero (wide)\n  std::uint32_t val;   // next value byte (packed only)\n  std::uint32_t gene;  // last gene decoded (packed only; 0xFFFFFFFF = none yet)\n};\n\nstruct PackedRows {\n  const std::uint32_t* code_ptr;  // byte offset of each row in the code stream\n  const std::uint32_t* val_ptr;   // byte offset of each row in the value stream\n  const std::uint8_t* code;\n  const std::uint8_t* vals;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;      // prefetch distance in non-zeros; 0 = off\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;         // one per row; used only when blocks.count > 1\n  std::uint32_t scale;     // rung 9: k when gene * k fits in u32, else 1\n  bool pre;                // rung 9: true when the scratch holds gene * k\n\n  // Decode row i into s.genes / s.counts; returns its number of non-zeros.\n  std::uint32_t decode(std::uint64_t i, Scratch& s) const {\n    const std::uint8_t* cp = code + code_ptr[i];\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + val_ptr[i];\n    std::uint32_t* genes = s.genes.get();\n    float* counts = s.counts.get();\n    std::uint32_t g = 0xFFFFFFFFu;  // \"gene -1\": the first gap is gene + 1\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = *cp++;\n      std::uint32_t gap = b & 0x7Fu;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[0]) | (std::uint32_t(cp[1]) << 8);\n        cp += 2;\n      }\n      g += gap;\n      // Count: 1 if bit 7 is set, else the next byte of the value stream.\n      // Written as arithmetic on purpose: GCC compiles `one ? 1 : peek` into a\n      // branch, and a 70/30 coin flip per non-zero is a branch no predictor can\n      // learn (measured: ~9 cycles per non-zero instead of ~3).\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;   // 1 if the count is in the value stream\n      const std::uint32_t peek = *vp;                 // always in bounds: stream is padded\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);  // peek, or 1\n      vp += uses_val;                                 // consume a value byte only if used\n      if (v == 0) {                                   // escape: two-byte count\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = g * scale;  // rung 9: gene * k, so the kernel only adds\n      counts[n] = static_cast<float>(v);\n      ++n;\n    }\n    return n;\n  }\n\n  // Rung 7: decode only the non-zeros of row i whose gene is below gene_end,\n  // starting where cur says the previous block stopped; leave cur at the\n  // first non-zero of a later block. Same decoder as above, plus a peek at the\n  // next gene before consuming it.\n  std::uint32_t decode_until(std::uint64_t i, std::uint32_t gene_end, Cursor& cur,\n                             Scratch& s) const {\n    const std::uint8_t* cp = code + cur.code;\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + cur.val;\n    std::uint32_t* genes = s.genes.get();\n    float* counts = s.counts.get();\n    std::uint32_t g = cur.gene;\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = cp[0];\n      std::uint32_t gap = b & 0x7Fu, len = 1;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[1]) | (std::uint32_t(cp[2]) << 8);\n        len = 3;\n      }\n      if (g + gap >= gene_end) break;  // belongs to a later block: leave it for then\n      cp += len;\n      g += gap;\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;\n      const std::uint32_t peek = *vp;\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);\n      vp += uses_val;\n      if (v == 0) {\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = g * scale;  // rung 9: gene * k, so the kernel only adds\n      counts[n] = static_cast<float>(v);\n      ++n;\n    }\n    cur = Cursor{static_cast<std::uint32_t>(cp - code), static_cast<std::uint32_t>(vp - vals), g};\n    return n;\n  }\n\n  // Rows [r0, r1), gene block b.\n  template <bool PF, bool PRE>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint32_t gene_end = static_cast<std::uint32_t>(\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes));\n    const bool accumulate = b > 0;  // later blocks add to the partial sums in Y\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t n;\n      if (blocks.count == 1) {\n        n = decode(i, s);  // W fits: the whole row at once, as in rung 6\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur = Cursor{code_ptr[i], val_ptr[i], 0xFFFFFFFFu};\n        n = decode_until(i, gene_end, cur, s);\n        // Nothing of this row in block b: its partial sums in Y are already\n        // right. (Block 0 still writes every row, so Y never relies on the\n        // harness having zero-filled it.)\n        if (n == 0 && accumulate) continue;\n      }\n      const std::uint32_t* genes = s.genes.get();\n      const float* counts = s.counts.get();\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, PRE>(genes, counts, n, W + c0, k, yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, PRE>(rem, genes, counts, n, W + full, k, yi + full, dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    if (dist > 0) {\n      if (pre) rows<true, true>(r0, r1, b, s);\n      else rows<true, false>(r0, r1, b, s);\n    } else {\n      if (pre) rows<false, true>(r0, r1, b, s);\n      else rows<false, false>(r0, r1, b, s);\n    }\n  }\n};\n\n// The wide fallback prepare writes when the packed encoding cannot represent\n// the input exactly: plain u32 / f64 CSR, handled exactly as rung 4 did.\nstruct WideRows {\n  const std::uint32_t* row_nnz;\n  const std::uint32_t* col;\n  const double* val;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;\n\n  template <bool PF>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint64_t gene_end =\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes);\n    const bool accumulate = b > 0;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t q0, n;\n      if (blocks.count == 1) {\n        q0 = row_nnz[i];\n        n = row_nnz[i + 1] - q0;\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur.code = row_nnz[i];\n        q0 = cur.code;\n        std::uint32_t q = q0;\n        while (q < row_nnz[i + 1] && col[q] < gene_end) ++q;\n        n = q - q0;\n        cur.code = q;\n        if (n == 0 && accumulate) continue;\n      }\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, false>(col + q0, val + q0, n, W + c0, k, yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, false>(rem, col + q0, val + q0, n, W + full, k, yi + full, dist,\n                                 accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch&) const {\n    if (dist > 0) rows<true>(r0, r1, b);\n    else rows<false>(r0, r1, b);\n  }\n};\n\n// ---- rung 6: cache sizes and the prefetch policy ---------------------------\n\nstruct Caches {\n  std::uint64_t l1d, l2, llc;  // bytes; llc = the largest cache shared by a cluster/socket\n  std::uint64_t l2_min;        // rung 7: the smallest L2 any thread may run on\n};\n\n#if defined(__APPLE__)\nstd::uint64_t sysctl_u64(const char* name) {\n  std::uint64_t v = 0;\n  std::size_t len = sizeof(v);\n  if (sysctlbyname(name, &v, &len, nullptr, 0) != 0) return 0;\n  return len == 4 ? static_cast<std::uint32_t>(v) : v;\n}\n#endif\n\n// This machine's cache sizes. On Apple silicon the performance cluster's\n// values (perflevel0: 128 KB L1d, 16 MB L2 on an M4) -- the L2 is the last\n// level each cluster owns. On Linux, glibc's sysconf. Defaults where unknown.\nCaches query_caches() {\n  Caches c{0, 0, 0, 0};\n#if defined(__APPLE__)\n  c.l1d = sysctl_u64(\"hw.perflevel0.l1dcachesize\");\n  c.l2 = sysctl_u64(\"hw.perflevel0.l2cachesize\");\n  if (c.l1d == 0) c.l1d = sysctl_u64(\"hw.l1dcachesize\");\n  if (c.l2 == 0) c.l2 = sysctl_u64(\"hw.l2cachesize\");\n  c.llc = c.l2;\n  c.l2_min = sysctl_u64(\"hw.perflevel1.l2cachesize\");  // efficiency cluster, if any\n#elif defined(__linux__) && defined(_SC_LEVEL1_DCACHE_SIZE)\n  const long l1 = sysconf(_SC_LEVEL1_DCACHE_SIZE);\n  const long l2 = sysconf(_SC_LEVEL2_CACHE_SIZE);\n  const long l3 = sysconf(_SC_LEVEL3_CACHE_SIZE);\n  c.l1d = l1 > 0 ? static_cast<std::uint64_t>(l1) : 0;\n  c.l2 = l2 > 0 ? static_cast<std::uint64_t>(l2) : 0;\n  c.llc = l3 > 0 ? static_cast<std::uint64_t>(l3) : c.l2;\n#endif\n  if (c.l1d == 0) c.l1d = 32 << 10;  // unknown: a typical L1d\n  if (c.l2 == 0) c.l2 = 1 << 20;     // unknown: a typical per-core L2\n  if (c.llc == 0) c.llc = 8 << 20;   // unknown: a typical shared L3\n  if (c.llc < c.l2) c.llc = c.l2;\n  if (c.l2_min == 0 || c.l2_min > c.l2) c.l2_min = c.l2;\n  return c;\n}\n\n// Prefetch distance for this run: 0 (off) when W fits in half of L2, else\n// P1_PREFETCH_DIST or 8 non-zeros ahead.\nstd::uint32_t prefetch_distance(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  std::uint32_t dist = 8;\n  if (const char* s = std::getenv(\"P1_PREFETCH_DIST\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v >= 0) dist = static_cast<std::uint32_t>(v);\n  }\n  const std::uint64_t w_bytes = n_genes * k * sizeof(double);\n  return w_bytes > c.l2 / 2 ? dist : 0;\n}\n\n// Rung 7: how to cut W into gene blocks. No blocking while W fits in the\n// smallest L2; otherwise blocks of a quarter of that L2. P1_W_BLOCK_BYTES sets\n// the block size and blocks whenever W is larger. Blocks are made equal in\n// size, and there are never more than kMaxBlocks of them.\nBlocks plan_blocks(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  const std::uint64_t row_bytes = k * sizeof(double);\n  const std::uint64_t w_bytes = n_genes * row_bytes;\n  std::uint64_t budget = c.l2_min / 4, fits = c.l2_min;\n  if (const char* s = std::getenv(\"P1_W_BLOCK_BYTES\")) {\n    const long long v = std::strtoll(s, nullptr, 10);\n    if (v > 0) budget = fits = static_cast<std::uint64_t>(v);\n  }\n  if (n_genes == 0 || row_bytes == 0 || w_bytes <= fits)\n    return Blocks{static_cast<std::uint32_t>(n_genes), 1};\n  const std::uint64_t max_genes = std::max<std::uint64_t>(1, budget / row_bytes);\n  std::uint64_t count = std::min<std::uint64_t>((n_genes + max_genes - 1) / max_genes, kMaxBlocks);\n  const std::uint64_t genes = (n_genes + count - 1) / count;\n  count = (n_genes + genes - 1) / genes;\n  return Blocks{static_cast<std::uint32_t>(genes), static_cast<std::uint32_t>(count)};\n}\n\n// ---- threading: rung 3's, with one Scratch per thread and, new in rung 7, a\n// barrier between gene blocks ----------------------------------------------\n\nunsigned threads_granted() {\n#if defined(P1_OPENMP)\n  return static_cast<unsigned>(std::max(1, omp_get_max_threads()));\n#elif defined(P1_STD_THREAD)\n  if (const char* s = std::getenv(\"OMP_NUM_THREADS\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v > 0) return static_cast<unsigned>(v);\n  }\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0)\n    return static_cast<unsigned>(CPU_COUNT(&set));\n#endif\n  const unsigned hc = std::thread::hardware_concurrency();\n  return hc > 0 ? hc : 1;\n#else\n  return 1;\n#endif\n}\n\n#if defined(P1_STD_THREAD)\n// A reusable barrier (C++17 has none): each thread waits here until all have\n// finished the current gene block. The party count starts at the number of\n// threads planned and is lowered if creating one of them fails.\n//\n// A waiter first spins for up to 50 us: the other threads are usually only\n// microseconds behind, and a sleep plus wake-up through the kernel costs tens\n// of microseconds on macOS -- with 27 blocks at k = 256, that adds up. After\n// 50 us it sleeps, so an oversubscribed machine does not burn cores.\nclass Barrier {\n public:\n  explicit Barrier(unsigned parties) : parties_(parties) {}\n  void set_parties(unsigned parties) {\n    std::lock_guard<std::mutex> lock(m_);\n    parties_ = parties;\n    if (arrived_ > 0 && arrived_ >= parties_) release();\n  }\n  void wait() {\n    std::unique_lock<std::mutex> lock(m_);\n    const unsigned gen = generation_.load(std::memory_order_relaxed);\n    if (++arrived_ >= parties_) {\n      release();\n      return;\n    }\n    lock.unlock();\n    const auto until = std::chrono::steady_clock::now() + std::chrono::microseconds(50);\n    while (std::chrono::steady_clock::now() < until)\n      if (generation_.load(std::memory_order_acquire) != gen) return;\n    lock.lock();\n    cv_.wait(lock, [&] { return generation_.load(std::memory_order_relaxed) != gen; });\n  }\n\n private:\n  void release() {  // called with m_ held\n    arrived_ = 0;\n    generation_.fetch_add(1, std::memory_order_release);\n    cv_.notify_all();\n  }\n  std::mutex m_;\n  std::condition_variable cv_;\n  unsigned parties_, arrived_ = 0;\n  std::atomic<unsigned> generation_{0};\n};\n#endif\n\ntemplate <class Rows>\nvoid run_rows(const Rows& rows, const std::uint32_t* row_nnz, std::uint64_t n_cells,\n              std::uint64_t k, std::uint64_t scratch_cap, std::uint32_t n_blocks) {\n  const std::uint64_t nnz = row_nnz[n_cells];\n  const std::uint64_t floor_threads = std::max<std::uint64_t>(1, nnz * k / kMinWorkPerThread);\n  const unsigned nt =\n      static_cast<unsigned>(std::min<std::uint64_t>(threads_granted(), floor_threads));\n  if (nt <= 1) {\n    Scratch s(scratch_cap);\n    for (std::uint32_t b = 0; b < n_blocks; ++b) rows(0, n_cells, b, s);\n    return;\n  }\n\n  std::vector<std::uint32_t> bounds;\n  bounds.reserve(static_cast<std::size_t>(nt * kChunksPerThread + 2));\n  bounds.push_back(0);\n  const std::uint64_t target = std::max<std::uint64_t>(1, nnz / (nt * kChunksPerThread));\n  std::uint64_t chunk_start = row_nnz[0];\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    if (row_nnz[i + 1] - chunk_start >= target) {\n      bounds.push_back(static_cast<std::uint32_t>(i + 1));\n      chunk_start = row_nnz[i + 1];\n    }\n  if (bounds.back() != n_cells) bounds.push_back(static_cast<std::uint32_t>(n_cells));\n  const std::size_t n_chunks = bounds.size() - 1;\n\n#if defined(P1_OPENMP)\n  const long nc = static_cast<long>(n_chunks);\n#pragma omp parallel num_threads(nt)\n  {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      // The implicit barrier at the end of this loop keeps all threads on the\n      // same block of W: nobody starts block b + 1 before every row has done b.\n#pragma omp for schedule(dynamic, 1)\n      for (long c = 0; c < nc; ++c) rows(bounds[c], bounds[c + 1], b, s);\n    }\n  }\n#elif defined(P1_STD_THREAD)\n  // One chunk counter per block, so a block's chunks are handed out afresh.\n  std::unique_ptr<std::atomic<std::size_t>[]> next(new std::atomic<std::size_t>[n_blocks]);\n  for (std::uint32_t b = 0; b < n_blocks; ++b) next[b].store(0, std::memory_order_relaxed);\n  Barrier barrier(nt);\n  auto worker = [&]() {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      for (;;) {\n        const std::size_t c = next[b].fetch_add(1, std::memory_order_relaxed);\n        if (c >= n_chunks) break;\n        rows(bounds[c], bounds[c + 1], b, s);\n      }\n      if (b + 1 < n_blocks) barrier.wait();  // one block: never waits, as rung 6\n    }\n  };\n  std::vector<std::thread> helpers;\n  helpers.reserve(nt - 1);\n  for (unsigned t = 1; t < nt; ++t) {\n    try {\n      helpers.emplace_back(worker);\n    } catch (...) {\n      break;\n    }\n  }\n  if (helpers.size() + 1 < nt) barrier.set_parties(static_cast<unsigned>(helpers.size() + 1));\n  worker();\n  for (std::thread& h : helpers) h.join();\n#endif\n}\n\n}  // namespace\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  const Mapped m = w.map(\"csr_packed.bin\");\n  if (m.bytes() < 128) fatal(\"csr_packed.bin is truncated; re-run prepare\");\n  const std::uint64_t* hdr = m.as<std::uint64_t>(0);\n  const std::uint64_t n_cells = hdr[0], n_genes = hdr[1], format = hdr[3];\n  if (n_cells != p.n_cells || n_genes != p.n_genes)\n    fatal(\"csr_packed.bin dimensions do not match the harness; re-run prepare\");\n  if (m.bytes() != hdr[10]) fatal(\"csr_packed.bin has the wrong size; re-run prepare\");\n  const std::uint32_t* row_nnz = m.as<std::uint32_t>(hdr[5]);\n\n  const Caches caches = query_caches();\n  const Blocks blocks = plan_blocks(n_genes, p.k, caches);\n  // Prefetch when the working set -- now one block of W -- outgrows L2.\n  const std::uint32_t dist = prefetch_distance(blocks.genes, p.k, caches);\n  std::unique_ptr<Cursor[]> cursors(blocks.count > 1 ? new Cursor[n_cells] : nullptr);\n\n  // Rung 9: the decoded rows hold gene * k, unless that would overflow a u32.\n  const bool pre = n_genes * p.k <= 0xFFFFFFFFull;\n  const std::uint32_t scale = pre ? static_cast<std::uint32_t>(p.k) : 1u;\n\n  if (format == 3) {\n    const PackedRows rows{m.as<std::uint32_t>(hdr[6]), m.as<std::uint32_t>(hdr[7]),\n                          m.as<std::uint8_t>(hdr[8]),  m.as<std::uint8_t>(hdr[9]),\n                          p.W, y.data, p.k, dist, n_genes, blocks, cursors.get(), scale, pre};\n    run_rows(rows, row_nnz, n_cells, p.k, hdr[4], blocks.count);\n  } else if (format == 2) {\n    const WideRows rows{row_nnz, m.as<std::uint32_t>(hdr[6]), m.as<double>(hdr[7]),\n                        p.W, y.data, p.k, dist, n_genes, blocks, cursors.get()};\n    run_rows(rows, row_nnz, n_cells, p.k, 0, blocks.count);\n  } else {\n    fatal(\"csr_packed.bin has an unknown format; re-run prepare\");\n  }\n}",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    },
    {
      "id": "7a32982a-d401-47ca-94e1-515f03e0515b",
      "name": "ahsan-09a",
      "is_baseline": false,
      "version": 3,
      "owner_handle": "kowshid",
      "updated_at": "2026-09-23T01:00:24.547683+00:00",
      "reason": "no run on your account yet",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- RUNG 9a: unchanged from rung 5. UNTIMED.\n//\n// Rungs 6, 7 and 9 change only solution.cpp. Rung 7 blocks W by gene without\n// any new offsets in this file: blocks are visited in ascending gene order, so\n// each row's decoder simply stops at a block boundary and resumes there.\n//\n// ---- rung 5 notes, unchanged ----\n//\n// RUNG 5: a data structure designed from the data.\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// The ONE change from rung 4: how each non-zero is encoded. Rung 2 spent 4\n// bytes on every non-zero (u16 gene + u16 count). The data says we can do with\n// about 1.3, by using what Lecture 3 (slides 28-34) points out about it:\n//\n//   * 69.8% of the counts are exactly 1.          -> say \"1\" with a single bit\n//   * genes are sorted within a row, gaps are small -> store the gap, not the\n//     (median 10, 99.8% under 128)                   gene (delta encoding)\n//   * counts other than 1 are small (max 419)      -> one byte each, rarely more\n//\n// Encoding, per row, in gene order:\n//\n//   code stream, one byte per non-zero:\n//     bit 7     1 if the count is 1, else 0 (count is in the value stream)\n//     bits 0-6  gap to the previous gene, 1..127 (the first gap is gene + 1)\n//     0 in bits 0-6 is an escape: the gap follows as two bytes (u16)\n//   value stream, one byte per non-zero whose count is not 1:\n//     2..255    the count itself\n//     0         escape: the count follows as two bytes (u16)\n//\n// Unlike the lecture's grouped-by-value layout, this keeps every row in gene\n// order, so the kernel adds the terms of each Y element in exactly the same\n// order as before: the compression costs no exactness.\n//\n// Each row starts at a recorded byte offset in both streams, so any row can be\n// decoded on its own -- which is what lets rung 3's threads keep splitting the\n// rows freely. A per-row count of non-zeros is kept for the thread balancing.\n//\n// csr_packed.bin layout, little-endian:\n//\n//     u64 header[16]:\n//       [0] n_cells  [1] n_genes  [2] nnz  [3] format  [4] max_row_nnz\n//       [5] off_row_nnz  [6] off_a  [7] off_b  [8] off_c  [9] off_d\n//       [10] total_bytes\n//     format 3 (packed): a = u32 code_ptr[n_cells + 1]  (byte offset of each row)\n//                        b = u32 val_ptr[n_cells + 1]   (byte offset of each row)\n//                        c = u8 code stream, d = u8 value stream (+64 pad bytes)\n//     format 2 (wide fallback, when a gene index or count does not fit):\n//                        a = u32 gene index[nnz], b = f64 value[nnz]\n//     u32 row_nnz[n_cells + 1] at off_row_nnz: prefix count of non-zeros\n//\n// Every section starts on a 64-byte boundary.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <vector>\n\n#if defined(_OPENMP)\n#include <omp.h>\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kFormatWide = 2;\nconstexpr std::uint64_t kFormatPacked = 3;\n\n// Diagnostic only (unchanged from rung 3): mirrors threads_granted() in solve.\nvoid report_threads() {\n  const char* env = std::getenv(\"OMP_NUM_THREADS\");\n#if defined(_OPENMP)\n  std::fprintf(stderr, \"prepare: solve will be granted %d threads (OpenMP; OMP_NUM_THREADS=%s)\\n\",\n               omp_get_max_threads(), env ? env : \"unset\");\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n  unsigned n = std::thread::hardware_concurrency();\n  const char* source = \"hardware threads\";\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0) {\n    n = static_cast<unsigned>(CPU_COUNT(&set));\n    source = \"CPUs in the affinity mask\";\n  }\n#endif\n  if (env && std::strtol(env, nullptr, 10) > 0) {\n    n = static_cast<unsigned>(std::strtol(env, nullptr, 10));\n    source = \"OMP_NUM_THREADS\";\n  }\n  std::fprintf(stderr, \"prepare: solve will be granted %u threads (std::thread; from %s)\\n\", n,\n               source);\n#else\n  std::fprintf(stderr, \"prepare: solve will run serially (browser build, no threads)\\n\");\n#endif\n}\n\nstd::uint64_t pad64(std::uint64_t x) { return (x + 63) & ~std::uint64_t(63); }\n\nvoid pad_to(hpcbench::Writer& w, std::uint64_t& pos, std::uint64_t target) {\n  static const unsigned char zeros[64] = {};\n  while (pos < target) {\n    const std::uint64_t n = std::min<std::uint64_t>(64, target - pos);\n    w.write(zeros, static_cast<std::size_t>(n));\n    pos += n;\n  }\n}\n\nvoid put(hpcbench::Writer& w, std::uint64_t& pos, const void* data, std::uint64_t bytes) {\n  w.write(data, static_cast<std::size_t>(bytes));\n  pos += bytes;\n}\n\n}  // namespace\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  const std::uint64_t n_cells = in.n_cells;\n  const std::uint64_t n_genes = in.n_genes;\n\n  // Pass 1: can the packed encoding represent this input exactly?\n  // Gaps must fit u16 (gene index <= 65,534) and counts must be integers\n  // 1..65,535. Otherwise fall back to u32 index + f64 value.\n  bool packable = n_genes <= 65535;\n  std::uint64_t nnz = 0, max_row = 0;\n  std::vector<std::uint32_t> row_nnz(n_cells + 1, 0);\n  for (std::uint64_t i = 0; i < n_cells; ++i) {\n    const double* xi = in.X + i * n_genes;\n    std::uint64_t row = 0;\n    for (std::uint64_t g = 0; g < n_genes; ++g) {\n      const double x = xi[g];\n      if (x == 0.0) continue;\n      ++row;\n      if (!(x >= 1.0 && x <= 65535.0 && x == std::floor(x))) packable = false;\n    }\n    nnz += row;\n    if (nnz > UINT32_MAX) fatal(\"more than 2^32 non-zeros: u32 offsets overflow\");\n    max_row = std::max(max_row, row);\n    row_nnz[i + 1] = static_cast<std::uint32_t>(nnz);\n  }\n\n  std::uint64_t header[16] = {};\n  header[0] = n_cells;\n  header[1] = n_genes;\n  header[2] = nnz;\n  header[4] = max_row;\n  header[5] = 128;  // off_row_nnz\n  const std::uint64_t after_row_nnz = pad64(header[5] + 4 * (n_cells + 1));\n\n  Writer w = out.create(\"csr_packed.bin\");\n  std::uint64_t pos = 0;\n\n  if (packable) {\n    // Pass 2: encode.\n    std::vector<std::uint8_t> code, vals;\n    std::vector<std::uint32_t> code_ptr(n_cells + 1, 0), val_ptr(n_cells + 1, 0);\n    code.reserve(static_cast<std::size_t>(nnz + nnz / 64));\n    vals.reserve(static_cast<std::size_t>(nnz / 2));\n    std::uint64_t gap_escapes = 0, val_escapes = 0;\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      std::uint64_t prev = ~std::uint64_t(0);  // \"gene -1\": the first gap is gene + 1\n      for (std::uint64_t g = 0; g < n_genes; ++g) {\n        const double x = xi[g];\n        if (x == 0.0) continue;\n        const std::uint64_t gap = g - prev;  // 1..65,535\n        prev = g;\n        const std::uint32_t v = static_cast<std::uint32_t>(x);\n        const std::uint8_t one = (v == 1) ? 0x80 : 0x00;\n        if (gap < 128) {\n          code.push_back(static_cast<std::uint8_t>(one | gap));\n        } else {\n          code.push_back(one);  // gap field 0 = escape\n          code.push_back(static_cast<std::uint8_t>(gap & 0xFF));\n          code.push_back(static_cast<std::uint8_t>(gap >> 8));\n          ++gap_escapes;\n        }\n        if (v != 1) {\n          if (v < 256) {\n            vals.push_back(static_cast<std::uint8_t>(v));\n          } else {\n            vals.push_back(0);  // escape\n            vals.push_back(static_cast<std::uint8_t>(v & 0xFF));\n            vals.push_back(static_cast<std::uint8_t>(v >> 8));\n            ++val_escapes;\n          }\n        }\n      }\n      if (code.size() > UINT32_MAX || vals.size() > UINT32_MAX)\n        fatal(\"packed streams exceed 4 GB\");\n      code_ptr[i + 1] = static_cast<std::uint32_t>(code.size());\n      val_ptr[i + 1] = static_cast<std::uint32_t>(vals.size());\n    }\n\n    header[3] = kFormatPacked;\n    header[6] = after_row_nnz;                               // code_ptr\n    header[7] = pad64(header[6] + 4 * (n_cells + 1));        // val_ptr\n    header[8] = pad64(header[7] + 4 * (n_cells + 1));        // code stream\n    header[9] = pad64(header[8] + code.size());              // value stream\n    header[10] = header[9] + vals.size() + 64;               // + pad: the decoder\n                                                             // may peek one byte ahead\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, code_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[7]);\n    put(w, pos, val_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[8]);\n    put(w, pos, code.data(), code.size());\n    pad_to(w, pos, header[9]);\n    put(w, pos, vals.data(), vals.size());\n    pad_to(w, pos, header[10]);\n    w.close();\n\n    std::fprintf(stderr,\n                 \"prepare: packed CSR, %llu nonzeros, %.2f MB (%.2f bytes/nnz); \"\n                 \"code %.2f MB, values %.2f MB, %llu gap and %llu value escapes\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6,\n                 double(header[10]) / double(nnz), double(code.size()) / 1e6,\n                 double(vals.size()) / 1e6, (unsigned long long)gap_escapes,\n                 (unsigned long long)val_escapes);\n  } else {\n    std::vector<std::uint32_t> col;\n    std::vector<double> val;\n    col.reserve(static_cast<std::size_t>(nnz));\n    val.reserve(static_cast<std::size_t>(nnz));\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      for (std::uint64_t g = 0; g < n_genes; ++g)\n        if (xi[g] != 0.0) {\n          col.push_back(static_cast<std::uint32_t>(g));\n          val.push_back(xi[g]);\n        }\n    }\n    header[3] = kFormatWide;\n    header[6] = after_row_nnz;                      // u32 gene index\n    header[7] = pad64(header[6] + 4 * nnz);         // f64 value\n    header[10] = header[7] + 8 * nnz;\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, col.data(), 4 * nnz);\n    pad_to(w, pos, header[7]);\n    put(w, pos, val.data(), 8 * nnz);\n    w.close();\n    std::fprintf(stderr, \"prepare: wide fallback CSR (u32 + f64), %llu nonzeros, %.2f MB\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6);\n  }\n  report_threads();\n}",
        "src/solution.cpp": "// src/solution.cpp -- RUNG 9a (ablation): rung 9 with only the index change.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Stacked on rung 7. The ONE change: what a decoded row stores, and therefore\n// what the kernel re-reads once per output panel.\n//\n// Rung 7 decoded each non-zero into a u32 gene index and an f64 count, 12 bytes\n// each, and the kernel turned the gene into an address with a 64-bit multiply\n//     w = W + col[q] * k\n// EVERY time it swept the row -- once per panel, so eight times at k = 256.\n//\n// Rung 9 stores instead:\n//   * the element offset of the gene's W row, gene * k, in the same u32. The\n//     multiply happens once per non-zero while decoding rather than once per\n//     non-zero per panel; the kernel just adds. Used only when n_genes * k fits\n//     in 32 bits (3.5 M at k = 256, so always here); otherwise the rung 7 path\n//     runs unchanged.\n//   * counts stay f64. This is the half of rung 9 that this ablation removes:\n//     rung 9 also narrows them to f32 (exact, since the counts are integers\n//     below 2^24), which shrinks a decoded row from 12 to 8 bytes per non-zero\n//     but adds one f32 -> f64 widening per non-zero per panel. On a 1-core Xeon\n//     that trade measured 0.96x at k = 8-16 and neutral above, so rung 9a keeps\n//     the index change alone. Rung 9 vs rung 9a is the measurement.\n//\n// What it does NOT do: narrow W or Y. Both are f64 by the harness's interface,\n// and W's values are arbitrary doubles in [-1, 1), so no narrower type holds\n// them exactly. Narrowing W to f32 would be about 1e-7 relative error -- far\n// inside the task's 1% tolerance, but no longer the baseline's answer, so it is\n// not part of a bitwise-exact rung.\n//\n// Correctness: same terms, same ascending gene order, same fused multiply-adds\n// on the same double values. Bit-for-bit the baseline's.\n//\n// Knobs, as in rung 7: P1_W_BLOCK_BYTES, P1_PREFETCH_DIST.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cstdint>\n#include <cstdlib>\n#include <cstring>\n#include <memory>\n#include <vector>\n\n#if defined(__APPLE__)\n#include <sys/sysctl.h>\n#elif defined(__linux__)\n#include <unistd.h>\n#endif\n\n#if defined(_OPENMP)\n#include <omp.h>\n#define P1_OPENMP 1\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#define P1_STD_THREAD 1\n#include <atomic>\n#include <chrono>\n#include <condition_variable>\n#include <mutex>\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\n#ifndef P1_PANEL  // override with -DP1_PANEL=... (up to 64) in CXXFLAGS to experiment\n#define P1_PANEL 32\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kMinWorkPerThread = std::uint64_t(1) << 19;  // multiply-adds\nconstexpr std::uint64_t kChunksPerThread = 32;\nconstexpr std::uint64_t kMaxBlocks = 64;  // each block costs a barrier\n\n// ---- rung 4's kernel, unchanged -------------------------------------------\n\ntypedef double v4d __attribute__((vector_size(32)));  // four doubles\n\nconstexpr int kPanel = P1_PANEL;\n\n// acc + x * w, rounded exactly once, the way the baseline's `y += x * w` is.\n//\n// Where the target has a hardware fused multiply-add (__FP_FAST_FMA: every\n// arm64, x86 with FMA), the compiler fuses the baseline's statement into one\n// instruction. It does not reliably fuse ours: GCC's tuning for recent Intel\n// cores (\"avoid FMA chains\") splits a loop-carried accumulation into a multiply\n// and an add when only one or two accumulator vectors carry it -- exactly our\n// narrow panels -- which rounds twice. So the fusion is spelled out here.\n// Without hardware FMA the baseline rounds twice, and so does this.\ninline double madd(double x, double w, double acc) {\n#if defined(__FP_FAST_FMA)\n  return __builtin_fma(x, w, acc);\n#else\n  return x * w + acc;\n#endif\n}\n\ninline v4d vmadd(v4d x, v4d w, v4d acc) {\n#if defined(__FP_FAST_FMA)\n  // Four scalar fused multiply-adds; every compiler tested turns them into one\n  // vector FMA (vfmadd231pd on x86, two fmla on arm64).\n  return v4d{__builtin_fma(x[0], w[0], acc[0]), __builtin_fma(x[1], w[1], acc[1]),\n             __builtin_fma(x[2], w[2], acc[2]), __builtin_fma(x[3], w[3], acc[3])};\n#else\n  return x * w + acc;\n#endif\n}\n\n// One row, one panel of P output columns:  yc[0:P] = sum over the row's\n// non-zeros of x * Wc[g*k + 0:P], accumulated in registers. With `accumulate`\n// the sums start from what yc already holds -- the partial sums left there by\n// the previous gene blocks -- instead of from zero.\n//\n// noinline: each width is compiled as its own function. Measured necessity,\n// not style: GCC 13 inlined all 32 prefetching widths into the switch below and\n// the result ran k = 16 at 18.1 ms instead of 10.1 ms (k = 13: 15.1 vs 8.5) at\n// every prefetch distance from 1 to 64 -- a code-generation effect, not a\n// memory one. The same attribute on rungs 4 and 5 measured no difference.\n// PRE: col[] already holds gene * k (rung 9's decoded rows), so the kernel adds\n// instead of multiplying. PRE = false keeps rung 7's behaviour for plain CSR.\ntemplate <int P, bool PF, bool PRE, class Idx, class Val>\n__attribute__((noinline)) void row_panel(const Idx* col, const Val* val, std::uint32_t n,\n                                         const double* Wc, std::uint64_t k, double* yc,\n                                         std::uint32_t dist, bool accumulate) {\n  constexpr int V = P / 4;  // full 4-wide vectors\n  constexpr int R = P % 4;  // 1-3 leftover columns, scalar\n  v4d acc[V > 0 ? V : 1];\n  double tail[R > 0 ? R : 1];\n  if (accumulate) {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) std::memcpy(&acc[v], yc + 4 * v, sizeof(v4d));\n    for (int r = 0; r < R; ++r) tail[r] = yc[4 * V + r];\n  } else {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) acc[v] = v4d{0.0, 0.0, 0.0, 0.0};\n    for (int r = 0; r < R; ++r) tail[r] = 0.0;\n  }\n\n  for (std::uint32_t q = 0; q < n; ++q) {\n    if (PF && q + dist < n) {\n      // The W segment this row will need `dist` non-zeros from now: one\n      // prefetch per 64 bytes (a cache line on x86, half of one on Apple).\n      const std::uint64_t ahead_off = PRE ? static_cast<std::uint64_t>(col[q + dist])\n                                          : static_cast<std::uint64_t>(col[q + dist]) * k;\n      const char* ahead = reinterpret_cast<const char*>(Wc + ahead_off);\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n    const double x = static_cast<double>(val[q]);\n    const v4d xv = v4d{x, x, x, x};\n    const double* w = Wc + (PRE ? static_cast<std::uint64_t>(col[q])\n                                : static_cast<std::uint64_t>(col[q]) * k);\n    // Fully unrolled: GCC stops at 8 vectors on its own and would otherwise\n    // keep a 64-wide panel's accumulators on the stack.\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) {\n      v4d wv;\n      std::memcpy(&wv, w + 4 * v, sizeof(wv));  // unaligned vector load\n      acc[v] = vmadd(xv, wv, acc[v]);\n    }\n    for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n  }\n\n#pragma GCC unroll 16\n  for (int v = 0; v < V; ++v) std::memcpy(yc + 4 * v, &acc[v], sizeof(v4d));\n  for (int r = 0; r < R; ++r) yc[4 * V + r] = tail[r];\n}\n\n// The switch: a run-time width selects a compile-time kernel.\ntemplate <bool PF, bool PRE, class Idx, class Val>\nvoid row_panel_any(int width, const Idx* col, const Val* val, std::uint32_t n,\n                   const double* Wc, std::uint64_t k, double* yc, std::uint32_t dist,\n                   bool accumulate) {\n  switch (width) {\n#define P1_CASE(P)                                                  \\\n  case P:                                                           \\\n    row_panel<P, PF, PRE>(col, val, n, Wc, k, yc, dist, accumulate);     \\\n    return;\n    P1_CASE(1)\n    P1_CASE(2)\n    P1_CASE(3)\n    P1_CASE(4)\n    P1_CASE(5)\n    P1_CASE(6)\n    P1_CASE(7)\n    P1_CASE(8)\n    P1_CASE(9)\n    P1_CASE(10)\n    P1_CASE(11)\n    P1_CASE(12)\n    P1_CASE(13)\n    P1_CASE(14)\n    P1_CASE(15)\n    P1_CASE(16)\n    P1_CASE(17)\n    P1_CASE(18)\n    P1_CASE(19)\n    P1_CASE(20)\n    P1_CASE(21)\n    P1_CASE(22)\n    P1_CASE(23)\n    P1_CASE(24)\n    P1_CASE(25)\n    P1_CASE(26)\n    P1_CASE(27)\n    P1_CASE(28)\n    P1_CASE(29)\n    P1_CASE(30)\n    P1_CASE(31)\n    P1_CASE(32)\n#if P1_PANEL > 32\n    P1_CASE(33)\n    P1_CASE(34)\n    P1_CASE(35)\n    P1_CASE(36)\n    P1_CASE(37)\n    P1_CASE(38)\n    P1_CASE(39)\n    P1_CASE(40)\n    P1_CASE(41)\n    P1_CASE(42)\n    P1_CASE(43)\n    P1_CASE(44)\n    P1_CASE(45)\n    P1_CASE(46)\n    P1_CASE(47)\n    P1_CASE(48)\n    P1_CASE(49)\n    P1_CASE(50)\n    P1_CASE(51)\n    P1_CASE(52)\n    P1_CASE(53)\n    P1_CASE(54)\n    P1_CASE(55)\n    P1_CASE(56)\n    P1_CASE(57)\n    P1_CASE(58)\n    P1_CASE(59)\n    P1_CASE(60)\n    P1_CASE(61)\n    P1_CASE(62)\n    P1_CASE(63)\n    P1_CASE(64)\n#endif\n#undef P1_CASE\n    default:\n      hpcbench::fatal(\"panel width out of range\");\n  }\n}\n\n// ---- rung 5: the packed representation -------------------------------------\n\n// One row's worth of decoded non-zeros, owned by one thread.\nstruct Scratch {\n  std::unique_ptr<std::uint32_t[]> genes;  // rung 9: gene * k when it fits in u32\n  std::unique_ptr<double[]> counts;\n  explicit Scratch(std::uint64_t cap)\n      : genes(new std::uint32_t[cap > 0 ? cap : 1]), counts(new double[cap > 0 ? cap : 1]) {}\n};\n\n// ---- rung 7: gene blocks --------------------------------------------------\n\n// The genes [b * genes, min((b + 1) * genes, n_genes)) form block b of W.\nstruct Blocks {\n  std::uint32_t genes;  // genes per block (the last one may be smaller)\n  std::uint32_t count;  // number of blocks; 1 = W fits, no blocking\n};\n\n// Where row i's decoding stopped at the end of the previous block.\nstruct Cursor {\n  std::uint32_t code;  // next code byte (packed) / next non-zero (wide)\n  std::uint32_t val;   // next value byte (packed only)\n  std::uint32_t gene;  // last gene decoded (packed only; 0xFFFFFFFF = none yet)\n};\n\nstruct PackedRows {\n  const std::uint32_t* code_ptr;  // byte offset of each row in the code stream\n  const std::uint32_t* val_ptr;   // byte offset of each row in the value stream\n  const std::uint8_t* code;\n  const std::uint8_t* vals;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;      // prefetch distance in non-zeros; 0 = off\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;         // one per row; used only when blocks.count > 1\n  std::uint32_t scale;     // rung 9: k when gene * k fits in u32, else 1\n  bool pre;                // rung 9: true when the scratch holds gene * k\n\n  // Decode row i into s.genes / s.counts; returns its number of non-zeros.\n  std::uint32_t decode(std::uint64_t i, Scratch& s) const {\n    const std::uint8_t* cp = code + code_ptr[i];\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + val_ptr[i];\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = 0xFFFFFFFFu;  // \"gene -1\": the first gap is gene + 1\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = *cp++;\n      std::uint32_t gap = b & 0x7Fu;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[0]) | (std::uint32_t(cp[1]) << 8);\n        cp += 2;\n      }\n      g += gap;\n      // Count: 1 if bit 7 is set, else the next byte of the value stream.\n      // Written as arithmetic on purpose: GCC compiles `one ? 1 : peek` into a\n      // branch, and a 70/30 coin flip per non-zero is a branch no predictor can\n      // learn (measured: ~9 cycles per non-zero instead of ~3).\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;   // 1 if the count is in the value stream\n      const std::uint32_t peek = *vp;                 // always in bounds: stream is padded\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);  // peek, or 1\n      vp += uses_val;                                 // consume a value byte only if used\n      if (v == 0) {                                   // escape: two-byte count\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = g * scale;  // rung 9: gene * k, so the kernel only adds\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    return n;\n  }\n\n  // Rung 7: decode only the non-zeros of row i whose gene is below gene_end,\n  // starting where cur says the previous block stopped; leave cur at the\n  // first non-zero of a later block. Same decoder as above, plus a peek at the\n  // next gene before consuming it.\n  std::uint32_t decode_until(std::uint64_t i, std::uint32_t gene_end, Cursor& cur,\n                             Scratch& s) const {\n    const std::uint8_t* cp = code + cur.code;\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + cur.val;\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = cur.gene;\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = cp[0];\n      std::uint32_t gap = b & 0x7Fu, len = 1;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[1]) | (std::uint32_t(cp[2]) << 8);\n        len = 3;\n      }\n      if (g + gap >= gene_end) break;  // belongs to a later block: leave it for then\n      cp += len;\n      g += gap;\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;\n      const std::uint32_t peek = *vp;\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);\n      vp += uses_val;\n      if (v == 0) {\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = g * scale;  // rung 9: gene * k, so the kernel only adds\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    cur = Cursor{static_cast<std::uint32_t>(cp - code), static_cast<std::uint32_t>(vp - vals), g};\n    return n;\n  }\n\n  // Rows [r0, r1), gene block b.\n  template <bool PF, bool PRE>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint32_t gene_end = static_cast<std::uint32_t>(\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes));\n    const bool accumulate = b > 0;  // later blocks add to the partial sums in Y\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t n;\n      if (blocks.count == 1) {\n        n = decode(i, s);  // W fits: the whole row at once, as in rung 6\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur = Cursor{code_ptr[i], val_ptr[i], 0xFFFFFFFFu};\n        n = decode_until(i, gene_end, cur, s);\n        // Nothing of this row in block b: its partial sums in Y are already\n        // right. (Block 0 still writes every row, so Y never relies on the\n        // harness having zero-filled it.)\n        if (n == 0 && accumulate) continue;\n      }\n      const std::uint32_t* genes = s.genes.get();\n      const double* counts = s.counts.get();\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, PRE>(genes, counts, n, W + c0, k, yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, PRE>(rem, genes, counts, n, W + full, k, yi + full, dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    if (dist > 0) {\n      if (pre) rows<true, true>(r0, r1, b, s);\n      else rows<true, false>(r0, r1, b, s);\n    } else {\n      if (pre) rows<false, true>(r0, r1, b, s);\n      else rows<false, false>(r0, r1, b, s);\n    }\n  }\n};\n\n// The wide fallback prepare writes when the packed encoding cannot represent\n// the input exactly: plain u32 / f64 CSR, handled exactly as rung 4 did.\nstruct WideRows {\n  const std::uint32_t* row_nnz;\n  const std::uint32_t* col;\n  const double* val;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;\n\n  template <bool PF>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint64_t gene_end =\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes);\n    const bool accumulate = b > 0;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t q0, n;\n      if (blocks.count == 1) {\n        q0 = row_nnz[i];\n        n = row_nnz[i + 1] - q0;\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur.code = row_nnz[i];\n        q0 = cur.code;\n        std::uint32_t q = q0;\n        while (q < row_nnz[i + 1] && col[q] < gene_end) ++q;\n        n = q - q0;\n        cur.code = q;\n        if (n == 0 && accumulate) continue;\n      }\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, false>(col + q0, val + q0, n, W + c0, k, yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, false>(rem, col + q0, val + q0, n, W + full, k, yi + full, dist,\n                                 accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch&) const {\n    if (dist > 0) rows<true>(r0, r1, b);\n    else rows<false>(r0, r1, b);\n  }\n};\n\n// ---- rung 6: cache sizes and the prefetch policy ---------------------------\n\nstruct Caches {\n  std::uint64_t l1d, l2, llc;  // bytes; llc = the largest cache shared by a cluster/socket\n  std::uint64_t l2_min;        // rung 7: the smallest L2 any thread may run on\n};\n\n#if defined(__APPLE__)\nstd::uint64_t sysctl_u64(const char* name) {\n  std::uint64_t v = 0;\n  std::size_t len = sizeof(v);\n  if (sysctlbyname(name, &v, &len, nullptr, 0) != 0) return 0;\n  return len == 4 ? static_cast<std::uint32_t>(v) : v;\n}\n#endif\n\n// This machine's cache sizes. On Apple silicon the performance cluster's\n// values (perflevel0: 128 KB L1d, 16 MB L2 on an M4) -- the L2 is the last\n// level each cluster owns. On Linux, glibc's sysconf. Defaults where unknown.\nCaches query_caches() {\n  Caches c{0, 0, 0, 0};\n#if defined(__APPLE__)\n  c.l1d = sysctl_u64(\"hw.perflevel0.l1dcachesize\");\n  c.l2 = sysctl_u64(\"hw.perflevel0.l2cachesize\");\n  if (c.l1d == 0) c.l1d = sysctl_u64(\"hw.l1dcachesize\");\n  if (c.l2 == 0) c.l2 = sysctl_u64(\"hw.l2cachesize\");\n  c.llc = c.l2;\n  c.l2_min = sysctl_u64(\"hw.perflevel1.l2cachesize\");  // efficiency cluster, if any\n#elif defined(__linux__) && defined(_SC_LEVEL1_DCACHE_SIZE)\n  const long l1 = sysconf(_SC_LEVEL1_DCACHE_SIZE);\n  const long l2 = sysconf(_SC_LEVEL2_CACHE_SIZE);\n  const long l3 = sysconf(_SC_LEVEL3_CACHE_SIZE);\n  c.l1d = l1 > 0 ? static_cast<std::uint64_t>(l1) : 0;\n  c.l2 = l2 > 0 ? static_cast<std::uint64_t>(l2) : 0;\n  c.llc = l3 > 0 ? static_cast<std::uint64_t>(l3) : c.l2;\n#endif\n  if (c.l1d == 0) c.l1d = 32 << 10;  // unknown: a typical L1d\n  if (c.l2 == 0) c.l2 = 1 << 20;     // unknown: a typical per-core L2\n  if (c.llc == 0) c.llc = 8 << 20;   // unknown: a typical shared L3\n  if (c.llc < c.l2) c.llc = c.l2;\n  if (c.l2_min == 0 || c.l2_min > c.l2) c.l2_min = c.l2;\n  return c;\n}\n\n// Prefetch distance for this run: 0 (off) when W fits in half of L2, else\n// P1_PREFETCH_DIST or 8 non-zeros ahead.\nstd::uint32_t prefetch_distance(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  std::uint32_t dist = 8;\n  if (const char* s = std::getenv(\"P1_PREFETCH_DIST\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v >= 0) dist = static_cast<std::uint32_t>(v);\n  }\n  const std::uint64_t w_bytes = n_genes * k * sizeof(double);\n  return w_bytes > c.l2 / 2 ? dist : 0;\n}\n\n// Rung 7: how to cut W into gene blocks. No blocking while W fits in the\n// smallest L2; otherwise blocks of a quarter of that L2. P1_W_BLOCK_BYTES sets\n// the block size and blocks whenever W is larger. Blocks are made equal in\n// size, and there are never more than kMaxBlocks of them.\nBlocks plan_blocks(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  const std::uint64_t row_bytes = k * sizeof(double);\n  const std::uint64_t w_bytes = n_genes * row_bytes;\n  std::uint64_t budget = c.l2_min / 4, fits = c.l2_min;\n  if (const char* s = std::getenv(\"P1_W_BLOCK_BYTES\")) {\n    const long long v = std::strtoll(s, nullptr, 10);\n    if (v > 0) budget = fits = static_cast<std::uint64_t>(v);\n  }\n  if (n_genes == 0 || row_bytes == 0 || w_bytes <= fits)\n    return Blocks{static_cast<std::uint32_t>(n_genes), 1};\n  const std::uint64_t max_genes = std::max<std::uint64_t>(1, budget / row_bytes);\n  std::uint64_t count = std::min<std::uint64_t>((n_genes + max_genes - 1) / max_genes, kMaxBlocks);\n  const std::uint64_t genes = (n_genes + count - 1) / count;\n  count = (n_genes + genes - 1) / genes;\n  return Blocks{static_cast<std::uint32_t>(genes), static_cast<std::uint32_t>(count)};\n}\n\n// ---- threading: rung 3's, with one Scratch per thread and, new in rung 7, a\n// barrier between gene blocks ----------------------------------------------\n\nunsigned threads_granted() {\n#if defined(P1_OPENMP)\n  return static_cast<unsigned>(std::max(1, omp_get_max_threads()));\n#elif defined(P1_STD_THREAD)\n  if (const char* s = std::getenv(\"OMP_NUM_THREADS\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v > 0) return static_cast<unsigned>(v);\n  }\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0)\n    return static_cast<unsigned>(CPU_COUNT(&set));\n#endif\n  const unsigned hc = std::thread::hardware_concurrency();\n  return hc > 0 ? hc : 1;\n#else\n  return 1;\n#endif\n}\n\n#if defined(P1_STD_THREAD)\n// A reusable barrier (C++17 has none): each thread waits here until all have\n// finished the current gene block. The party count starts at the number of\n// threads planned and is lowered if creating one of them fails.\n//\n// A waiter first spins for up to 50 us: the other threads are usually only\n// microseconds behind, and a sleep plus wake-up through the kernel costs tens\n// of microseconds on macOS -- with 27 blocks at k = 256, that adds up. After\n// 50 us it sleeps, so an oversubscribed machine does not burn cores.\nclass Barrier {\n public:\n  explicit Barrier(unsigned parties) : parties_(parties) {}\n  void set_parties(unsigned parties) {\n    std::lock_guard<std::mutex> lock(m_);\n    parties_ = parties;\n    if (arrived_ > 0 && arrived_ >= parties_) release();\n  }\n  void wait() {\n    std::unique_lock<std::mutex> lock(m_);\n    const unsigned gen = generation_.load(std::memory_order_relaxed);\n    if (++arrived_ >= parties_) {\n      release();\n      return;\n    }\n    lock.unlock();\n    const auto until = std::chrono::steady_clock::now() + std::chrono::microseconds(50);\n    while (std::chrono::steady_clock::now() < until)\n      if (generation_.load(std::memory_order_acquire) != gen) return;\n    lock.lock();\n    cv_.wait(lock, [&] { return generation_.load(std::memory_order_relaxed) != gen; });\n  }\n\n private:\n  void release() {  // called with m_ held\n    arrived_ = 0;\n    generation_.fetch_add(1, std::memory_order_release);\n    cv_.notify_all();\n  }\n  std::mutex m_;\n  std::condition_variable cv_;\n  unsigned parties_, arrived_ = 0;\n  std::atomic<unsigned> generation_{0};\n};\n#endif\n\ntemplate <class Rows>\nvoid run_rows(const Rows& rows, const std::uint32_t* row_nnz, std::uint64_t n_cells,\n              std::uint64_t k, std::uint64_t scratch_cap, std::uint32_t n_blocks) {\n  const std::uint64_t nnz = row_nnz[n_cells];\n  const std::uint64_t floor_threads = std::max<std::uint64_t>(1, nnz * k / kMinWorkPerThread);\n  const unsigned nt =\n      static_cast<unsigned>(std::min<std::uint64_t>(threads_granted(), floor_threads));\n  if (nt <= 1) {\n    Scratch s(scratch_cap);\n    for (std::uint32_t b = 0; b < n_blocks; ++b) rows(0, n_cells, b, s);\n    return;\n  }\n\n  std::vector<std::uint32_t> bounds;\n  bounds.reserve(static_cast<std::size_t>(nt * kChunksPerThread + 2));\n  bounds.push_back(0);\n  const std::uint64_t target = std::max<std::uint64_t>(1, nnz / (nt * kChunksPerThread));\n  std::uint64_t chunk_start = row_nnz[0];\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    if (row_nnz[i + 1] - chunk_start >= target) {\n      bounds.push_back(static_cast<std::uint32_t>(i + 1));\n      chunk_start = row_nnz[i + 1];\n    }\n  if (bounds.back() != n_cells) bounds.push_back(static_cast<std::uint32_t>(n_cells));\n  const std::size_t n_chunks = bounds.size() - 1;\n\n#if defined(P1_OPENMP)\n  const long nc = static_cast<long>(n_chunks);\n#pragma omp parallel num_threads(nt)\n  {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      // The implicit barrier at the end of this loop keeps all threads on the\n      // same block of W: nobody starts block b + 1 before every row has done b.\n#pragma omp for schedule(dynamic, 1)\n      for (long c = 0; c < nc; ++c) rows(bounds[c], bounds[c + 1], b, s);\n    }\n  }\n#elif defined(P1_STD_THREAD)\n  // One chunk counter per block, so a block's chunks are handed out afresh.\n  std::unique_ptr<std::atomic<std::size_t>[]> next(new std::atomic<std::size_t>[n_blocks]);\n  for (std::uint32_t b = 0; b < n_blocks; ++b) next[b].store(0, std::memory_order_relaxed);\n  Barrier barrier(nt);\n  auto worker = [&]() {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      for (;;) {\n        const std::size_t c = next[b].fetch_add(1, std::memory_order_relaxed);\n        if (c >= n_chunks) break;\n        rows(bounds[c], bounds[c + 1], b, s);\n      }\n      if (b + 1 < n_blocks) barrier.wait();  // one block: never waits, as rung 6\n    }\n  };\n  std::vector<std::thread> helpers;\n  helpers.reserve(nt - 1);\n  for (unsigned t = 1; t < nt; ++t) {\n    try {\n      helpers.emplace_back(worker);\n    } catch (...) {\n      break;\n    }\n  }\n  if (helpers.size() + 1 < nt) barrier.set_parties(static_cast<unsigned>(helpers.size() + 1));\n  worker();\n  for (std::thread& h : helpers) h.join();\n#endif\n}\n\n}  // namespace\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  const Mapped m = w.map(\"csr_packed.bin\");\n  if (m.bytes() < 128) fatal(\"csr_packed.bin is truncated; re-run prepare\");\n  const std::uint64_t* hdr = m.as<std::uint64_t>(0);\n  const std::uint64_t n_cells = hdr[0], n_genes = hdr[1], format = hdr[3];\n  if (n_cells != p.n_cells || n_genes != p.n_genes)\n    fatal(\"csr_packed.bin dimensions do not match the harness; re-run prepare\");\n  if (m.bytes() != hdr[10]) fatal(\"csr_packed.bin has the wrong size; re-run prepare\");\n  const std::uint32_t* row_nnz = m.as<std::uint32_t>(hdr[5]);\n\n  const Caches caches = query_caches();\n  const Blocks blocks = plan_blocks(n_genes, p.k, caches);\n  // Prefetch when the working set -- now one block of W -- outgrows L2.\n  const std::uint32_t dist = prefetch_distance(blocks.genes, p.k, caches);\n  std::unique_ptr<Cursor[]> cursors(blocks.count > 1 ? new Cursor[n_cells] : nullptr);\n\n  // Rung 9: the decoded rows hold gene * k, unless that would overflow a u32.\n  const bool pre = n_genes * p.k <= 0xFFFFFFFFull;\n  const std::uint32_t scale = pre ? static_cast<std::uint32_t>(p.k) : 1u;\n\n  if (format == 3) {\n    const PackedRows rows{m.as<std::uint32_t>(hdr[6]), m.as<std::uint32_t>(hdr[7]),\n                          m.as<std::uint8_t>(hdr[8]),  m.as<std::uint8_t>(hdr[9]),\n                          p.W, y.data, p.k, dist, n_genes, blocks, cursors.get(), scale, pre};\n    run_rows(rows, row_nnz, n_cells, p.k, hdr[4], blocks.count);\n  } else if (format == 2) {\n    const WideRows rows{row_nnz, m.as<std::uint32_t>(hdr[6]), m.as<double>(hdr[7]),\n                        p.W, y.data, p.k, dist, n_genes, blocks, cursors.get()};\n    run_rows(rows, row_nnz, n_cells, p.k, 0, blocks.count);\n  } else {\n    fatal(\"csr_packed.bin has an unknown format; re-run prepare\");\n  }\n}",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    },
    {
      "id": "0075e469-aa84-432f-b6a2-fa0b368ebe2b",
      "name": "ahsan-10",
      "is_baseline": false,
      "version": 3,
      "owner_handle": "kowshid",
      "updated_at": "2026-09-23T14:37:13.648254+00:00",
      "reason": "no run on your account yet",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- RUNG 10: unchanged from rung 5. UNTIMED.\n//\n// Rungs 6, 7 and 9 change only solution.cpp. Rung 7 blocks W by gene without\n// any new offsets in this file: blocks are visited in ascending gene order, so\n// each row's decoder simply stops at a block boundary and resumes there.\n//\n// ---- rung 5 notes, unchanged ----\n//\n// RUNG 5: a data structure designed from the data.\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// The ONE change from rung 4: how each non-zero is encoded. Rung 2 spent 4\n// bytes on every non-zero (u16 gene + u16 count). The data says we can do with\n// about 1.3, by using what Lecture 3 (slides 28-34) points out about it:\n//\n//   * 69.8% of the counts are exactly 1.          -> say \"1\" with a single bit\n//   * genes are sorted within a row, gaps are small -> store the gap, not the\n//     (median 10, 99.8% under 128)                   gene (delta encoding)\n//   * counts other than 1 are small (max 419)      -> one byte each, rarely more\n//\n// Encoding, per row, in gene order:\n//\n//   code stream, one byte per non-zero:\n//     bit 7     1 if the count is 1, else 0 (count is in the value stream)\n//     bits 0-6  gap to the previous gene, 1..127 (the first gap is gene + 1)\n//     0 in bits 0-6 is an escape: the gap follows as two bytes (u16)\n//   value stream, one byte per non-zero whose count is not 1:\n//     2..255    the count itself\n//     0         escape: the count follows as two bytes (u16)\n//\n// Unlike the lecture's grouped-by-value layout, this keeps every row in gene\n// order, so the kernel adds the terms of each Y element in exactly the same\n// order as before: the compression costs no exactness.\n//\n// Each row starts at a recorded byte offset in both streams, so any row can be\n// decoded on its own -- which is what lets rung 3's threads keep splitting the\n// rows freely. A per-row count of non-zeros is kept for the thread balancing.\n//\n// csr_packed.bin layout, little-endian:\n//\n//     u64 header[16]:\n//       [0] n_cells  [1] n_genes  [2] nnz  [3] format  [4] max_row_nnz\n//       [5] off_row_nnz  [6] off_a  [7] off_b  [8] off_c  [9] off_d\n//       [10] total_bytes\n//     format 3 (packed): a = u32 code_ptr[n_cells + 1]  (byte offset of each row)\n//                        b = u32 val_ptr[n_cells + 1]   (byte offset of each row)\n//                        c = u8 code stream, d = u8 value stream (+64 pad bytes)\n//     format 2 (wide fallback, when a gene index or count does not fit):\n//                        a = u32 gene index[nnz], b = f64 value[nnz]\n//     u32 row_nnz[n_cells + 1] at off_row_nnz: prefix count of non-zeros\n//\n// Every section starts on a 64-byte boundary.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <vector>\n\n#if defined(_OPENMP)\n#include <omp.h>\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kFormatWide = 2;\nconstexpr std::uint64_t kFormatPacked = 3;\n\n// Diagnostic only (unchanged from rung 3): mirrors threads_granted() in solve.\nvoid report_threads() {\n  const char* env = std::getenv(\"OMP_NUM_THREADS\");\n#if defined(_OPENMP)\n  std::fprintf(stderr, \"prepare: solve will be granted %d threads (OpenMP; OMP_NUM_THREADS=%s)\\n\",\n               omp_get_max_threads(), env ? env : \"unset\");\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n  unsigned n = std::thread::hardware_concurrency();\n  const char* source = \"hardware threads\";\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0) {\n    n = static_cast<unsigned>(CPU_COUNT(&set));\n    source = \"CPUs in the affinity mask\";\n  }\n#endif\n  if (env && std::strtol(env, nullptr, 10) > 0) {\n    n = static_cast<unsigned>(std::strtol(env, nullptr, 10));\n    source = \"OMP_NUM_THREADS\";\n  }\n  std::fprintf(stderr, \"prepare: solve will be granted %u threads (std::thread; from %s)\\n\", n,\n               source);\n#else\n  std::fprintf(stderr, \"prepare: solve will run serially (browser build, no threads)\\n\");\n#endif\n}\n\nstd::uint64_t pad64(std::uint64_t x) { return (x + 63) & ~std::uint64_t(63); }\n\nvoid pad_to(hpcbench::Writer& w, std::uint64_t& pos, std::uint64_t target) {\n  static const unsigned char zeros[64] = {};\n  while (pos < target) {\n    const std::uint64_t n = std::min<std::uint64_t>(64, target - pos);\n    w.write(zeros, static_cast<std::size_t>(n));\n    pos += n;\n  }\n}\n\nvoid put(hpcbench::Writer& w, std::uint64_t& pos, const void* data, std::uint64_t bytes) {\n  w.write(data, static_cast<std::size_t>(bytes));\n  pos += bytes;\n}\n\n}  // namespace\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  const std::uint64_t n_cells = in.n_cells;\n  const std::uint64_t n_genes = in.n_genes;\n\n  // Pass 1: can the packed encoding represent this input exactly?\n  // Gaps must fit u16 (gene index <= 65,534) and counts must be integers\n  // 1..65,535. Otherwise fall back to u32 index + f64 value.\n  bool packable = n_genes <= 65535;\n  std::uint64_t nnz = 0, max_row = 0;\n  std::vector<std::uint32_t> row_nnz(n_cells + 1, 0);\n  for (std::uint64_t i = 0; i < n_cells; ++i) {\n    const double* xi = in.X + i * n_genes;\n    std::uint64_t row = 0;\n    for (std::uint64_t g = 0; g < n_genes; ++g) {\n      const double x = xi[g];\n      if (x == 0.0) continue;\n      ++row;\n      if (!(x >= 1.0 && x <= 65535.0 && x == std::floor(x))) packable = false;\n    }\n    nnz += row;\n    if (nnz > UINT32_MAX) fatal(\"more than 2^32 non-zeros: u32 offsets overflow\");\n    max_row = std::max(max_row, row);\n    row_nnz[i + 1] = static_cast<std::uint32_t>(nnz);\n  }\n\n  std::uint64_t header[16] = {};\n  header[0] = n_cells;\n  header[1] = n_genes;\n  header[2] = nnz;\n  header[4] = max_row;\n  header[5] = 128;  // off_row_nnz\n  const std::uint64_t after_row_nnz = pad64(header[5] + 4 * (n_cells + 1));\n\n  Writer w = out.create(\"csr_packed.bin\");\n  std::uint64_t pos = 0;\n\n  if (packable) {\n    // Pass 2: encode.\n    std::vector<std::uint8_t> code, vals;\n    std::vector<std::uint32_t> code_ptr(n_cells + 1, 0), val_ptr(n_cells + 1, 0);\n    code.reserve(static_cast<std::size_t>(nnz + nnz / 64));\n    vals.reserve(static_cast<std::size_t>(nnz / 2));\n    std::uint64_t gap_escapes = 0, val_escapes = 0;\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      std::uint64_t prev = ~std::uint64_t(0);  // \"gene -1\": the first gap is gene + 1\n      for (std::uint64_t g = 0; g < n_genes; ++g) {\n        const double x = xi[g];\n        if (x == 0.0) continue;\n        const std::uint64_t gap = g - prev;  // 1..65,535\n        prev = g;\n        const std::uint32_t v = static_cast<std::uint32_t>(x);\n        const std::uint8_t one = (v == 1) ? 0x80 : 0x00;\n        if (gap < 128) {\n          code.push_back(static_cast<std::uint8_t>(one | gap));\n        } else {\n          code.push_back(one);  // gap field 0 = escape\n          code.push_back(static_cast<std::uint8_t>(gap & 0xFF));\n          code.push_back(static_cast<std::uint8_t>(gap >> 8));\n          ++gap_escapes;\n        }\n        if (v != 1) {\n          if (v < 256) {\n            vals.push_back(static_cast<std::uint8_t>(v));\n          } else {\n            vals.push_back(0);  // escape\n            vals.push_back(static_cast<std::uint8_t>(v & 0xFF));\n            vals.push_back(static_cast<std::uint8_t>(v >> 8));\n            ++val_escapes;\n          }\n        }\n      }\n      if (code.size() > UINT32_MAX || vals.size() > UINT32_MAX)\n        fatal(\"packed streams exceed 4 GB\");\n      code_ptr[i + 1] = static_cast<std::uint32_t>(code.size());\n      val_ptr[i + 1] = static_cast<std::uint32_t>(vals.size());\n    }\n\n    header[3] = kFormatPacked;\n    header[6] = after_row_nnz;                               // code_ptr\n    header[7] = pad64(header[6] + 4 * (n_cells + 1));        // val_ptr\n    header[8] = pad64(header[7] + 4 * (n_cells + 1));        // code stream\n    header[9] = pad64(header[8] + code.size());              // value stream\n    header[10] = header[9] + vals.size() + 64;               // + pad: the decoder\n                                                             // may peek one byte ahead\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, code_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[7]);\n    put(w, pos, val_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[8]);\n    put(w, pos, code.data(), code.size());\n    pad_to(w, pos, header[9]);\n    put(w, pos, vals.data(), vals.size());\n    pad_to(w, pos, header[10]);\n    w.close();\n\n    std::fprintf(stderr,\n                 \"prepare: packed CSR, %llu nonzeros, %.2f MB (%.2f bytes/nnz); \"\n                 \"code %.2f MB, values %.2f MB, %llu gap and %llu value escapes\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6,\n                 double(header[10]) / double(nnz), double(code.size()) / 1e6,\n                 double(vals.size()) / 1e6, (unsigned long long)gap_escapes,\n                 (unsigned long long)val_escapes);\n  } else {\n    std::vector<std::uint32_t> col;\n    std::vector<double> val;\n    col.reserve(static_cast<std::size_t>(nnz));\n    val.reserve(static_cast<std::size_t>(nnz));\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      for (std::uint64_t g = 0; g < n_genes; ++g)\n        if (xi[g] != 0.0) {\n          col.push_back(static_cast<std::uint32_t>(g));\n          val.push_back(xi[g]);\n        }\n    }\n    header[3] = kFormatWide;\n    header[6] = after_row_nnz;                      // u32 gene index\n    header[7] = pad64(header[6] + 4 * nnz);         // f64 value\n    header[10] = header[7] + 8 * nnz;\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, col.data(), 4 * nnz);\n    pad_to(w, pos, header[7]);\n    put(w, pos, val.data(), 8 * nnz);\n    w.close();\n    std::fprintf(stderr, \"prepare: wide fallback CSR (u32 + f64), %llu nonzeros, %.2f MB\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6);\n  }\n  report_threads();\n}",
        "src/solution.cpp": "// src/solution.cpp -- RUNG 10: lay W out the way the kernel walks it.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Stacked on rung 9a. The ONE change: W is copied once, at the start of solve,\n// into tiles of (gene block x column panel), and the kernel reads the tiles.\n//\n// The harness hands over W as n_genes x k, row-major. A kernel pass over one\n// panel of one gene block touches 32 columns of each of the block's genes --\n// 256 contiguous bytes out of every k * 8 -- so the pass walks the whole block\n// (1 MB at k = 256) to use an eighth of it. Repacked, that same pass reads one\n// contiguous tile of block_genes * 32 doubles (about 130 KB), which is what\n// rung 12 will keep hot.\n//\n// The tile of block b, panel p starts at b * G * k + G * p * 32 (G = genes per\n// block), and gene g's row inside it is at (g - b * G) * panel_width. The\n// decoded rows therefore hold the gene's index WITHIN its block, and the kernel\n// multiplies by the panel width, which is a compile-time constant -- a shift,\n// where rung 9a still had a 64-bit multiply by k at decode time.\n//\n// The copy costs one pass over W on the clock (28 MB read and written at\n// k = 256, spread over the threads, with a barrier before the multiply starts).\n// At small k that pass is pure overhead; it is meant to pay at large k, and the\n// knob below turns it off for the A/B.\n//\n// Correctness: a copy of doubles, read back in the same order. Same terms, same\n// ascending gene order, same fused multiply-adds. Bit-for-bit the baseline's.\n//\n// Knobs: P1_REPACK=0 keeps rung 9a's layout; P1_W_BLOCK_BYTES and\n// P1_PREFETCH_DIST as before.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cstdint>\n#include <cstdlib>\n#include <cstring>\n#include <memory>\n#include <vector>\n\n#if defined(__APPLE__)\n#include <sys/sysctl.h>\n#elif defined(__linux__)\n#include <unistd.h>\n#endif\n\n#if defined(_OPENMP)\n#include <omp.h>\n#define P1_OPENMP 1\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#define P1_STD_THREAD 1\n#include <atomic>\n#include <chrono>\n#include <condition_variable>\n#include <mutex>\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\n#ifndef P1_PANEL  // override with -DP1_PANEL=... (up to 64) in CXXFLAGS to experiment\n#define P1_PANEL 32\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kMinWorkPerThread = std::uint64_t(1) << 19;  // multiply-adds\nconstexpr std::uint64_t kChunksPerThread = 32;\nconstexpr std::uint64_t kMaxBlocks = 64;  // each block costs a barrier\n\n// ---- rung 4's kernel, unchanged -------------------------------------------\n\ntypedef double v4d __attribute__((vector_size(32)));  // four doubles\n\nconstexpr int kPanel = P1_PANEL;\n\n// acc + x * w, rounded exactly once, the way the baseline's `y += x * w` is.\n//\n// Where the target has a hardware fused multiply-add (__FP_FAST_FMA: every\n// arm64, x86 with FMA), the compiler fuses the baseline's statement into one\n// instruction. It does not reliably fuse ours: GCC's tuning for recent Intel\n// cores (\"avoid FMA chains\") splits a loop-carried accumulation into a multiply\n// and an add when only one or two accumulator vectors carry it -- exactly our\n// narrow panels -- which rounds twice. So the fusion is spelled out here.\n// Without hardware FMA the baseline rounds twice, and so does this.\ninline double madd(double x, double w, double acc) {\n#if defined(__FP_FAST_FMA)\n  return __builtin_fma(x, w, acc);\n#else\n  return x * w + acc;\n#endif\n}\n\ninline v4d vmadd(v4d x, v4d w, v4d acc) {\n#if defined(__FP_FAST_FMA)\n  // Four scalar fused multiply-adds; every compiler tested turns them into one\n  // vector FMA (vfmadd231pd on x86, two fmla on arm64).\n  return v4d{__builtin_fma(x[0], w[0], acc[0]), __builtin_fma(x[1], w[1], acc[1]),\n             __builtin_fma(x[2], w[2], acc[2]), __builtin_fma(x[3], w[3], acc[3])};\n#else\n  return x * w + acc;\n#endif\n}\n\n// One row, one panel of P output columns:  yc[0:P] = sum over the row's\n// non-zeros of x * Wc[g*k + 0:P], accumulated in registers. With `accumulate`\n// the sums start from what yc already holds -- the partial sums left there by\n// the previous gene blocks -- instead of from zero.\n//\n// noinline: each width is compiled as its own function. Measured necessity,\n// not style: GCC 13 inlined all 32 prefetching widths into the switch below and\n// the result ran k = 16 at 18.1 ms instead of 10.1 ms (k = 13: 15.1 vs 8.5) at\n// every prefetch distance from 1 to 64 -- a code-generation effect, not a\n// memory one. The same attribute on rungs 4 and 5 measured no difference.\n// How col[] addresses W:\n//   TILED  gene index within its block; the row is at col[q] * P in the tile\n//   PRE    gene * k already (rung 9a), so the kernel only adds\n//   else   plain gene index, multiplied by k here (rung 7, and the CSR paths)\ntemplate <int P, bool PF, bool TILED, bool PRE, class Idx, class Val>\n__attribute__((noinline)) void row_panel(const Idx* col, const Val* val, std::uint32_t n,\n                                         const double* Wc, std::uint64_t k, double* yc,\n                                         std::uint32_t dist, bool accumulate) {\n  constexpr int V = P / 4;  // full 4-wide vectors\n  constexpr int R = P % 4;  // 1-3 leftover columns, scalar\n  v4d acc[V > 0 ? V : 1];\n  double tail[R > 0 ? R : 1];\n  if (accumulate) {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) std::memcpy(&acc[v], yc + 4 * v, sizeof(v4d));\n    for (int r = 0; r < R; ++r) tail[r] = yc[4 * V + r];\n  } else {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) acc[v] = v4d{0.0, 0.0, 0.0, 0.0};\n    for (int r = 0; r < R; ++r) tail[r] = 0.0;\n  }\n\n  for (std::uint32_t q = 0; q < n; ++q) {\n    if (PF && q + dist < n) {\n      // The W segment this row will need `dist` non-zeros from now: one\n      // prefetch per 64 bytes (a cache line on x86, half of one on Apple).\n      const std::uint64_t ahead_off =\n          TILED ? static_cast<std::uint64_t>(col[q + dist]) * P\n                : (PRE ? static_cast<std::uint64_t>(col[q + dist])\n                       : static_cast<std::uint64_t>(col[q + dist]) * k);\n      const char* ahead = reinterpret_cast<const char*>(Wc + ahead_off);\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n    const double x = static_cast<double>(val[q]);\n    const v4d xv = v4d{x, x, x, x};\n    const double* w = Wc + (TILED ? static_cast<std::uint64_t>(col[q]) * P\n                                  : (PRE ? static_cast<std::uint64_t>(col[q])\n                                         : static_cast<std::uint64_t>(col[q]) * k));\n    // Fully unrolled: GCC stops at 8 vectors on its own and would otherwise\n    // keep a 64-wide panel's accumulators on the stack.\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) {\n      v4d wv;\n      std::memcpy(&wv, w + 4 * v, sizeof(wv));  // unaligned vector load\n      acc[v] = vmadd(xv, wv, acc[v]);\n    }\n    for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n  }\n\n#pragma GCC unroll 16\n  for (int v = 0; v < V; ++v) std::memcpy(yc + 4 * v, &acc[v], sizeof(v4d));\n  for (int r = 0; r < R; ++r) yc[4 * V + r] = tail[r];\n}\n\n// The switch: a run-time width selects a compile-time kernel.\ntemplate <bool PF, bool TILED, bool PRE, class Idx, class Val>\nvoid row_panel_any(int width, const Idx* col, const Val* val, std::uint32_t n,\n                   const double* Wc, std::uint64_t k, double* yc, std::uint32_t dist,\n                   bool accumulate) {\n  switch (width) {\n#define P1_CASE(P)                                                  \\\n  case P:                                                           \\\n    row_panel<P, PF, TILED, PRE>(col, val, n, Wc, k, yc, dist, accumulate);     \\\n    return;\n    P1_CASE(1)\n    P1_CASE(2)\n    P1_CASE(3)\n    P1_CASE(4)\n    P1_CASE(5)\n    P1_CASE(6)\n    P1_CASE(7)\n    P1_CASE(8)\n    P1_CASE(9)\n    P1_CASE(10)\n    P1_CASE(11)\n    P1_CASE(12)\n    P1_CASE(13)\n    P1_CASE(14)\n    P1_CASE(15)\n    P1_CASE(16)\n    P1_CASE(17)\n    P1_CASE(18)\n    P1_CASE(19)\n    P1_CASE(20)\n    P1_CASE(21)\n    P1_CASE(22)\n    P1_CASE(23)\n    P1_CASE(24)\n    P1_CASE(25)\n    P1_CASE(26)\n    P1_CASE(27)\n    P1_CASE(28)\n    P1_CASE(29)\n    P1_CASE(30)\n    P1_CASE(31)\n    P1_CASE(32)\n#if P1_PANEL > 32\n    P1_CASE(33)\n    P1_CASE(34)\n    P1_CASE(35)\n    P1_CASE(36)\n    P1_CASE(37)\n    P1_CASE(38)\n    P1_CASE(39)\n    P1_CASE(40)\n    P1_CASE(41)\n    P1_CASE(42)\n    P1_CASE(43)\n    P1_CASE(44)\n    P1_CASE(45)\n    P1_CASE(46)\n    P1_CASE(47)\n    P1_CASE(48)\n    P1_CASE(49)\n    P1_CASE(50)\n    P1_CASE(51)\n    P1_CASE(52)\n    P1_CASE(53)\n    P1_CASE(54)\n    P1_CASE(55)\n    P1_CASE(56)\n    P1_CASE(57)\n    P1_CASE(58)\n    P1_CASE(59)\n    P1_CASE(60)\n    P1_CASE(61)\n    P1_CASE(62)\n    P1_CASE(63)\n    P1_CASE(64)\n#endif\n#undef P1_CASE\n    default:\n      hpcbench::fatal(\"panel width out of range\");\n  }\n}\n\n// ---- rung 5: the packed representation -------------------------------------\n\n// One row's worth of decoded non-zeros, owned by one thread.\nstruct Scratch {\n  std::unique_ptr<std::uint32_t[]> genes;  // rung 9: gene * k when it fits in u32\n  std::unique_ptr<double[]> counts;\n  explicit Scratch(std::uint64_t cap)\n      : genes(new std::uint32_t[cap > 0 ? cap : 1]), counts(new double[cap > 0 ? cap : 1]) {}\n};\n\n// ---- rung 7: gene blocks --------------------------------------------------\n\n// The genes [b * genes, min((b + 1) * genes, n_genes)) form block b of W.\nstruct Blocks {\n  std::uint32_t genes;  // genes per block (the last one may be smaller)\n  std::uint32_t count;  // number of blocks; 1 = W fits, no blocking\n};\n\n// Where row i's decoding stopped at the end of the previous block.\nstruct Cursor {\n  std::uint32_t code;  // next code byte (packed) / next non-zero (wide)\n  std::uint32_t val;   // next value byte (packed only)\n  std::uint32_t gene;  // last gene decoded (packed only; 0xFFFFFFFF = none yet)\n};\n\nstruct PackedRows {\n  const std::uint32_t* code_ptr;  // byte offset of each row in the code stream\n  const std::uint32_t* val_ptr;   // byte offset of each row in the value stream\n  const std::uint8_t* code;\n  const std::uint8_t* vals;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;      // prefetch distance in non-zeros; 0 = off\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;         // one per row; used only when blocks.count > 1\n  std::uint32_t scale;     // rung 9: k when gene * k fits in u32, else 1\n  bool pre;                // rung 9: true when the scratch holds gene * k\n  double* tiles;           // rung 10: W in (gene block x panel) tiles, or null\n  bool tiled;              // rung 10: true when the kernel reads the tiles\n\n  // Rung 10: copy this slice of W's genes into the tiles. Threads split the\n  // genes between them and meet at a barrier before any multiplying starts.\n  void repack(std::uint64_t g0, std::uint64_t g1) const {\n    const std::uint64_t G = blocks.genes;\n    for (std::uint64_t g = g0; g < g1; ++g) {\n      const std::uint64_t b = g / G, lg = g - b * G;\n      const double* src = W + g * k;\n      double* dst = tiles + b * G * k;\n      for (std::uint64_t c0 = 0; c0 < k; c0 += kPanel) {\n        const std::uint64_t width = std::min<std::uint64_t>(kPanel, k - c0);\n        std::memcpy(dst + G * c0 + lg * width, src + c0, width * sizeof(double));\n      }\n    }\n  }\n\n  // Decode row i into s.genes / s.counts; returns its number of non-zeros.\n  template <bool TILED>\n  std::uint32_t decode(std::uint64_t i, std::uint32_t origin, Scratch& s) const {\n    const std::uint8_t* cp = code + code_ptr[i];\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + val_ptr[i];\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = 0xFFFFFFFFu;  // \"gene -1\": the first gap is gene + 1\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = *cp++;\n      std::uint32_t gap = b & 0x7Fu;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[0]) | (std::uint32_t(cp[1]) << 8);\n        cp += 2;\n      }\n      g += gap;\n      // Count: 1 if bit 7 is set, else the next byte of the value stream.\n      // Written as arithmetic on purpose: GCC compiles `one ? 1 : peek` into a\n      // branch, and a 70/30 coin flip per non-zero is a branch no predictor can\n      // learn (measured: ~9 cycles per non-zero instead of ~3).\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;   // 1 if the count is in the value stream\n      const std::uint32_t peek = *vp;                 // always in bounds: stream is padded\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);  // peek, or 1\n      vp += uses_val;                                 // consume a value byte only if used\n      if (v == 0) {                                   // escape: two-byte count\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    return n;\n  }\n\n  // Rung 7: decode only the non-zeros of row i whose gene is below gene_end,\n  // starting where cur says the previous block stopped; leave cur at the\n  // first non-zero of a later block. Same decoder as above, plus a peek at the\n  // next gene before consuming it.\n  template <bool TILED>\n  std::uint32_t decode_until(std::uint64_t i, std::uint32_t gene_end, std::uint32_t origin,\n                             Cursor& cur, Scratch& s) const {\n    const std::uint8_t* cp = code + cur.code;\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + cur.val;\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = cur.gene;\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = cp[0];\n      std::uint32_t gap = b & 0x7Fu, len = 1;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[1]) | (std::uint32_t(cp[2]) << 8);\n        len = 3;\n      }\n      if (g + gap >= gene_end) break;  // belongs to a later block: leave it for then\n      cp += len;\n      g += gap;\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;\n      const std::uint32_t peek = *vp;\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);\n      vp += uses_val;\n      if (v == 0) {\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    cur = Cursor{static_cast<std::uint32_t>(cp - code), static_cast<std::uint32_t>(vp - vals), g};\n    return n;\n  }\n\n  // Rows [r0, r1), gene block b.\n  template <bool PF, bool TILED, bool PRE>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint32_t gene_end = static_cast<std::uint32_t>(\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes));\n    const bool accumulate = b > 0;  // later blocks add to the partial sums in Y\n    const std::uint32_t origin = static_cast<std::uint32_t>(std::uint64_t(b) * blocks.genes);\n    // Rung 10: the block's tiles start here; panel c0 of it is G * c0 further on.\n    const double* const wb = TILED ? tiles + std::uint64_t(b) * blocks.genes * k : W;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t n;\n      if (blocks.count == 1) {\n        n = decode<TILED>(i, origin, s);  // W fits: the whole row at once\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur = Cursor{code_ptr[i], val_ptr[i], 0xFFFFFFFFu};\n        n = decode_until<TILED>(i, gene_end, origin, cur, s);\n        // Nothing of this row in block b: its partial sums in Y are already\n        // right. (Block 0 still writes every row, so Y never relies on the\n        // harness having zero-filled it.)\n        if (n == 0 && accumulate) continue;\n      }\n      const std::uint32_t* genes = s.genes.get();\n      const double* counts = s.counts.get();\n      double* yi = Y + i * k;\n      const std::uint64_t stride = TILED ? blocks.genes : 1;  // panel c0 starts G * c0 in\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, TILED, PRE>(genes, counts, n, wb + stride * c0, k, yi + c0, dist,\n                                          accumulate);\n      if (rem)\n        row_panel_any<PF, TILED, PRE>(rem, genes, counts, n, wb + stride * full, k, yi + full,\n                                      dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    if (dist > 0) {\n      if (tiled) rows<true, true, false>(r0, r1, b, s);\n      else if (pre) rows<true, false, true>(r0, r1, b, s);\n      else rows<true, false, false>(r0, r1, b, s);\n    } else {\n      if (tiled) rows<false, true, false>(r0, r1, b, s);\n      else if (pre) rows<false, false, true>(r0, r1, b, s);\n      else rows<false, false, false>(r0, r1, b, s);\n    }\n  }\n};\n\n// The wide fallback prepare writes when the packed encoding cannot represent\n// the input exactly: plain u32 / f64 CSR, handled exactly as rung 4 did.\nstruct WideRows {\n  const std::uint32_t* row_nnz;\n  const std::uint32_t* col;\n  const double* val;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;\n\n  void repack(std::uint64_t, std::uint64_t) const {}  // rung 10: plain CSR keeps W as given\n\n  template <bool PF>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint64_t gene_end =\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes);\n    const bool accumulate = b > 0;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t q0, n;\n      if (blocks.count == 1) {\n        q0 = row_nnz[i];\n        n = row_nnz[i + 1] - q0;\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur.code = row_nnz[i];\n        q0 = cur.code;\n        std::uint32_t q = q0;\n        while (q < row_nnz[i + 1] && col[q] < gene_end) ++q;\n        n = q - q0;\n        cur.code = q;\n        if (n == 0 && accumulate) continue;\n      }\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, false, false>(col + q0, val + q0, n, W + c0, k, yi + c0, dist,\n                                            accumulate);\n      if (rem)\n        row_panel_any<PF, false, false>(rem, col + q0, val + q0, n, W + full, k, yi + full, dist,\n                                        accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch&) const {\n    if (dist > 0) rows<true>(r0, r1, b);\n    else rows<false>(r0, r1, b);\n  }\n};\n\n// ---- rung 6: cache sizes and the prefetch policy ---------------------------\n\nstruct Caches {\n  std::uint64_t l1d, l2, llc;  // bytes; llc = the largest cache shared by a cluster/socket\n  std::uint64_t l2_min;        // rung 7: the smallest L2 any thread may run on\n};\n\n#if defined(__APPLE__)\nstd::uint64_t sysctl_u64(const char* name) {\n  std::uint64_t v = 0;\n  std::size_t len = sizeof(v);\n  if (sysctlbyname(name, &v, &len, nullptr, 0) != 0) return 0;\n  return len == 4 ? static_cast<std::uint32_t>(v) : v;\n}\n#endif\n\n// This machine's cache sizes. On Apple silicon the performance cluster's\n// values (perflevel0: 128 KB L1d, 16 MB L2 on an M4) -- the L2 is the last\n// level each cluster owns. On Linux, glibc's sysconf. Defaults where unknown.\nCaches query_caches() {\n  Caches c{0, 0, 0, 0};\n#if defined(__APPLE__)\n  c.l1d = sysctl_u64(\"hw.perflevel0.l1dcachesize\");\n  c.l2 = sysctl_u64(\"hw.perflevel0.l2cachesize\");\n  if (c.l1d == 0) c.l1d = sysctl_u64(\"hw.l1dcachesize\");\n  if (c.l2 == 0) c.l2 = sysctl_u64(\"hw.l2cachesize\");\n  c.llc = c.l2;\n  c.l2_min = sysctl_u64(\"hw.perflevel1.l2cachesize\");  // efficiency cluster, if any\n#elif defined(__linux__) && defined(_SC_LEVEL1_DCACHE_SIZE)\n  const long l1 = sysconf(_SC_LEVEL1_DCACHE_SIZE);\n  const long l2 = sysconf(_SC_LEVEL2_CACHE_SIZE);\n  const long l3 = sysconf(_SC_LEVEL3_CACHE_SIZE);\n  c.l1d = l1 > 0 ? static_cast<std::uint64_t>(l1) : 0;\n  c.l2 = l2 > 0 ? static_cast<std::uint64_t>(l2) : 0;\n  c.llc = l3 > 0 ? static_cast<std::uint64_t>(l3) : c.l2;\n#endif\n  if (c.l1d == 0) c.l1d = 32 << 10;  // unknown: a typical L1d\n  if (c.l2 == 0) c.l2 = 1 << 20;     // unknown: a typical per-core L2\n  if (c.llc == 0) c.llc = 8 << 20;   // unknown: a typical shared L3\n  if (c.llc < c.l2) c.llc = c.l2;\n  if (c.l2_min == 0 || c.l2_min > c.l2) c.l2_min = c.l2;\n  return c;\n}\n\n// Prefetch distance for this run: 0 (off) when W fits in half of L2, else\n// P1_PREFETCH_DIST or 8 non-zeros ahead.\nstd::uint32_t prefetch_distance(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  std::uint32_t dist = 8;\n  if (const char* s = std::getenv(\"P1_PREFETCH_DIST\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v >= 0) dist = static_cast<std::uint32_t>(v);\n  }\n  const std::uint64_t w_bytes = n_genes * k * sizeof(double);\n  return w_bytes > c.l2 / 2 ? dist : 0;\n}\n\n// Rung 7: how to cut W into gene blocks. No blocking while W fits in the\n// smallest L2; otherwise blocks of a quarter of that L2. P1_W_BLOCK_BYTES sets\n// the block size and blocks whenever W is larger. Blocks are made equal in\n// size, and there are never more than kMaxBlocks of them.\nBlocks plan_blocks(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  const std::uint64_t row_bytes = k * sizeof(double);\n  const std::uint64_t w_bytes = n_genes * row_bytes;\n  std::uint64_t budget = c.l2_min / 4, fits = c.l2_min;\n  if (const char* s = std::getenv(\"P1_W_BLOCK_BYTES\")) {\n    const long long v = std::strtoll(s, nullptr, 10);\n    if (v > 0) budget = fits = static_cast<std::uint64_t>(v);\n  }\n  if (n_genes == 0 || row_bytes == 0 || w_bytes <= fits)\n    return Blocks{static_cast<std::uint32_t>(n_genes), 1};\n  const std::uint64_t max_genes = std::max<std::uint64_t>(1, budget / row_bytes);\n  std::uint64_t count = std::min<std::uint64_t>((n_genes + max_genes - 1) / max_genes, kMaxBlocks);\n  const std::uint64_t genes = (n_genes + count - 1) / count;\n  count = (n_genes + genes - 1) / genes;\n  return Blocks{static_cast<std::uint32_t>(genes), static_cast<std::uint32_t>(count)};\n}\n\n// ---- threading: rung 3's, with one Scratch per thread and, new in rung 7, a\n// barrier between gene blocks ----------------------------------------------\n\nunsigned threads_granted() {\n#if defined(P1_OPENMP)\n  return static_cast<unsigned>(std::max(1, omp_get_max_threads()));\n#elif defined(P1_STD_THREAD)\n  if (const char* s = std::getenv(\"OMP_NUM_THREADS\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v > 0) return static_cast<unsigned>(v);\n  }\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0)\n    return static_cast<unsigned>(CPU_COUNT(&set));\n#endif\n  const unsigned hc = std::thread::hardware_concurrency();\n  return hc > 0 ? hc : 1;\n#else\n  return 1;\n#endif\n}\n\n#if defined(P1_STD_THREAD)\n// A reusable barrier (C++17 has none): each thread waits here until all have\n// finished the current gene block. The party count starts at the number of\n// threads planned and is lowered if creating one of them fails.\n//\n// A waiter first spins for up to 50 us: the other threads are usually only\n// microseconds behind, and a sleep plus wake-up through the kernel costs tens\n// of microseconds on macOS -- with 27 blocks at k = 256, that adds up. After\n// 50 us it sleeps, so an oversubscribed machine does not burn cores.\nclass Barrier {\n public:\n  explicit Barrier(unsigned parties) : parties_(parties) {}\n  void set_parties(unsigned parties) {\n    std::lock_guard<std::mutex> lock(m_);\n    parties_ = parties;\n    if (arrived_ > 0 && arrived_ >= parties_) release();\n  }\n  void wait() {\n    std::unique_lock<std::mutex> lock(m_);\n    const unsigned gen = generation_.load(std::memory_order_relaxed);\n    if (++arrived_ >= parties_) {\n      release();\n      return;\n    }\n    lock.unlock();\n    const auto until = std::chrono::steady_clock::now() + std::chrono::microseconds(50);\n    while (std::chrono::steady_clock::now() < until)\n      if (generation_.load(std::memory_order_acquire) != gen) return;\n    lock.lock();\n    cv_.wait(lock, [&] { return generation_.load(std::memory_order_relaxed) != gen; });\n  }\n\n private:\n  void release() {  // called with m_ held\n    arrived_ = 0;\n    generation_.fetch_add(1, std::memory_order_release);\n    cv_.notify_all();\n  }\n  std::mutex m_;\n  std::condition_variable cv_;\n  unsigned parties_, arrived_ = 0;\n  std::atomic<unsigned> generation_{0};\n};\n#endif\n\ntemplate <class Rows>\nvoid run_rows(const Rows& rows, const std::uint32_t* row_nnz, std::uint64_t n_cells,\n              std::uint64_t k, std::uint64_t scratch_cap, std::uint32_t n_blocks,\n              bool do_repack, std::uint64_t n_genes) {\n  const std::uint64_t nnz = row_nnz[n_cells];\n  const std::uint64_t floor_threads = std::max<std::uint64_t>(1, nnz * k / kMinWorkPerThread);\n  const unsigned nt =\n      static_cast<unsigned>(std::min<std::uint64_t>(threads_granted(), floor_threads));\n  if (nt <= 1) {\n    Scratch s(scratch_cap);\n    if (do_repack) rows.repack(0, n_genes);  // rung 10\n    for (std::uint32_t b = 0; b < n_blocks; ++b) rows(0, n_cells, b, s);\n    return;\n  }\n\n  std::vector<std::uint32_t> bounds;\n  bounds.reserve(static_cast<std::size_t>(nt * kChunksPerThread + 2));\n  bounds.push_back(0);\n  const std::uint64_t target = std::max<std::uint64_t>(1, nnz / (nt * kChunksPerThread));\n  std::uint64_t chunk_start = row_nnz[0];\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    if (row_nnz[i + 1] - chunk_start >= target) {\n      bounds.push_back(static_cast<std::uint32_t>(i + 1));\n      chunk_start = row_nnz[i + 1];\n    }\n  if (bounds.back() != n_cells) bounds.push_back(static_cast<std::uint32_t>(n_cells));\n  const std::size_t n_chunks = bounds.size() - 1;\n\n  // Rung 10: the threads build W's tiles first and meet at a barrier; nothing\n  // reads a tile before every thread has finished writing its genes.\n  const std::size_t n_gchunks = static_cast<std::size_t>(nt) * 4;\n  const auto gene_lo = [&](std::size_t j) { return n_genes * j / n_gchunks; };\n\n#if defined(P1_OPENMP)\n  const long nc = static_cast<long>(n_chunks);\n#pragma omp parallel num_threads(nt)\n  {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n#pragma omp for schedule(static)\n      for (long gg = 0; gg < static_cast<long>(n_gchunks); ++gg)\n        rows.repack(gene_lo(static_cast<std::size_t>(gg)),\n                    gene_lo(static_cast<std::size_t>(gg) + 1));\n    }  // implicit barrier\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      // The implicit barrier at the end of this loop keeps all threads on the\n      // same block of W: nobody starts block b + 1 before every row has done b.\n#pragma omp for schedule(dynamic, 1)\n      for (long c = 0; c < nc; ++c) rows(bounds[c], bounds[c + 1], b, s);\n    }\n  }\n#elif defined(P1_STD_THREAD)\n  // One chunk counter per block, so a block's chunks are handed out afresh.\n  std::unique_ptr<std::atomic<std::size_t>[]> next(new std::atomic<std::size_t>[n_blocks]);\n  for (std::uint32_t b = 0; b < n_blocks; ++b) next[b].store(0, std::memory_order_relaxed);\n  Barrier barrier(nt);\n  std::atomic<std::size_t> next_gene{0};\n  auto worker = [&]() {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n      for (;;) {\n        const std::size_t gc = next_gene.fetch_add(1, std::memory_order_relaxed);\n        if (gc >= n_gchunks) break;\n        rows.repack(gene_lo(gc), gene_lo(gc + 1));\n      }\n      barrier.wait();\n    }\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      for (;;) {\n        const std::size_t c = next[b].fetch_add(1, std::memory_order_relaxed);\n        if (c >= n_chunks) break;\n        rows(bounds[c], bounds[c + 1], b, s);\n      }\n      if (b + 1 < n_blocks) barrier.wait();  // one block: never waits, as rung 6\n    }\n  };\n  std::vector<std::thread> helpers;\n  helpers.reserve(nt - 1);\n  for (unsigned t = 1; t < nt; ++t) {\n    try {\n      helpers.emplace_back(worker);\n    } catch (...) {\n      break;\n    }\n  }\n  if (helpers.size() + 1 < nt) barrier.set_parties(static_cast<unsigned>(helpers.size() + 1));\n  worker();\n  for (std::thread& h : helpers) h.join();\n#endif\n}\n\n}  // namespace\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  const Mapped m = w.map(\"csr_packed.bin\");\n  if (m.bytes() < 128) fatal(\"csr_packed.bin is truncated; re-run prepare\");\n  const std::uint64_t* hdr = m.as<std::uint64_t>(0);\n  const std::uint64_t n_cells = hdr[0], n_genes = hdr[1], format = hdr[3];\n  if (n_cells != p.n_cells || n_genes != p.n_genes)\n    fatal(\"csr_packed.bin dimensions do not match the harness; re-run prepare\");\n  if (m.bytes() != hdr[10]) fatal(\"csr_packed.bin has the wrong size; re-run prepare\");\n  const std::uint32_t* row_nnz = m.as<std::uint32_t>(hdr[5]);\n\n  const Caches caches = query_caches();\n  const Blocks blocks = plan_blocks(n_genes, p.k, caches);\n  // Prefetch when the working set -- now one block of W -- outgrows L2.\n  const std::uint32_t dist = prefetch_distance(blocks.genes, p.k, caches);\n  std::unique_ptr<Cursor[]> cursors(blocks.count > 1 ? new Cursor[n_cells] : nullptr);\n\n  // Rung 9: the decoded rows hold gene * k, unless that would overflow a u32.\n  const bool pre = n_genes * p.k <= 0xFFFFFFFFull;\n  const std::uint32_t scale = pre ? static_cast<std::uint32_t>(p.k) : 1u;\n\n  // Rung 10: W repacked into (gene block x panel) tiles. P1_REPACK=0 keeps\n  // rung 9a's layout, which is the A/B for this rung.\n  bool tiled = format == 3 && p.k > 0 && blocks.genes > 0;\n  if (const char* e = std::getenv(\"P1_REPACK\")) {\n    if (std::strtoll(e, nullptr, 10) == 0) tiled = false;\n  }\n  std::unique_ptr<double[]> tiles(\n      tiled ? new double[std::uint64_t(blocks.count) * blocks.genes * p.k] : nullptr);\n\n  if (format == 3) {\n    const PackedRows rows{m.as<std::uint32_t>(hdr[6]), m.as<std::uint32_t>(hdr[7]),\n                          m.as<std::uint8_t>(hdr[8]),  m.as<std::uint8_t>(hdr[9]),\n                          p.W, y.data, p.k, dist, n_genes, blocks, cursors.get(), scale, pre,\n                          tiles.get(), tiled};\n    run_rows(rows, row_nnz, n_cells, p.k, hdr[4], blocks.count, tiled, n_genes);\n  } else if (format == 2) {\n    const WideRows rows{row_nnz, m.as<std::uint32_t>(hdr[6]), m.as<double>(hdr[7]),\n                        p.W, y.data, p.k, dist, n_genes, blocks, cursors.get()};\n    run_rows(rows, row_nnz, n_cells, p.k, 0, blocks.count, false, n_genes);\n  } else {\n    fatal(\"csr_packed.bin has an unknown format; re-run prepare\");\n  }\n}",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    },
    {
      "id": "942d1a98-7004-442b-851c-c6908b97efd5",
      "name": "ahsan-11",
      "is_baseline": false,
      "version": 3,
      "owner_handle": "kowshid",
      "updated_at": "2026-09-23T14:37:42.945673+00:00",
      "reason": "no run on your account yet",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- RUNG 11: unchanged from rung 5. UNTIMED.\n//\n// Rungs 6, 7 and 9 change only solution.cpp. Rung 7 blocks W by gene without\n// any new offsets in this file: blocks are visited in ascending gene order, so\n// each row's decoder simply stops at a block boundary and resumes there.\n//\n// ---- rung 5 notes, unchanged ----\n//\n// RUNG 5: a data structure designed from the data.\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// The ONE change from rung 4: how each non-zero is encoded. Rung 2 spent 4\n// bytes on every non-zero (u16 gene + u16 count). The data says we can do with\n// about 1.3, by using what Lecture 3 (slides 28-34) points out about it:\n//\n//   * 69.8% of the counts are exactly 1.          -> say \"1\" with a single bit\n//   * genes are sorted within a row, gaps are small -> store the gap, not the\n//     (median 10, 99.8% under 128)                   gene (delta encoding)\n//   * counts other than 1 are small (max 419)      -> one byte each, rarely more\n//\n// Encoding, per row, in gene order:\n//\n//   code stream, one byte per non-zero:\n//     bit 7     1 if the count is 1, else 0 (count is in the value stream)\n//     bits 0-6  gap to the previous gene, 1..127 (the first gap is gene + 1)\n//     0 in bits 0-6 is an escape: the gap follows as two bytes (u16)\n//   value stream, one byte per non-zero whose count is not 1:\n//     2..255    the count itself\n//     0         escape: the count follows as two bytes (u16)\n//\n// Unlike the lecture's grouped-by-value layout, this keeps every row in gene\n// order, so the kernel adds the terms of each Y element in exactly the same\n// order as before: the compression costs no exactness.\n//\n// Each row starts at a recorded byte offset in both streams, so any row can be\n// decoded on its own -- which is what lets rung 3's threads keep splitting the\n// rows freely. A per-row count of non-zeros is kept for the thread balancing.\n//\n// csr_packed.bin layout, little-endian:\n//\n//     u64 header[16]:\n//       [0] n_cells  [1] n_genes  [2] nnz  [3] format  [4] max_row_nnz\n//       [5] off_row_nnz  [6] off_a  [7] off_b  [8] off_c  [9] off_d\n//       [10] total_bytes\n//     format 3 (packed): a = u32 code_ptr[n_cells + 1]  (byte offset of each row)\n//                        b = u32 val_ptr[n_cells + 1]   (byte offset of each row)\n//                        c = u8 code stream, d = u8 value stream (+64 pad bytes)\n//     format 2 (wide fallback, when a gene index or count does not fit):\n//                        a = u32 gene index[nnz], b = f64 value[nnz]\n//     u32 row_nnz[n_cells + 1] at off_row_nnz: prefix count of non-zeros\n//\n// Every section starts on a 64-byte boundary.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <vector>\n\n#if defined(_OPENMP)\n#include <omp.h>\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kFormatWide = 2;\nconstexpr std::uint64_t kFormatPacked = 3;\n\n// Diagnostic only (unchanged from rung 3): mirrors threads_granted() in solve.\nvoid report_threads() {\n  const char* env = std::getenv(\"OMP_NUM_THREADS\");\n#if defined(_OPENMP)\n  std::fprintf(stderr, \"prepare: solve will be granted %d threads (OpenMP; OMP_NUM_THREADS=%s)\\n\",\n               omp_get_max_threads(), env ? env : \"unset\");\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n  unsigned n = std::thread::hardware_concurrency();\n  const char* source = \"hardware threads\";\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0) {\n    n = static_cast<unsigned>(CPU_COUNT(&set));\n    source = \"CPUs in the affinity mask\";\n  }\n#endif\n  if (env && std::strtol(env, nullptr, 10) > 0) {\n    n = static_cast<unsigned>(std::strtol(env, nullptr, 10));\n    source = \"OMP_NUM_THREADS\";\n  }\n  std::fprintf(stderr, \"prepare: solve will be granted %u threads (std::thread; from %s)\\n\", n,\n               source);\n#else\n  std::fprintf(stderr, \"prepare: solve will run serially (browser build, no threads)\\n\");\n#endif\n}\n\nstd::uint64_t pad64(std::uint64_t x) { return (x + 63) & ~std::uint64_t(63); }\n\nvoid pad_to(hpcbench::Writer& w, std::uint64_t& pos, std::uint64_t target) {\n  static const unsigned char zeros[64] = {};\n  while (pos < target) {\n    const std::uint64_t n = std::min<std::uint64_t>(64, target - pos);\n    w.write(zeros, static_cast<std::size_t>(n));\n    pos += n;\n  }\n}\n\nvoid put(hpcbench::Writer& w, std::uint64_t& pos, const void* data, std::uint64_t bytes) {\n  w.write(data, static_cast<std::size_t>(bytes));\n  pos += bytes;\n}\n\n}  // namespace\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  const std::uint64_t n_cells = in.n_cells;\n  const std::uint64_t n_genes = in.n_genes;\n\n  // Pass 1: can the packed encoding represent this input exactly?\n  // Gaps must fit u16 (gene index <= 65,534) and counts must be integers\n  // 1..65,535. Otherwise fall back to u32 index + f64 value.\n  bool packable = n_genes <= 65535;\n  std::uint64_t nnz = 0, max_row = 0;\n  std::vector<std::uint32_t> row_nnz(n_cells + 1, 0);\n  for (std::uint64_t i = 0; i < n_cells; ++i) {\n    const double* xi = in.X + i * n_genes;\n    std::uint64_t row = 0;\n    for (std::uint64_t g = 0; g < n_genes; ++g) {\n      const double x = xi[g];\n      if (x == 0.0) continue;\n      ++row;\n      if (!(x >= 1.0 && x <= 65535.0 && x == std::floor(x))) packable = false;\n    }\n    nnz += row;\n    if (nnz > UINT32_MAX) fatal(\"more than 2^32 non-zeros: u32 offsets overflow\");\n    max_row = std::max(max_row, row);\n    row_nnz[i + 1] = static_cast<std::uint32_t>(nnz);\n  }\n\n  std::uint64_t header[16] = {};\n  header[0] = n_cells;\n  header[1] = n_genes;\n  header[2] = nnz;\n  header[4] = max_row;\n  header[5] = 128;  // off_row_nnz\n  const std::uint64_t after_row_nnz = pad64(header[5] + 4 * (n_cells + 1));\n\n  Writer w = out.create(\"csr_packed.bin\");\n  std::uint64_t pos = 0;\n\n  if (packable) {\n    // Pass 2: encode.\n    std::vector<std::uint8_t> code, vals;\n    std::vector<std::uint32_t> code_ptr(n_cells + 1, 0), val_ptr(n_cells + 1, 0);\n    code.reserve(static_cast<std::size_t>(nnz + nnz / 64));\n    vals.reserve(static_cast<std::size_t>(nnz / 2));\n    std::uint64_t gap_escapes = 0, val_escapes = 0;\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      std::uint64_t prev = ~std::uint64_t(0);  // \"gene -1\": the first gap is gene + 1\n      for (std::uint64_t g = 0; g < n_genes; ++g) {\n        const double x = xi[g];\n        if (x == 0.0) continue;\n        const std::uint64_t gap = g - prev;  // 1..65,535\n        prev = g;\n        const std::uint32_t v = static_cast<std::uint32_t>(x);\n        const std::uint8_t one = (v == 1) ? 0x80 : 0x00;\n        if (gap < 128) {\n          code.push_back(static_cast<std::uint8_t>(one | gap));\n        } else {\n          code.push_back(one);  // gap field 0 = escape\n          code.push_back(static_cast<std::uint8_t>(gap & 0xFF));\n          code.push_back(static_cast<std::uint8_t>(gap >> 8));\n          ++gap_escapes;\n        }\n        if (v != 1) {\n          if (v < 256) {\n            vals.push_back(static_cast<std::uint8_t>(v));\n          } else {\n            vals.push_back(0);  // escape\n            vals.push_back(static_cast<std::uint8_t>(v & 0xFF));\n            vals.push_back(static_cast<std::uint8_t>(v >> 8));\n            ++val_escapes;\n          }\n        }\n      }\n      if (code.size() > UINT32_MAX || vals.size() > UINT32_MAX)\n        fatal(\"packed streams exceed 4 GB\");\n      code_ptr[i + 1] = static_cast<std::uint32_t>(code.size());\n      val_ptr[i + 1] = static_cast<std::uint32_t>(vals.size());\n    }\n\n    header[3] = kFormatPacked;\n    header[6] = after_row_nnz;                               // code_ptr\n    header[7] = pad64(header[6] + 4 * (n_cells + 1));        // val_ptr\n    header[8] = pad64(header[7] + 4 * (n_cells + 1));        // code stream\n    header[9] = pad64(header[8] + code.size());              // value stream\n    header[10] = header[9] + vals.size() + 64;               // + pad: the decoder\n                                                             // may peek one byte ahead\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, code_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[7]);\n    put(w, pos, val_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[8]);\n    put(w, pos, code.data(), code.size());\n    pad_to(w, pos, header[9]);\n    put(w, pos, vals.data(), vals.size());\n    pad_to(w, pos, header[10]);\n    w.close();\n\n    std::fprintf(stderr,\n                 \"prepare: packed CSR, %llu nonzeros, %.2f MB (%.2f bytes/nnz); \"\n                 \"code %.2f MB, values %.2f MB, %llu gap and %llu value escapes\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6,\n                 double(header[10]) / double(nnz), double(code.size()) / 1e6,\n                 double(vals.size()) / 1e6, (unsigned long long)gap_escapes,\n                 (unsigned long long)val_escapes);\n  } else {\n    std::vector<std::uint32_t> col;\n    std::vector<double> val;\n    col.reserve(static_cast<std::size_t>(nnz));\n    val.reserve(static_cast<std::size_t>(nnz));\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      for (std::uint64_t g = 0; g < n_genes; ++g)\n        if (xi[g] != 0.0) {\n          col.push_back(static_cast<std::uint32_t>(g));\n          val.push_back(xi[g]);\n        }\n    }\n    header[3] = kFormatWide;\n    header[6] = after_row_nnz;                      // u32 gene index\n    header[7] = pad64(header[6] + 4 * nnz);         // f64 value\n    header[10] = header[7] + 8 * nnz;\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, col.data(), 4 * nnz);\n    pad_to(w, pos, header[7]);\n    put(w, pos, val.data(), 8 * nnz);\n    w.close();\n    std::fprintf(stderr, \"prepare: wide fallback CSR (u32 + f64), %llu nonzeros, %.2f MB\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6);\n  }\n  report_threads();\n}",
        "src/solution.cpp": "// src/solution.cpp -- RUNG 11: stop multiplying by one.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Stacked on rung 10. The ONE change: a decoded row is split into runs of\n// count-1 non-zeros, and the kernel adds those instead of multiplying.\n//\n// 69.8% of this matrix's non-zeros are exactly 1, and rung 5's format already\n// says which: bit 7 of the code byte. So the decoder can record, for free, the\n// length of each run of ones, and the kernel walks a row as\n//     [L ones] [one other] [L ones] [one other] ...\n// with the ones handled by  acc += w  -- no count to load, no broadcast, no\n// multiply. Runs average about 3.3 non-zeros here.\n//\n// Exact, not approximate: fma(1.0, w, acc) and acc + w are the same double, so\n// the terms, their order and their roundings are unchanged. The count array\n// also shrinks, because only the non-ones need a value.\n//\n// What this cannot save: the multiply itself. An FMA and an add cost the same\n// on both of these cores, so the win, if any, is the loads and the broadcast --\n// against one branch per run, which is the risk.\n//\n// Correctness: bit-for-bit the baseline's.\n//\n// Knobs: P1_REPACK, P1_W_BLOCK_BYTES, P1_PREFETCH_DIST.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cstdint>\n#include <cstdlib>\n#include <cstring>\n#include <memory>\n#include <vector>\n\n#if defined(__APPLE__)\n#include <sys/sysctl.h>\n#elif defined(__linux__)\n#include <unistd.h>\n#endif\n\n#if defined(_OPENMP)\n#include <omp.h>\n#define P1_OPENMP 1\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#define P1_STD_THREAD 1\n#include <atomic>\n#include <chrono>\n#include <condition_variable>\n#include <mutex>\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\n#ifndef P1_PANEL  // override with -DP1_PANEL=... (up to 64) in CXXFLAGS to experiment\n#define P1_PANEL 32\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kMinWorkPerThread = std::uint64_t(1) << 19;  // multiply-adds\nconstexpr std::uint64_t kChunksPerThread = 32;\nconstexpr std::uint64_t kMaxBlocks = 64;  // each block costs a barrier\n\n// ---- rung 4's kernel, unchanged -------------------------------------------\n\ntypedef double v4d __attribute__((vector_size(32)));  // four doubles\n\nconstexpr int kPanel = P1_PANEL;\n\n// acc + x * w, rounded exactly once, the way the baseline's `y += x * w` is.\n//\n// Where the target has a hardware fused multiply-add (__FP_FAST_FMA: every\n// arm64, x86 with FMA), the compiler fuses the baseline's statement into one\n// instruction. It does not reliably fuse ours: GCC's tuning for recent Intel\n// cores (\"avoid FMA chains\") splits a loop-carried accumulation into a multiply\n// and an add when only one or two accumulator vectors carry it -- exactly our\n// narrow panels -- which rounds twice. So the fusion is spelled out here.\n// Without hardware FMA the baseline rounds twice, and so does this.\ninline double madd(double x, double w, double acc) {\n#if defined(__FP_FAST_FMA)\n  return __builtin_fma(x, w, acc);\n#else\n  return x * w + acc;\n#endif\n}\n\ninline v4d vmadd(v4d x, v4d w, v4d acc) {\n#if defined(__FP_FAST_FMA)\n  // Four scalar fused multiply-adds; every compiler tested turns them into one\n  // vector FMA (vfmadd231pd on x86, two fmla on arm64).\n  return v4d{__builtin_fma(x[0], w[0], acc[0]), __builtin_fma(x[1], w[1], acc[1]),\n             __builtin_fma(x[2], w[2], acc[2]), __builtin_fma(x[3], w[3], acc[3])};\n#else\n  return x * w + acc;\n#endif\n}\n\n// One row, one panel of P output columns:  yc[0:P] = sum over the row's\n// non-zeros of x * Wc[g*k + 0:P], accumulated in registers. With `accumulate`\n// the sums start from what yc already holds -- the partial sums left there by\n// the previous gene blocks -- instead of from zero.\n//\n// noinline: each width is compiled as its own function. Measured necessity,\n// not style: GCC 13 inlined all 32 prefetching widths into the switch below and\n// the result ran k = 16 at 18.1 ms instead of 10.1 ms (k = 13: 15.1 vs 8.5) at\n// every prefetch distance from 1 to 64 -- a code-generation effect, not a\n// memory one. The same attribute on rungs 4 and 5 measured no difference.\n// How col[] addresses W:\n//   TILED  gene index within its block; the row is at col[q] * P in the tile\n//   PRE    gene * k already (rung 9a), so the kernel only adds\n//   else   plain gene index, multiplied by k here (rung 7, and the CSR paths)\n// RUNS: col[] is grouped as runs of count-1 non-zeros, each run followed by one\n// non-one whose value is the next entry of val[]; runs[] holds the run lengths.\ntemplate <int P, bool PF, bool TILED, bool PRE, bool RUNS, class Idx, class Val>\n__attribute__((noinline)) void row_panel(const Idx* col, const Val* val,\n                                         const std::uint32_t* runs, std::uint32_t segs,\n                                         std::uint32_t n, const double* Wc, std::uint64_t k,\n                                         double* yc, std::uint32_t dist, bool accumulate) {\n  constexpr int V = P / 4;  // full 4-wide vectors\n  constexpr int R = P % 4;  // 1-3 leftover columns, scalar\n  v4d acc[V > 0 ? V : 1];\n  double tail[R > 0 ? R : 1];\n  if (accumulate) {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) std::memcpy(&acc[v], yc + 4 * v, sizeof(v4d));\n    for (int r = 0; r < R; ++r) tail[r] = yc[4 * V + r];\n  } else {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) acc[v] = v4d{0.0, 0.0, 0.0, 0.0};\n    for (int r = 0; r < R; ++r) tail[r] = 0.0;\n  }\n\n  // One non-zero: address it, prefetch ahead of it, and accumulate.\n  const auto addr = [&](std::uint32_t q) {\n    return Wc + (TILED ? static_cast<std::uint64_t>(col[q]) * P\n                       : (PRE ? static_cast<std::uint64_t>(col[q])\n                              : static_cast<std::uint64_t>(col[q]) * k));\n  };\n  const auto prefetch = [&](std::uint32_t q) {\n    if (PF && q + dist < n) {\n      const char* ahead = reinterpret_cast<const char*>(addr(q + dist));\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n  };\n  if (RUNS) {\n    std::uint32_t q = 0, vi = 0;\n    for (std::uint32_t j = 0; j < segs; ++j) {\n      for (std::uint32_t L = runs[j]; L > 0; --L, ++q) {  // count == 1: add, do not multiply\n        prefetch(q);\n        const double* w = addr(q);\n#pragma GCC unroll 16\n        for (int v = 0; v < V; ++v) {\n          v4d wv;\n          std::memcpy(&wv, w + 4 * v, sizeof(wv));\n          acc[v] = acc[v] + wv;\n        }\n        for (int r = 0; r < R; ++r) tail[r] = tail[r] + w[4 * V + r];\n      }\n      if (q < n) {  // the non-one that closes the run\n        prefetch(q);\n        const double x = static_cast<double>(val[vi++]);\n        const v4d xv = v4d{x, x, x, x};\n        const double* w = addr(q);\n#pragma GCC unroll 16\n        for (int v = 0; v < V; ++v) {\n          v4d wv;\n          std::memcpy(&wv, w + 4 * v, sizeof(wv));\n          acc[v] = vmadd(xv, wv, acc[v]);\n        }\n        for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n        ++q;\n      }\n    }\n  } else\n  for (std::uint32_t q = 0; q < n; ++q) {\n    if (PF && q + dist < n) {\n      // The W segment this row will need `dist` non-zeros from now: one\n      // prefetch per 64 bytes (a cache line on x86, half of one on Apple).\n      const std::uint64_t ahead_off =\n          TILED ? static_cast<std::uint64_t>(col[q + dist]) * P\n                : (PRE ? static_cast<std::uint64_t>(col[q + dist])\n                       : static_cast<std::uint64_t>(col[q + dist]) * k);\n      const char* ahead = reinterpret_cast<const char*>(Wc + ahead_off);\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n    const double x = static_cast<double>(val[q]);\n    const v4d xv = v4d{x, x, x, x};\n    const double* w = Wc + (TILED ? static_cast<std::uint64_t>(col[q]) * P\n                                  : (PRE ? static_cast<std::uint64_t>(col[q])\n                                         : static_cast<std::uint64_t>(col[q]) * k));\n    // Fully unrolled: GCC stops at 8 vectors on its own and would otherwise\n    // keep a 64-wide panel's accumulators on the stack.\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) {\n      v4d wv;\n      std::memcpy(&wv, w + 4 * v, sizeof(wv));  // unaligned vector load\n      acc[v] = vmadd(xv, wv, acc[v]);\n    }\n    for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n  }\n\n#pragma GCC unroll 16\n  for (int v = 0; v < V; ++v) std::memcpy(yc + 4 * v, &acc[v], sizeof(v4d));\n  for (int r = 0; r < R; ++r) yc[4 * V + r] = tail[r];\n}\n\n// The switch: a run-time width selects a compile-time kernel.\ntemplate <bool PF, bool TILED, bool PRE, bool RUNS, class Idx, class Val>\nvoid row_panel_any(int width, const Idx* col, const Val* val, const std::uint32_t* runs,\n                   std::uint32_t segs, std::uint32_t n, const double* Wc, std::uint64_t k,\n                   double* yc, std::uint32_t dist, bool accumulate) {\n  switch (width) {\n#define P1_CASE(P)                                                  \\\n  case P:                                                           \\\n    row_panel<P, PF, TILED, PRE, RUNS>(col, val, runs, segs, n, Wc, k, yc, dist, accumulate); \\\n    return;\n    P1_CASE(1)\n    P1_CASE(2)\n    P1_CASE(3)\n    P1_CASE(4)\n    P1_CASE(5)\n    P1_CASE(6)\n    P1_CASE(7)\n    P1_CASE(8)\n    P1_CASE(9)\n    P1_CASE(10)\n    P1_CASE(11)\n    P1_CASE(12)\n    P1_CASE(13)\n    P1_CASE(14)\n    P1_CASE(15)\n    P1_CASE(16)\n    P1_CASE(17)\n    P1_CASE(18)\n    P1_CASE(19)\n    P1_CASE(20)\n    P1_CASE(21)\n    P1_CASE(22)\n    P1_CASE(23)\n    P1_CASE(24)\n    P1_CASE(25)\n    P1_CASE(26)\n    P1_CASE(27)\n    P1_CASE(28)\n    P1_CASE(29)\n    P1_CASE(30)\n    P1_CASE(31)\n    P1_CASE(32)\n#if P1_PANEL > 32\n    P1_CASE(33)\n    P1_CASE(34)\n    P1_CASE(35)\n    P1_CASE(36)\n    P1_CASE(37)\n    P1_CASE(38)\n    P1_CASE(39)\n    P1_CASE(40)\n    P1_CASE(41)\n    P1_CASE(42)\n    P1_CASE(43)\n    P1_CASE(44)\n    P1_CASE(45)\n    P1_CASE(46)\n    P1_CASE(47)\n    P1_CASE(48)\n    P1_CASE(49)\n    P1_CASE(50)\n    P1_CASE(51)\n    P1_CASE(52)\n    P1_CASE(53)\n    P1_CASE(54)\n    P1_CASE(55)\n    P1_CASE(56)\n    P1_CASE(57)\n    P1_CASE(58)\n    P1_CASE(59)\n    P1_CASE(60)\n    P1_CASE(61)\n    P1_CASE(62)\n    P1_CASE(63)\n    P1_CASE(64)\n#endif\n#undef P1_CASE\n    default:\n      hpcbench::fatal(\"panel width out of range\");\n  }\n}\n\n// ---- rung 5: the packed representation -------------------------------------\n\n// One row's worth of decoded non-zeros, owned by one thread.\nstruct Scratch {\n  std::unique_ptr<std::uint32_t[]> genes;  // gene index (within its block when tiled)\n  std::unique_ptr<double[]> counts;        // rung 11: only the counts that are not 1\n  std::unique_ptr<std::uint32_t[]> runs;   // rung 11: lengths of the runs of ones\n  std::uint32_t segs = 0;                  // rung 11: how many runs the last decode wrote\n  explicit Scratch(std::uint64_t cap)\n      : genes(new std::uint32_t[cap > 0 ? cap : 1]),\n        counts(new double[cap > 0 ? cap : 1]),\n        runs(new std::uint32_t[cap > 0 ? cap + 1 : 2]) {}\n};\n\n// ---- rung 7: gene blocks --------------------------------------------------\n\n// The genes [b * genes, min((b + 1) * genes, n_genes)) form block b of W.\nstruct Blocks {\n  std::uint32_t genes;  // genes per block (the last one may be smaller)\n  std::uint32_t count;  // number of blocks; 1 = W fits, no blocking\n};\n\n// Where row i's decoding stopped at the end of the previous block.\nstruct Cursor {\n  std::uint32_t code;  // next code byte (packed) / next non-zero (wide)\n  std::uint32_t val;   // next value byte (packed only)\n  std::uint32_t gene;  // last gene decoded (packed only; 0xFFFFFFFF = none yet)\n};\n\nstruct PackedRows {\n  const std::uint32_t* code_ptr;  // byte offset of each row in the code stream\n  const std::uint32_t* val_ptr;   // byte offset of each row in the value stream\n  const std::uint8_t* code;\n  const std::uint8_t* vals;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;      // prefetch distance in non-zeros; 0 = off\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;         // one per row; used only when blocks.count > 1\n  std::uint32_t scale;     // rung 9: k when gene * k fits in u32, else 1\n  bool pre;                // rung 9: true when the scratch holds gene * k\n  double* tiles;           // rung 10: W in (gene block x panel) tiles, or null\n  bool tiled;              // rung 10: true when the kernel reads the tiles\n\n  // Rung 10: copy this slice of W's genes into the tiles. Threads split the\n  // genes between them and meet at a barrier before any multiplying starts.\n  void repack(std::uint64_t g0, std::uint64_t g1) const {\n    const std::uint64_t G = blocks.genes;\n    for (std::uint64_t g = g0; g < g1; ++g) {\n      const std::uint64_t b = g / G, lg = g - b * G;\n      const double* src = W + g * k;\n      double* dst = tiles + b * G * k;\n      for (std::uint64_t c0 = 0; c0 < k; c0 += kPanel) {\n        const std::uint64_t width = std::min<std::uint64_t>(kPanel, k - c0);\n        std::memcpy(dst + G * c0 + lg * width, src + c0, width * sizeof(double));\n      }\n    }\n  }\n\n  // Decode row i into s.genes / s.counts; returns its number of non-zeros.\n  template <bool TILED>\n  std::uint32_t decode(std::uint64_t i, std::uint32_t origin, Scratch& s) const {\n    const std::uint8_t* cp = code + code_ptr[i];\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + val_ptr[i];\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t* runs = s.runs.get();\n    std::uint32_t segs = 0, nv = 0, ones = 0;  // rung 11: current run of ones\n    std::uint32_t g = 0xFFFFFFFFu;  // \"gene -1\": the first gap is gene + 1\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = *cp++;\n      std::uint32_t gap = b & 0x7Fu;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[0]) | (std::uint32_t(cp[1]) << 8);\n        cp += 2;\n      }\n      g += gap;\n      // Count: 1 if bit 7 is set, else the next byte of the value stream.\n      // Written as arithmetic on purpose: GCC compiles `one ? 1 : peek` into a\n      // branch, and a 70/30 coin flip per non-zero is a branch no predictor can\n      // learn (measured: ~9 cycles per non-zero instead of ~3).\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;   // 1 if the count is in the value stream\n      const std::uint32_t peek = *vp;                 // always in bounds: stream is padded\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);  // peek, or 1\n      vp += uses_val;                                 // consume a value byte only if used\n      if (v == 0) {                                   // escape: two-byte count\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      if (uses_val) {  // rung 11: a count of 1 closes nothing and stores nothing\n        runs[segs++] = ones;\n        ones = 0;\n        counts[nv++] = static_cast<double>(v);\n      } else {\n        ++ones;\n      }\n      ++n;\n    }\n    if (ones) runs[segs++] = ones;  // a trailing run with no non-one after it\n    s.segs = segs;\n    return n;\n  }\n\n  // Rung 7: decode only the non-zeros of row i whose gene is below gene_end,\n  // starting where cur says the previous block stopped; leave cur at the\n  // first non-zero of a later block. Same decoder as above, plus a peek at the\n  // next gene before consuming it.\n  template <bool TILED>\n  std::uint32_t decode_until(std::uint64_t i, std::uint32_t gene_end, std::uint32_t origin,\n                             Cursor& cur, Scratch& s) const {\n    const std::uint8_t* cp = code + cur.code;\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + cur.val;\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t* runs = s.runs.get();\n    std::uint32_t segs = 0, nv = 0, ones = 0;  // rung 11: current run of ones\n    std::uint32_t g = cur.gene;\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = cp[0];\n      std::uint32_t gap = b & 0x7Fu, len = 1;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[1]) | (std::uint32_t(cp[2]) << 8);\n        len = 3;\n      }\n      if (g + gap >= gene_end) break;  // belongs to a later block: leave it for then\n      cp += len;\n      g += gap;\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;\n      const std::uint32_t peek = *vp;\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);\n      vp += uses_val;\n      if (v == 0) {\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      if (uses_val) {\n        runs[segs++] = ones;\n        ones = 0;\n        counts[nv++] = static_cast<double>(v);\n      } else {\n        ++ones;\n      }\n      ++n;\n    }\n    if (ones) runs[segs++] = ones;\n    s.segs = segs;\n    cur = Cursor{static_cast<std::uint32_t>(cp - code), static_cast<std::uint32_t>(vp - vals), g};\n    return n;\n  }\n\n  // Rows [r0, r1), gene block b.\n  template <bool PF, bool TILED, bool PRE>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint32_t gene_end = static_cast<std::uint32_t>(\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes));\n    const bool accumulate = b > 0;  // later blocks add to the partial sums in Y\n    const std::uint32_t origin = static_cast<std::uint32_t>(std::uint64_t(b) * blocks.genes);\n    // Rung 10: the block's tiles start here; panel c0 of it is G * c0 further on.\n    const double* const wb = TILED ? tiles + std::uint64_t(b) * blocks.genes * k : W;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t n;\n      if (blocks.count == 1) {\n        n = decode<TILED>(i, origin, s);  // W fits: the whole row at once\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur = Cursor{code_ptr[i], val_ptr[i], 0xFFFFFFFFu};\n        n = decode_until<TILED>(i, gene_end, origin, cur, s);\n        // Nothing of this row in block b: its partial sums in Y are already\n        // right. (Block 0 still writes every row, so Y never relies on the\n        // harness having zero-filled it.)\n        if (n == 0 && accumulate) continue;\n      }\n      const std::uint32_t* genes = s.genes.get();\n      const double* counts = s.counts.get();\n      const std::uint32_t* runs = s.runs.get();\n      const std::uint32_t segs = s.segs;\n      double* yi = Y + i * k;\n      const std::uint64_t stride = TILED ? blocks.genes : 1;  // panel c0 starts G * c0 in\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, TILED, PRE, true>(genes, counts, runs, segs, n, wb + stride * c0, k,\n                                                yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, TILED, PRE, true>(rem, genes, counts, runs, segs, n, wb + stride * full,\n                                            k, yi + full, dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    if (dist > 0) {\n      if (tiled) rows<true, true, false>(r0, r1, b, s);\n      else if (pre) rows<true, false, true>(r0, r1, b, s);\n      else rows<true, false, false>(r0, r1, b, s);\n    } else {\n      if (tiled) rows<false, true, false>(r0, r1, b, s);\n      else if (pre) rows<false, false, true>(r0, r1, b, s);\n      else rows<false, false, false>(r0, r1, b, s);\n    }\n  }\n};\n\n// The wide fallback prepare writes when the packed encoding cannot represent\n// the input exactly: plain u32 / f64 CSR, handled exactly as rung 4 did.\nstruct WideRows {\n  const std::uint32_t* row_nnz;\n  const std::uint32_t* col;\n  const double* val;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;\n\n  void repack(std::uint64_t, std::uint64_t) const {}  // rung 10: plain CSR keeps W as given\n\n  template <bool PF>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint64_t gene_end =\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes);\n    const bool accumulate = b > 0;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t q0, n;\n      if (blocks.count == 1) {\n        q0 = row_nnz[i];\n        n = row_nnz[i + 1] - q0;\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur.code = row_nnz[i];\n        q0 = cur.code;\n        std::uint32_t q = q0;\n        while (q < row_nnz[i + 1] && col[q] < gene_end) ++q;\n        n = q - q0;\n        cur.code = q;\n        if (n == 0 && accumulate) continue;\n      }\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, false, false, false>(col + q0, val + q0, nullptr, 0, n, W + c0, k,\n                                                   yi + c0, dist, accumulate);\n      if (rem)\n        row_panel_any<PF, false, false, false>(rem, col + q0, val + q0, nullptr, 0, n, W + full, k,\n                                               yi + full, dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch&) const {\n    if (dist > 0) rows<true>(r0, r1, b);\n    else rows<false>(r0, r1, b);\n  }\n};\n\n// ---- rung 6: cache sizes and the prefetch policy ---------------------------\n\nstruct Caches {\n  std::uint64_t l1d, l2, llc;  // bytes; llc = the largest cache shared by a cluster/socket\n  std::uint64_t l2_min;        // rung 7: the smallest L2 any thread may run on\n};\n\n#if defined(__APPLE__)\nstd::uint64_t sysctl_u64(const char* name) {\n  std::uint64_t v = 0;\n  std::size_t len = sizeof(v);\n  if (sysctlbyname(name, &v, &len, nullptr, 0) != 0) return 0;\n  return len == 4 ? static_cast<std::uint32_t>(v) : v;\n}\n#endif\n\n// This machine's cache sizes. On Apple silicon the performance cluster's\n// values (perflevel0: 128 KB L1d, 16 MB L2 on an M4) -- the L2 is the last\n// level each cluster owns. On Linux, glibc's sysconf. Defaults where unknown.\nCaches query_caches() {\n  Caches c{0, 0, 0, 0};\n#if defined(__APPLE__)\n  c.l1d = sysctl_u64(\"hw.perflevel0.l1dcachesize\");\n  c.l2 = sysctl_u64(\"hw.perflevel0.l2cachesize\");\n  if (c.l1d == 0) c.l1d = sysctl_u64(\"hw.l1dcachesize\");\n  if (c.l2 == 0) c.l2 = sysctl_u64(\"hw.l2cachesize\");\n  c.llc = c.l2;\n  c.l2_min = sysctl_u64(\"hw.perflevel1.l2cachesize\");  // efficiency cluster, if any\n#elif defined(__linux__) && defined(_SC_LEVEL1_DCACHE_SIZE)\n  const long l1 = sysconf(_SC_LEVEL1_DCACHE_SIZE);\n  const long l2 = sysconf(_SC_LEVEL2_CACHE_SIZE);\n  const long l3 = sysconf(_SC_LEVEL3_CACHE_SIZE);\n  c.l1d = l1 > 0 ? static_cast<std::uint64_t>(l1) : 0;\n  c.l2 = l2 > 0 ? static_cast<std::uint64_t>(l2) : 0;\n  c.llc = l3 > 0 ? static_cast<std::uint64_t>(l3) : c.l2;\n#endif\n  if (c.l1d == 0) c.l1d = 32 << 10;  // unknown: a typical L1d\n  if (c.l2 == 0) c.l2 = 1 << 20;     // unknown: a typical per-core L2\n  if (c.llc == 0) c.llc = 8 << 20;   // unknown: a typical shared L3\n  if (c.llc < c.l2) c.llc = c.l2;\n  if (c.l2_min == 0 || c.l2_min > c.l2) c.l2_min = c.l2;\n  return c;\n}\n\n// Prefetch distance for this run: 0 (off) when W fits in half of L2, else\n// P1_PREFETCH_DIST or 8 non-zeros ahead.\nstd::uint32_t prefetch_distance(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  std::uint32_t dist = 8;\n  if (const char* s = std::getenv(\"P1_PREFETCH_DIST\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v >= 0) dist = static_cast<std::uint32_t>(v);\n  }\n  const std::uint64_t w_bytes = n_genes * k * sizeof(double);\n  return w_bytes > c.l2 / 2 ? dist : 0;\n}\n\n// Rung 7: how to cut W into gene blocks. No blocking while W fits in the\n// smallest L2; otherwise blocks of a quarter of that L2. P1_W_BLOCK_BYTES sets\n// the block size and blocks whenever W is larger. Blocks are made equal in\n// size, and there are never more than kMaxBlocks of them.\nBlocks plan_blocks(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  const std::uint64_t row_bytes = k * sizeof(double);\n  const std::uint64_t w_bytes = n_genes * row_bytes;\n  std::uint64_t budget = c.l2_min / 4, fits = c.l2_min;\n  if (const char* s = std::getenv(\"P1_W_BLOCK_BYTES\")) {\n    const long long v = std::strtoll(s, nullptr, 10);\n    if (v > 0) budget = fits = static_cast<std::uint64_t>(v);\n  }\n  if (n_genes == 0 || row_bytes == 0 || w_bytes <= fits)\n    return Blocks{static_cast<std::uint32_t>(n_genes), 1};\n  const std::uint64_t max_genes = std::max<std::uint64_t>(1, budget / row_bytes);\n  std::uint64_t count = std::min<std::uint64_t>((n_genes + max_genes - 1) / max_genes, kMaxBlocks);\n  const std::uint64_t genes = (n_genes + count - 1) / count;\n  count = (n_genes + genes - 1) / genes;\n  return Blocks{static_cast<std::uint32_t>(genes), static_cast<std::uint32_t>(count)};\n}\n\n// ---- threading: rung 3's, with one Scratch per thread and, new in rung 7, a\n// barrier between gene blocks ----------------------------------------------\n\nunsigned threads_granted() {\n#if defined(P1_OPENMP)\n  return static_cast<unsigned>(std::max(1, omp_get_max_threads()));\n#elif defined(P1_STD_THREAD)\n  if (const char* s = std::getenv(\"OMP_NUM_THREADS\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v > 0) return static_cast<unsigned>(v);\n  }\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0)\n    return static_cast<unsigned>(CPU_COUNT(&set));\n#endif\n  const unsigned hc = std::thread::hardware_concurrency();\n  return hc > 0 ? hc : 1;\n#else\n  return 1;\n#endif\n}\n\n#if defined(P1_STD_THREAD)\n// A reusable barrier (C++17 has none): each thread waits here until all have\n// finished the current gene block. The party count starts at the number of\n// threads planned and is lowered if creating one of them fails.\n//\n// A waiter first spins for up to 50 us: the other threads are usually only\n// microseconds behind, and a sleep plus wake-up through the kernel costs tens\n// of microseconds on macOS -- with 27 blocks at k = 256, that adds up. After\n// 50 us it sleeps, so an oversubscribed machine does not burn cores.\nclass Barrier {\n public:\n  explicit Barrier(unsigned parties) : parties_(parties) {}\n  void set_parties(unsigned parties) {\n    std::lock_guard<std::mutex> lock(m_);\n    parties_ = parties;\n    if (arrived_ > 0 && arrived_ >= parties_) release();\n  }\n  void wait() {\n    std::unique_lock<std::mutex> lock(m_);\n    const unsigned gen = generation_.load(std::memory_order_relaxed);\n    if (++arrived_ >= parties_) {\n      release();\n      return;\n    }\n    lock.unlock();\n    const auto until = std::chrono::steady_clock::now() + std::chrono::microseconds(50);\n    while (std::chrono::steady_clock::now() < until)\n      if (generation_.load(std::memory_order_acquire) != gen) return;\n    lock.lock();\n    cv_.wait(lock, [&] { return generation_.load(std::memory_order_relaxed) != gen; });\n  }\n\n private:\n  void release() {  // called with m_ held\n    arrived_ = 0;\n    generation_.fetch_add(1, std::memory_order_release);\n    cv_.notify_all();\n  }\n  std::mutex m_;\n  std::condition_variable cv_;\n  unsigned parties_, arrived_ = 0;\n  std::atomic<unsigned> generation_{0};\n};\n#endif\n\ntemplate <class Rows>\nvoid run_rows(const Rows& rows, const std::uint32_t* row_nnz, std::uint64_t n_cells,\n              std::uint64_t k, std::uint64_t scratch_cap, std::uint32_t n_blocks,\n              bool do_repack, std::uint64_t n_genes) {\n  const std::uint64_t nnz = row_nnz[n_cells];\n  const std::uint64_t floor_threads = std::max<std::uint64_t>(1, nnz * k / kMinWorkPerThread);\n  const unsigned nt =\n      static_cast<unsigned>(std::min<std::uint64_t>(threads_granted(), floor_threads));\n  if (nt <= 1) {\n    Scratch s(scratch_cap);\n    if (do_repack) rows.repack(0, n_genes);  // rung 10\n    for (std::uint32_t b = 0; b < n_blocks; ++b) rows(0, n_cells, b, s);\n    return;\n  }\n\n  std::vector<std::uint32_t> bounds;\n  bounds.reserve(static_cast<std::size_t>(nt * kChunksPerThread + 2));\n  bounds.push_back(0);\n  const std::uint64_t target = std::max<std::uint64_t>(1, nnz / (nt * kChunksPerThread));\n  std::uint64_t chunk_start = row_nnz[0];\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    if (row_nnz[i + 1] - chunk_start >= target) {\n      bounds.push_back(static_cast<std::uint32_t>(i + 1));\n      chunk_start = row_nnz[i + 1];\n    }\n  if (bounds.back() != n_cells) bounds.push_back(static_cast<std::uint32_t>(n_cells));\n  const std::size_t n_chunks = bounds.size() - 1;\n\n  // Rung 10: the threads build W's tiles first and meet at a barrier; nothing\n  // reads a tile before every thread has finished writing its genes.\n  const std::size_t n_gchunks = static_cast<std::size_t>(nt) * 4;\n  const auto gene_lo = [&](std::size_t j) { return n_genes * j / n_gchunks; };\n\n#if defined(P1_OPENMP)\n  const long nc = static_cast<long>(n_chunks);\n#pragma omp parallel num_threads(nt)\n  {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n#pragma omp for schedule(static)\n      for (long gg = 0; gg < static_cast<long>(n_gchunks); ++gg)\n        rows.repack(gene_lo(static_cast<std::size_t>(gg)),\n                    gene_lo(static_cast<std::size_t>(gg) + 1));\n    }  // implicit barrier\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      // The implicit barrier at the end of this loop keeps all threads on the\n      // same block of W: nobody starts block b + 1 before every row has done b.\n#pragma omp for schedule(dynamic, 1)\n      for (long c = 0; c < nc; ++c) rows(bounds[c], bounds[c + 1], b, s);\n    }\n  }\n#elif defined(P1_STD_THREAD)\n  // One chunk counter per block, so a block's chunks are handed out afresh.\n  std::unique_ptr<std::atomic<std::size_t>[]> next(new std::atomic<std::size_t>[n_blocks]);\n  for (std::uint32_t b = 0; b < n_blocks; ++b) next[b].store(0, std::memory_order_relaxed);\n  Barrier barrier(nt);\n  std::atomic<std::size_t> next_gene{0};\n  auto worker = [&]() {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n      for (;;) {\n        const std::size_t gc = next_gene.fetch_add(1, std::memory_order_relaxed);\n        if (gc >= n_gchunks) break;\n        rows.repack(gene_lo(gc), gene_lo(gc + 1));\n      }\n      barrier.wait();\n    }\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      for (;;) {\n        const std::size_t c = next[b].fetch_add(1, std::memory_order_relaxed);\n        if (c >= n_chunks) break;\n        rows(bounds[c], bounds[c + 1], b, s);\n      }\n      if (b + 1 < n_blocks) barrier.wait();  // one block: never waits, as rung 6\n    }\n  };\n  std::vector<std::thread> helpers;\n  helpers.reserve(nt - 1);\n  for (unsigned t = 1; t < nt; ++t) {\n    try {\n      helpers.emplace_back(worker);\n    } catch (...) {\n      break;\n    }\n  }\n  if (helpers.size() + 1 < nt) barrier.set_parties(static_cast<unsigned>(helpers.size() + 1));\n  worker();\n  for (std::thread& h : helpers) h.join();\n#endif\n}\n\n}  // namespace\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  const Mapped m = w.map(\"csr_packed.bin\");\n  if (m.bytes() < 128) fatal(\"csr_packed.bin is truncated; re-run prepare\");\n  const std::uint64_t* hdr = m.as<std::uint64_t>(0);\n  const std::uint64_t n_cells = hdr[0], n_genes = hdr[1], format = hdr[3];\n  if (n_cells != p.n_cells || n_genes != p.n_genes)\n    fatal(\"csr_packed.bin dimensions do not match the harness; re-run prepare\");\n  if (m.bytes() != hdr[10]) fatal(\"csr_packed.bin has the wrong size; re-run prepare\");\n  const std::uint32_t* row_nnz = m.as<std::uint32_t>(hdr[5]);\n\n  const Caches caches = query_caches();\n  const Blocks blocks = plan_blocks(n_genes, p.k, caches);\n  // Prefetch when the working set -- now one block of W -- outgrows L2.\n  const std::uint32_t dist = prefetch_distance(blocks.genes, p.k, caches);\n  std::unique_ptr<Cursor[]> cursors(blocks.count > 1 ? new Cursor[n_cells] : nullptr);\n\n  // Rung 9: the decoded rows hold gene * k, unless that would overflow a u32.\n  const bool pre = n_genes * p.k <= 0xFFFFFFFFull;\n  const std::uint32_t scale = pre ? static_cast<std::uint32_t>(p.k) : 1u;\n\n  // Rung 10: W repacked into (gene block x panel) tiles. P1_REPACK=0 keeps\n  // rung 9a's layout, which is the A/B for this rung.\n  bool tiled = format == 3 && p.k > 0 && blocks.genes > 0;\n  if (const char* e = std::getenv(\"P1_REPACK\")) {\n    if (std::strtoll(e, nullptr, 10) == 0) tiled = false;\n  }\n  std::unique_ptr<double[]> tiles(\n      tiled ? new double[std::uint64_t(blocks.count) * blocks.genes * p.k] : nullptr);\n\n  if (format == 3) {\n    const PackedRows rows{m.as<std::uint32_t>(hdr[6]), m.as<std::uint32_t>(hdr[7]),\n                          m.as<std::uint8_t>(hdr[8]),  m.as<std::uint8_t>(hdr[9]),\n                          p.W, y.data, p.k, dist, n_genes, blocks, cursors.get(), scale, pre,\n                          tiles.get(), tiled};\n    run_rows(rows, row_nnz, n_cells, p.k, hdr[4], blocks.count, tiled, n_genes);\n  } else if (format == 2) {\n    const WideRows rows{row_nnz, m.as<std::uint32_t>(hdr[6]), m.as<double>(hdr[7]),\n                        p.W, y.data, p.k, dist, n_genes, blocks, cursors.get()};\n    run_rows(rows, row_nnz, n_cells, p.k, 0, blocks.count, false, n_genes);\n  } else {\n    fatal(\"csr_packed.bin has an unknown format; re-run prepare\");\n  }\n}",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    },
    {
      "id": "30490ddd-5130-47b8-903e-a8ff24952823",
      "name": "ahsan-12",
      "is_baseline": false,
      "version": 3,
      "owner_handle": "kowshid",
      "updated_at": "2026-09-23T14:38:27.595261+00:00",
      "reason": "no run on your account yet",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- RUNG 12: unchanged from rung 5. UNTIMED.\n//\n// Rungs 6, 7 and 9 change only solution.cpp. Rung 7 blocks W by gene without\n// any new offsets in this file: blocks are visited in ascending gene order, so\n// each row's decoder simply stops at a block boundary and resumes there.\n//\n// ---- rung 5 notes, unchanged ----\n//\n// RUNG 5: a data structure designed from the data.\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// The ONE change from rung 4: how each non-zero is encoded. Rung 2 spent 4\n// bytes on every non-zero (u16 gene + u16 count). The data says we can do with\n// about 1.3, by using what Lecture 3 (slides 28-34) points out about it:\n//\n//   * 69.8% of the counts are exactly 1.          -> say \"1\" with a single bit\n//   * genes are sorted within a row, gaps are small -> store the gap, not the\n//     (median 10, 99.8% under 128)                   gene (delta encoding)\n//   * counts other than 1 are small (max 419)      -> one byte each, rarely more\n//\n// Encoding, per row, in gene order:\n//\n//   code stream, one byte per non-zero:\n//     bit 7     1 if the count is 1, else 0 (count is in the value stream)\n//     bits 0-6  gap to the previous gene, 1..127 (the first gap is gene + 1)\n//     0 in bits 0-6 is an escape: the gap follows as two bytes (u16)\n//   value stream, one byte per non-zero whose count is not 1:\n//     2..255    the count itself\n//     0         escape: the count follows as two bytes (u16)\n//\n// Unlike the lecture's grouped-by-value layout, this keeps every row in gene\n// order, so the kernel adds the terms of each Y element in exactly the same\n// order as before: the compression costs no exactness.\n//\n// Each row starts at a recorded byte offset in both streams, so any row can be\n// decoded on its own -- which is what lets rung 3's threads keep splitting the\n// rows freely. A per-row count of non-zeros is kept for the thread balancing.\n//\n// csr_packed.bin layout, little-endian:\n//\n//     u64 header[16]:\n//       [0] n_cells  [1] n_genes  [2] nnz  [3] format  [4] max_row_nnz\n//       [5] off_row_nnz  [6] off_a  [7] off_b  [8] off_c  [9] off_d\n//       [10] total_bytes\n//     format 3 (packed): a = u32 code_ptr[n_cells + 1]  (byte offset of each row)\n//                        b = u32 val_ptr[n_cells + 1]   (byte offset of each row)\n//                        c = u8 code stream, d = u8 value stream (+64 pad bytes)\n//     format 2 (wide fallback, when a gene index or count does not fit):\n//                        a = u32 gene index[nnz], b = f64 value[nnz]\n//     u32 row_nnz[n_cells + 1] at off_row_nnz: prefix count of non-zeros\n//\n// Every section starts on a 64-byte boundary.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cmath>\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <vector>\n\n#if defined(_OPENMP)\n#include <omp.h>\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kFormatWide = 2;\nconstexpr std::uint64_t kFormatPacked = 3;\n\n// Diagnostic only (unchanged from rung 3): mirrors threads_granted() in solve.\nvoid report_threads() {\n  const char* env = std::getenv(\"OMP_NUM_THREADS\");\n#if defined(_OPENMP)\n  std::fprintf(stderr, \"prepare: solve will be granted %d threads (OpenMP; OMP_NUM_THREADS=%s)\\n\",\n               omp_get_max_threads(), env ? env : \"unset\");\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n  unsigned n = std::thread::hardware_concurrency();\n  const char* source = \"hardware threads\";\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0) {\n    n = static_cast<unsigned>(CPU_COUNT(&set));\n    source = \"CPUs in the affinity mask\";\n  }\n#endif\n  if (env && std::strtol(env, nullptr, 10) > 0) {\n    n = static_cast<unsigned>(std::strtol(env, nullptr, 10));\n    source = \"OMP_NUM_THREADS\";\n  }\n  std::fprintf(stderr, \"prepare: solve will be granted %u threads (std::thread; from %s)\\n\", n,\n               source);\n#else\n  std::fprintf(stderr, \"prepare: solve will run serially (browser build, no threads)\\n\");\n#endif\n}\n\nstd::uint64_t pad64(std::uint64_t x) { return (x + 63) & ~std::uint64_t(63); }\n\nvoid pad_to(hpcbench::Writer& w, std::uint64_t& pos, std::uint64_t target) {\n  static const unsigned char zeros[64] = {};\n  while (pos < target) {\n    const std::uint64_t n = std::min<std::uint64_t>(64, target - pos);\n    w.write(zeros, static_cast<std::size_t>(n));\n    pos += n;\n  }\n}\n\nvoid put(hpcbench::Writer& w, std::uint64_t& pos, const void* data, std::uint64_t bytes) {\n  w.write(data, static_cast<std::size_t>(bytes));\n  pos += bytes;\n}\n\n}  // namespace\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  const std::uint64_t n_cells = in.n_cells;\n  const std::uint64_t n_genes = in.n_genes;\n\n  // Pass 1: can the packed encoding represent this input exactly?\n  // Gaps must fit u16 (gene index <= 65,534) and counts must be integers\n  // 1..65,535. Otherwise fall back to u32 index + f64 value.\n  bool packable = n_genes <= 65535;\n  std::uint64_t nnz = 0, max_row = 0;\n  std::vector<std::uint32_t> row_nnz(n_cells + 1, 0);\n  for (std::uint64_t i = 0; i < n_cells; ++i) {\n    const double* xi = in.X + i * n_genes;\n    std::uint64_t row = 0;\n    for (std::uint64_t g = 0; g < n_genes; ++g) {\n      const double x = xi[g];\n      if (x == 0.0) continue;\n      ++row;\n      if (!(x >= 1.0 && x <= 65535.0 && x == std::floor(x))) packable = false;\n    }\n    nnz += row;\n    if (nnz > UINT32_MAX) fatal(\"more than 2^32 non-zeros: u32 offsets overflow\");\n    max_row = std::max(max_row, row);\n    row_nnz[i + 1] = static_cast<std::uint32_t>(nnz);\n  }\n\n  std::uint64_t header[16] = {};\n  header[0] = n_cells;\n  header[1] = n_genes;\n  header[2] = nnz;\n  header[4] = max_row;\n  header[5] = 128;  // off_row_nnz\n  const std::uint64_t after_row_nnz = pad64(header[5] + 4 * (n_cells + 1));\n\n  Writer w = out.create(\"csr_packed.bin\");\n  std::uint64_t pos = 0;\n\n  if (packable) {\n    // Pass 2: encode.\n    std::vector<std::uint8_t> code, vals;\n    std::vector<std::uint32_t> code_ptr(n_cells + 1, 0), val_ptr(n_cells + 1, 0);\n    code.reserve(static_cast<std::size_t>(nnz + nnz / 64));\n    vals.reserve(static_cast<std::size_t>(nnz / 2));\n    std::uint64_t gap_escapes = 0, val_escapes = 0;\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      std::uint64_t prev = ~std::uint64_t(0);  // \"gene -1\": the first gap is gene + 1\n      for (std::uint64_t g = 0; g < n_genes; ++g) {\n        const double x = xi[g];\n        if (x == 0.0) continue;\n        const std::uint64_t gap = g - prev;  // 1..65,535\n        prev = g;\n        const std::uint32_t v = static_cast<std::uint32_t>(x);\n        const std::uint8_t one = (v == 1) ? 0x80 : 0x00;\n        if (gap < 128) {\n          code.push_back(static_cast<std::uint8_t>(one | gap));\n        } else {\n          code.push_back(one);  // gap field 0 = escape\n          code.push_back(static_cast<std::uint8_t>(gap & 0xFF));\n          code.push_back(static_cast<std::uint8_t>(gap >> 8));\n          ++gap_escapes;\n        }\n        if (v != 1) {\n          if (v < 256) {\n            vals.push_back(static_cast<std::uint8_t>(v));\n          } else {\n            vals.push_back(0);  // escape\n            vals.push_back(static_cast<std::uint8_t>(v & 0xFF));\n            vals.push_back(static_cast<std::uint8_t>(v >> 8));\n            ++val_escapes;\n          }\n        }\n      }\n      if (code.size() > UINT32_MAX || vals.size() > UINT32_MAX)\n        fatal(\"packed streams exceed 4 GB\");\n      code_ptr[i + 1] = static_cast<std::uint32_t>(code.size());\n      val_ptr[i + 1] = static_cast<std::uint32_t>(vals.size());\n    }\n\n    header[3] = kFormatPacked;\n    header[6] = after_row_nnz;                               // code_ptr\n    header[7] = pad64(header[6] + 4 * (n_cells + 1));        // val_ptr\n    header[8] = pad64(header[7] + 4 * (n_cells + 1));        // code stream\n    header[9] = pad64(header[8] + code.size());              // value stream\n    header[10] = header[9] + vals.size() + 64;               // + pad: the decoder\n                                                             // may peek one byte ahead\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, code_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[7]);\n    put(w, pos, val_ptr.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[8]);\n    put(w, pos, code.data(), code.size());\n    pad_to(w, pos, header[9]);\n    put(w, pos, vals.data(), vals.size());\n    pad_to(w, pos, header[10]);\n    w.close();\n\n    std::fprintf(stderr,\n                 \"prepare: packed CSR, %llu nonzeros, %.2f MB (%.2f bytes/nnz); \"\n                 \"code %.2f MB, values %.2f MB, %llu gap and %llu value escapes\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6,\n                 double(header[10]) / double(nnz), double(code.size()) / 1e6,\n                 double(vals.size()) / 1e6, (unsigned long long)gap_escapes,\n                 (unsigned long long)val_escapes);\n  } else {\n    std::vector<std::uint32_t> col;\n    std::vector<double> val;\n    col.reserve(static_cast<std::size_t>(nnz));\n    val.reserve(static_cast<std::size_t>(nnz));\n    for (std::uint64_t i = 0; i < n_cells; ++i) {\n      const double* xi = in.X + i * n_genes;\n      for (std::uint64_t g = 0; g < n_genes; ++g)\n        if (xi[g] != 0.0) {\n          col.push_back(static_cast<std::uint32_t>(g));\n          val.push_back(xi[g]);\n        }\n    }\n    header[3] = kFormatWide;\n    header[6] = after_row_nnz;                      // u32 gene index\n    header[7] = pad64(header[6] + 4 * nnz);         // f64 value\n    header[10] = header[7] + 8 * nnz;\n    put(w, pos, header, sizeof(header));\n    pad_to(w, pos, header[5]);\n    put(w, pos, row_nnz.data(), 4 * (n_cells + 1));\n    pad_to(w, pos, header[6]);\n    put(w, pos, col.data(), 4 * nnz);\n    pad_to(w, pos, header[7]);\n    put(w, pos, val.data(), 8 * nnz);\n    w.close();\n    std::fprintf(stderr, \"prepare: wide fallback CSR (u32 + f64), %llu nonzeros, %.2f MB\\n\",\n                 (unsigned long long)nnz, double(header[10]) / 1e6);\n  }\n  report_threads();\n}",
        "src/solution.cpp": "// src/solution.cpp -- RUNG 12: block W sooner, and smaller.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// Stacked on rung 10 (rung 11's runs measured slower and are left out). The ONE\n// change is rung 7's blocking rule:\n//\n//     rung 7:   no blocking while W fits in the smallest L2; then quarter-L2 blocks\n//     rung 12:  no blocking while W fits in HALF that L2; then eighth-L2 blocks\n//\n// Two measurements point this way. On a 1-core Xeon (2 MB L2) the block-size\n// sweep at k = 256 read 1 MB: 2.13x, 512 KB: 2.27x, 256 KB: 2.26x over no\n// blocking -- flat from a quarter of L2 downwards, so the quarter was the top\n// of the good range, not the middle of it. On the M4, forcing 4 MB blocks\n// (P1_W_BLOCK_BYTES=4194304) cost 1.25x at k = 64 and 128 and 2.78x at k = 256\n// against the 1 MB default, so that machine wants blocks below its 4 MB\n// efficiency-cluster L2, not at it.\n//\n// On an M4 this blocks from k = 20 up (it was k = 39), with 512 KB blocks:\n// k = 32: 7 blocks, k = 64: 14, k = 128: 27, k = 256: 54.\n//\n// The cost is more blocks: one barrier each, and one reload and restore of each\n// row's Y panel each. Whether that is worth the smaller working set is exactly\n// what this rung measures.\n//\n// Correctness: blocking order and arithmetic are rung 7's, unchanged.\n// Bit-for-bit the baseline's.\n//\n// Knobs: P1_W_BLOCK_BYTES overrides both the size and the threshold, so\n// P1_W_BLOCK_BYTES=1048576 reproduces rung 10 on an M4; P1_REPACK and\n// P1_PREFETCH_DIST as before.\n#include \"task.hpp\"\n\n#include <algorithm>\n#include <cstdint>\n#include <cstdlib>\n#include <cstring>\n#include <memory>\n#include <vector>\n\n#if defined(__APPLE__)\n#include <sys/sysctl.h>\n#elif defined(__linux__)\n#include <unistd.h>\n#endif\n\n#if defined(_OPENMP)\n#include <omp.h>\n#define P1_OPENMP 1\n#elif !defined(__wasm__) && !defined(__EMSCRIPTEN__)\n#define P1_STD_THREAD 1\n#include <atomic>\n#include <chrono>\n#include <condition_variable>\n#include <mutex>\n#include <thread>\n#if defined(__linux__)\n#include <sched.h>\n#endif\n#endif\n\n#ifndef P1_PANEL  // override with -DP1_PANEL=... (up to 64) in CXXFLAGS to experiment\n#define P1_PANEL 32\n#endif\n\nnamespace {\n\nconstexpr std::uint64_t kMinWorkPerThread = std::uint64_t(1) << 19;  // multiply-adds\nconstexpr std::uint64_t kChunksPerThread = 32;\nconstexpr std::uint64_t kMaxBlocks = 64;  // each block costs a barrier\n\n// ---- rung 4's kernel, unchanged -------------------------------------------\n\ntypedef double v4d __attribute__((vector_size(32)));  // four doubles\n\nconstexpr int kPanel = P1_PANEL;\n\n// acc + x * w, rounded exactly once, the way the baseline's `y += x * w` is.\n//\n// Where the target has a hardware fused multiply-add (__FP_FAST_FMA: every\n// arm64, x86 with FMA), the compiler fuses the baseline's statement into one\n// instruction. It does not reliably fuse ours: GCC's tuning for recent Intel\n// cores (\"avoid FMA chains\") splits a loop-carried accumulation into a multiply\n// and an add when only one or two accumulator vectors carry it -- exactly our\n// narrow panels -- which rounds twice. So the fusion is spelled out here.\n// Without hardware FMA the baseline rounds twice, and so does this.\ninline double madd(double x, double w, double acc) {\n#if defined(__FP_FAST_FMA)\n  return __builtin_fma(x, w, acc);\n#else\n  return x * w + acc;\n#endif\n}\n\ninline v4d vmadd(v4d x, v4d w, v4d acc) {\n#if defined(__FP_FAST_FMA)\n  // Four scalar fused multiply-adds; every compiler tested turns them into one\n  // vector FMA (vfmadd231pd on x86, two fmla on arm64).\n  return v4d{__builtin_fma(x[0], w[0], acc[0]), __builtin_fma(x[1], w[1], acc[1]),\n             __builtin_fma(x[2], w[2], acc[2]), __builtin_fma(x[3], w[3], acc[3])};\n#else\n  return x * w + acc;\n#endif\n}\n\n// One row, one panel of P output columns:  yc[0:P] = sum over the row's\n// non-zeros of x * Wc[g*k + 0:P], accumulated in registers. With `accumulate`\n// the sums start from what yc already holds -- the partial sums left there by\n// the previous gene blocks -- instead of from zero.\n//\n// noinline: each width is compiled as its own function. Measured necessity,\n// not style: GCC 13 inlined all 32 prefetching widths into the switch below and\n// the result ran k = 16 at 18.1 ms instead of 10.1 ms (k = 13: 15.1 vs 8.5) at\n// every prefetch distance from 1 to 64 -- a code-generation effect, not a\n// memory one. The same attribute on rungs 4 and 5 measured no difference.\n// How col[] addresses W:\n//   TILED  gene index within its block; the row is at col[q] * P in the tile\n//   PRE    gene * k already (rung 9a), so the kernel only adds\n//   else   plain gene index, multiplied by k here (rung 7, and the CSR paths)\ntemplate <int P, bool PF, bool TILED, bool PRE, class Idx, class Val>\n__attribute__((noinline)) void row_panel(const Idx* col, const Val* val, std::uint32_t n,\n                                         const double* Wc, std::uint64_t k, double* yc,\n                                         std::uint32_t dist, bool accumulate) {\n  constexpr int V = P / 4;  // full 4-wide vectors\n  constexpr int R = P % 4;  // 1-3 leftover columns, scalar\n  v4d acc[V > 0 ? V : 1];\n  double tail[R > 0 ? R : 1];\n  if (accumulate) {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) std::memcpy(&acc[v], yc + 4 * v, sizeof(v4d));\n    for (int r = 0; r < R; ++r) tail[r] = yc[4 * V + r];\n  } else {\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) acc[v] = v4d{0.0, 0.0, 0.0, 0.0};\n    for (int r = 0; r < R; ++r) tail[r] = 0.0;\n  }\n\n  for (std::uint32_t q = 0; q < n; ++q) {\n    if (PF && q + dist < n) {\n      // The W segment this row will need `dist` non-zeros from now: one\n      // prefetch per 64 bytes (a cache line on x86, half of one on Apple).\n      const std::uint64_t ahead_off =\n          TILED ? static_cast<std::uint64_t>(col[q + dist]) * P\n                : (PRE ? static_cast<std::uint64_t>(col[q + dist])\n                       : static_cast<std::uint64_t>(col[q + dist]) * k);\n      const char* ahead = reinterpret_cast<const char*>(Wc + ahead_off);\n#pragma GCC unroll 16\n      for (int off = 0; off < P * 8; off += 64) __builtin_prefetch(ahead + off, 0, 3);\n    }\n    const double x = static_cast<double>(val[q]);\n    const v4d xv = v4d{x, x, x, x};\n    const double* w = Wc + (TILED ? static_cast<std::uint64_t>(col[q]) * P\n                                  : (PRE ? static_cast<std::uint64_t>(col[q])\n                                         : static_cast<std::uint64_t>(col[q]) * k));\n    // Fully unrolled: GCC stops at 8 vectors on its own and would otherwise\n    // keep a 64-wide panel's accumulators on the stack.\n#pragma GCC unroll 16\n    for (int v = 0; v < V; ++v) {\n      v4d wv;\n      std::memcpy(&wv, w + 4 * v, sizeof(wv));  // unaligned vector load\n      acc[v] = vmadd(xv, wv, acc[v]);\n    }\n    for (int r = 0; r < R; ++r) tail[r] = madd(x, w[4 * V + r], tail[r]);\n  }\n\n#pragma GCC unroll 16\n  for (int v = 0; v < V; ++v) std::memcpy(yc + 4 * v, &acc[v], sizeof(v4d));\n  for (int r = 0; r < R; ++r) yc[4 * V + r] = tail[r];\n}\n\n// The switch: a run-time width selects a compile-time kernel.\ntemplate <bool PF, bool TILED, bool PRE, class Idx, class Val>\nvoid row_panel_any(int width, const Idx* col, const Val* val, std::uint32_t n,\n                   const double* Wc, std::uint64_t k, double* yc, std::uint32_t dist,\n                   bool accumulate) {\n  switch (width) {\n#define P1_CASE(P)                                                  \\\n  case P:                                                           \\\n    row_panel<P, PF, TILED, PRE>(col, val, n, Wc, k, yc, dist, accumulate);     \\\n    return;\n    P1_CASE(1)\n    P1_CASE(2)\n    P1_CASE(3)\n    P1_CASE(4)\n    P1_CASE(5)\n    P1_CASE(6)\n    P1_CASE(7)\n    P1_CASE(8)\n    P1_CASE(9)\n    P1_CASE(10)\n    P1_CASE(11)\n    P1_CASE(12)\n    P1_CASE(13)\n    P1_CASE(14)\n    P1_CASE(15)\n    P1_CASE(16)\n    P1_CASE(17)\n    P1_CASE(18)\n    P1_CASE(19)\n    P1_CASE(20)\n    P1_CASE(21)\n    P1_CASE(22)\n    P1_CASE(23)\n    P1_CASE(24)\n    P1_CASE(25)\n    P1_CASE(26)\n    P1_CASE(27)\n    P1_CASE(28)\n    P1_CASE(29)\n    P1_CASE(30)\n    P1_CASE(31)\n    P1_CASE(32)\n#if P1_PANEL > 32\n    P1_CASE(33)\n    P1_CASE(34)\n    P1_CASE(35)\n    P1_CASE(36)\n    P1_CASE(37)\n    P1_CASE(38)\n    P1_CASE(39)\n    P1_CASE(40)\n    P1_CASE(41)\n    P1_CASE(42)\n    P1_CASE(43)\n    P1_CASE(44)\n    P1_CASE(45)\n    P1_CASE(46)\n    P1_CASE(47)\n    P1_CASE(48)\n    P1_CASE(49)\n    P1_CASE(50)\n    P1_CASE(51)\n    P1_CASE(52)\n    P1_CASE(53)\n    P1_CASE(54)\n    P1_CASE(55)\n    P1_CASE(56)\n    P1_CASE(57)\n    P1_CASE(58)\n    P1_CASE(59)\n    P1_CASE(60)\n    P1_CASE(61)\n    P1_CASE(62)\n    P1_CASE(63)\n    P1_CASE(64)\n#endif\n#undef P1_CASE\n    default:\n      hpcbench::fatal(\"panel width out of range\");\n  }\n}\n\n// ---- rung 5: the packed representation -------------------------------------\n\n// One row's worth of decoded non-zeros, owned by one thread.\nstruct Scratch {\n  std::unique_ptr<std::uint32_t[]> genes;  // rung 9: gene * k when it fits in u32\n  std::unique_ptr<double[]> counts;\n  explicit Scratch(std::uint64_t cap)\n      : genes(new std::uint32_t[cap > 0 ? cap : 1]), counts(new double[cap > 0 ? cap : 1]) {}\n};\n\n// ---- rung 7: gene blocks --------------------------------------------------\n\n// The genes [b * genes, min((b + 1) * genes, n_genes)) form block b of W.\nstruct Blocks {\n  std::uint32_t genes;  // genes per block (the last one may be smaller)\n  std::uint32_t count;  // number of blocks; 1 = W fits, no blocking\n};\n\n// Where row i's decoding stopped at the end of the previous block.\nstruct Cursor {\n  std::uint32_t code;  // next code byte (packed) / next non-zero (wide)\n  std::uint32_t val;   // next value byte (packed only)\n  std::uint32_t gene;  // last gene decoded (packed only; 0xFFFFFFFF = none yet)\n};\n\nstruct PackedRows {\n  const std::uint32_t* code_ptr;  // byte offset of each row in the code stream\n  const std::uint32_t* val_ptr;   // byte offset of each row in the value stream\n  const std::uint8_t* code;\n  const std::uint8_t* vals;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;      // prefetch distance in non-zeros; 0 = off\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;         // one per row; used only when blocks.count > 1\n  std::uint32_t scale;     // rung 9: k when gene * k fits in u32, else 1\n  bool pre;                // rung 9: true when the scratch holds gene * k\n  double* tiles;           // rung 10: W in (gene block x panel) tiles, or null\n  bool tiled;              // rung 10: true when the kernel reads the tiles\n\n  // Rung 10: copy this slice of W's genes into the tiles. Threads split the\n  // genes between them and meet at a barrier before any multiplying starts.\n  void repack(std::uint64_t g0, std::uint64_t g1) const {\n    const std::uint64_t G = blocks.genes;\n    for (std::uint64_t g = g0; g < g1; ++g) {\n      const std::uint64_t b = g / G, lg = g - b * G;\n      const double* src = W + g * k;\n      double* dst = tiles + b * G * k;\n      for (std::uint64_t c0 = 0; c0 < k; c0 += kPanel) {\n        const std::uint64_t width = std::min<std::uint64_t>(kPanel, k - c0);\n        std::memcpy(dst + G * c0 + lg * width, src + c0, width * sizeof(double));\n      }\n    }\n  }\n\n  // Decode row i into s.genes / s.counts; returns its number of non-zeros.\n  template <bool TILED>\n  std::uint32_t decode(std::uint64_t i, std::uint32_t origin, Scratch& s) const {\n    const std::uint8_t* cp = code + code_ptr[i];\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + val_ptr[i];\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = 0xFFFFFFFFu;  // \"gene -1\": the first gap is gene + 1\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = *cp++;\n      std::uint32_t gap = b & 0x7Fu;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[0]) | (std::uint32_t(cp[1]) << 8);\n        cp += 2;\n      }\n      g += gap;\n      // Count: 1 if bit 7 is set, else the next byte of the value stream.\n      // Written as arithmetic on purpose: GCC compiles `one ? 1 : peek` into a\n      // branch, and a 70/30 coin flip per non-zero is a branch no predictor can\n      // learn (measured: ~9 cycles per non-zero instead of ~3).\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;   // 1 if the count is in the value stream\n      const std::uint32_t peek = *vp;                 // always in bounds: stream is padded\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);  // peek, or 1\n      vp += uses_val;                                 // consume a value byte only if used\n      if (v == 0) {                                   // escape: two-byte count\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    return n;\n  }\n\n  // Rung 7: decode only the non-zeros of row i whose gene is below gene_end,\n  // starting where cur says the previous block stopped; leave cur at the\n  // first non-zero of a later block. Same decoder as above, plus a peek at the\n  // next gene before consuming it.\n  template <bool TILED>\n  std::uint32_t decode_until(std::uint64_t i, std::uint32_t gene_end, std::uint32_t origin,\n                             Cursor& cur, Scratch& s) const {\n    const std::uint8_t* cp = code + cur.code;\n    const std::uint8_t* const end = code + code_ptr[i + 1];\n    const std::uint8_t* vp = vals + cur.val;\n    std::uint32_t* genes = s.genes.get();\n    double* counts = s.counts.get();\n    std::uint32_t g = cur.gene;\n    std::uint32_t n = 0;\n    while (cp < end) {\n      const std::uint32_t b = cp[0];\n      std::uint32_t gap = b & 0x7Fu, len = 1;\n      if (gap == 0) {  // escape: two-byte gap\n        gap = std::uint32_t(cp[1]) | (std::uint32_t(cp[2]) << 8);\n        len = 3;\n      }\n      if (g + gap >= gene_end) break;  // belongs to a later block: leave it for then\n      cp += len;\n      g += gap;\n      const std::uint32_t uses_val = (b >> 7) ^ 1u;\n      const std::uint32_t peek = *vp;\n      std::uint32_t v = (peek & (0u - uses_val)) | (uses_val ^ 1u);\n      vp += uses_val;\n      if (v == 0) {\n        v = std::uint32_t(vp[0]) | (std::uint32_t(vp[1]) << 8);\n        vp += 2;\n      }\n      genes[n] = TILED ? g - origin : g * scale;\n      counts[n] = static_cast<double>(v);\n      ++n;\n    }\n    cur = Cursor{static_cast<std::uint32_t>(cp - code), static_cast<std::uint32_t>(vp - vals), g};\n    return n;\n  }\n\n  // Rows [r0, r1), gene block b.\n  template <bool PF, bool TILED, bool PRE>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint32_t gene_end = static_cast<std::uint32_t>(\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes));\n    const bool accumulate = b > 0;  // later blocks add to the partial sums in Y\n    const std::uint32_t origin = static_cast<std::uint32_t>(std::uint64_t(b) * blocks.genes);\n    // Rung 10: the block's tiles start here; panel c0 of it is G * c0 further on.\n    const double* const wb = TILED ? tiles + std::uint64_t(b) * blocks.genes * k : W;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t n;\n      if (blocks.count == 1) {\n        n = decode<TILED>(i, origin, s);  // W fits: the whole row at once\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur = Cursor{code_ptr[i], val_ptr[i], 0xFFFFFFFFu};\n        n = decode_until<TILED>(i, gene_end, origin, cur, s);\n        // Nothing of this row in block b: its partial sums in Y are already\n        // right. (Block 0 still writes every row, so Y never relies on the\n        // harness having zero-filled it.)\n        if (n == 0 && accumulate) continue;\n      }\n      const std::uint32_t* genes = s.genes.get();\n      const double* counts = s.counts.get();\n      double* yi = Y + i * k;\n      const std::uint64_t stride = TILED ? blocks.genes : 1;  // panel c0 starts G * c0 in\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, TILED, PRE>(genes, counts, n, wb + stride * c0, k, yi + c0, dist,\n                                          accumulate);\n      if (rem)\n        row_panel_any<PF, TILED, PRE>(rem, genes, counts, n, wb + stride * full, k, yi + full,\n                                      dist, accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch& s) const {\n    if (dist > 0) {\n      if (tiled) rows<true, true, false>(r0, r1, b, s);\n      else if (pre) rows<true, false, true>(r0, r1, b, s);\n      else rows<true, false, false>(r0, r1, b, s);\n    } else {\n      if (tiled) rows<false, true, false>(r0, r1, b, s);\n      else if (pre) rows<false, false, true>(r0, r1, b, s);\n      else rows<false, false, false>(r0, r1, b, s);\n    }\n  }\n};\n\n// The wide fallback prepare writes when the packed encoding cannot represent\n// the input exactly: plain u32 / f64 CSR, handled exactly as rung 4 did.\nstruct WideRows {\n  const std::uint32_t* row_nnz;\n  const std::uint32_t* col;\n  const double* val;\n  const double* W;\n  double* Y;\n  std::uint64_t k;\n  std::uint32_t dist;\n  std::uint64_t n_genes;\n  Blocks blocks;\n  Cursor* cursors;\n\n  void repack(std::uint64_t, std::uint64_t) const {}  // rung 10: plain CSR keeps W as given\n\n  template <bool PF>\n  void rows(std::uint64_t r0, std::uint64_t r1, std::uint32_t b) const {\n    const std::uint64_t full = k - k % kPanel;\n    const int rem = static_cast<int>(k % kPanel);\n    const std::uint64_t gene_end =\n        std::min<std::uint64_t>(n_genes, std::uint64_t(b + 1) * blocks.genes);\n    const bool accumulate = b > 0;\n    for (std::uint64_t i = r0; i < r1; ++i) {\n      std::uint32_t q0, n;\n      if (blocks.count == 1) {\n        q0 = row_nnz[i];\n        n = row_nnz[i + 1] - q0;\n      } else {\n        Cursor& cur = cursors[i];\n        if (b == 0) cur.code = row_nnz[i];\n        q0 = cur.code;\n        std::uint32_t q = q0;\n        while (q < row_nnz[i + 1] && col[q] < gene_end) ++q;\n        n = q - q0;\n        cur.code = q;\n        if (n == 0 && accumulate) continue;\n      }\n      double* yi = Y + i * k;\n      for (std::uint64_t c0 = 0; c0 < full; c0 += kPanel)\n        row_panel<kPanel, PF, false, false>(col + q0, val + q0, n, W + c0, k, yi + c0, dist,\n                                            accumulate);\n      if (rem)\n        row_panel_any<PF, false, false>(rem, col + q0, val + q0, n, W + full, k, yi + full, dist,\n                                        accumulate);\n    }\n  }\n  void operator()(std::uint64_t r0, std::uint64_t r1, std::uint32_t b, Scratch&) const {\n    if (dist > 0) rows<true>(r0, r1, b);\n    else rows<false>(r0, r1, b);\n  }\n};\n\n// ---- rung 6: cache sizes and the prefetch policy ---------------------------\n\nstruct Caches {\n  std::uint64_t l1d, l2, llc;  // bytes; llc = the largest cache shared by a cluster/socket\n  std::uint64_t l2_min;        // rung 7: the smallest L2 any thread may run on\n};\n\n#if defined(__APPLE__)\nstd::uint64_t sysctl_u64(const char* name) {\n  std::uint64_t v = 0;\n  std::size_t len = sizeof(v);\n  if (sysctlbyname(name, &v, &len, nullptr, 0) != 0) return 0;\n  return len == 4 ? static_cast<std::uint32_t>(v) : v;\n}\n#endif\n\n// This machine's cache sizes. On Apple silicon the performance cluster's\n// values (perflevel0: 128 KB L1d, 16 MB L2 on an M4) -- the L2 is the last\n// level each cluster owns. On Linux, glibc's sysconf. Defaults where unknown.\nCaches query_caches() {\n  Caches c{0, 0, 0, 0};\n#if defined(__APPLE__)\n  c.l1d = sysctl_u64(\"hw.perflevel0.l1dcachesize\");\n  c.l2 = sysctl_u64(\"hw.perflevel0.l2cachesize\");\n  if (c.l1d == 0) c.l1d = sysctl_u64(\"hw.l1dcachesize\");\n  if (c.l2 == 0) c.l2 = sysctl_u64(\"hw.l2cachesize\");\n  c.llc = c.l2;\n  c.l2_min = sysctl_u64(\"hw.perflevel1.l2cachesize\");  // efficiency cluster, if any\n#elif defined(__linux__) && defined(_SC_LEVEL1_DCACHE_SIZE)\n  const long l1 = sysconf(_SC_LEVEL1_DCACHE_SIZE);\n  const long l2 = sysconf(_SC_LEVEL2_CACHE_SIZE);\n  const long l3 = sysconf(_SC_LEVEL3_CACHE_SIZE);\n  c.l1d = l1 > 0 ? static_cast<std::uint64_t>(l1) : 0;\n  c.l2 = l2 > 0 ? static_cast<std::uint64_t>(l2) : 0;\n  c.llc = l3 > 0 ? static_cast<std::uint64_t>(l3) : c.l2;\n#endif\n  if (c.l1d == 0) c.l1d = 32 << 10;  // unknown: a typical L1d\n  if (c.l2 == 0) c.l2 = 1 << 20;     // unknown: a typical per-core L2\n  if (c.llc == 0) c.llc = 8 << 20;   // unknown: a typical shared L3\n  if (c.llc < c.l2) c.llc = c.l2;\n  if (c.l2_min == 0 || c.l2_min > c.l2) c.l2_min = c.l2;\n  return c;\n}\n\n// Prefetch distance for this run: 0 (off) when W fits in half of L2, else\n// P1_PREFETCH_DIST or 8 non-zeros ahead.\nstd::uint32_t prefetch_distance(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  std::uint32_t dist = 8;\n  if (const char* s = std::getenv(\"P1_PREFETCH_DIST\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v >= 0) dist = static_cast<std::uint32_t>(v);\n  }\n  const std::uint64_t w_bytes = n_genes * k * sizeof(double);\n  return w_bytes > c.l2 / 2 ? dist : 0;\n}\n\n// Rung 12: how to cut W into gene blocks. No blocking while W fits in half of\n// the smallest L2; otherwise blocks of an eighth of that L2. P1_W_BLOCK_BYTES sets\n// the block size and blocks whenever W is larger. Blocks are made equal in\n// size, and there are never more than kMaxBlocks of them.\nBlocks plan_blocks(std::uint64_t n_genes, std::uint64_t k, const Caches& c) {\n  const std::uint64_t row_bytes = k * sizeof(double);\n  const std::uint64_t w_bytes = n_genes * row_bytes;\n  std::uint64_t budget = c.l2_min / 8, fits = c.l2_min / 2;  // rung 12: sooner, smaller\n  if (const char* s = std::getenv(\"P1_W_BLOCK_BYTES\")) {\n    const long long v = std::strtoll(s, nullptr, 10);\n    if (v > 0) budget = fits = static_cast<std::uint64_t>(v);\n  }\n  if (n_genes == 0 || row_bytes == 0 || w_bytes <= fits)\n    return Blocks{static_cast<std::uint32_t>(n_genes), 1};\n  const std::uint64_t max_genes = std::max<std::uint64_t>(1, budget / row_bytes);\n  std::uint64_t count = std::min<std::uint64_t>((n_genes + max_genes - 1) / max_genes, kMaxBlocks);\n  const std::uint64_t genes = (n_genes + count - 1) / count;\n  count = (n_genes + genes - 1) / genes;\n  return Blocks{static_cast<std::uint32_t>(genes), static_cast<std::uint32_t>(count)};\n}\n\n// ---- threading: rung 3's, with one Scratch per thread and, new in rung 7, a\n// barrier between gene blocks ----------------------------------------------\n\nunsigned threads_granted() {\n#if defined(P1_OPENMP)\n  return static_cast<unsigned>(std::max(1, omp_get_max_threads()));\n#elif defined(P1_STD_THREAD)\n  if (const char* s = std::getenv(\"OMP_NUM_THREADS\")) {\n    const long v = std::strtol(s, nullptr, 10);\n    if (v > 0) return static_cast<unsigned>(v);\n  }\n#if defined(__linux__)\n  cpu_set_t set;\n  CPU_ZERO(&set);\n  if (sched_getaffinity(0, sizeof(set), &set) == 0 && CPU_COUNT(&set) > 0)\n    return static_cast<unsigned>(CPU_COUNT(&set));\n#endif\n  const unsigned hc = std::thread::hardware_concurrency();\n  return hc > 0 ? hc : 1;\n#else\n  return 1;\n#endif\n}\n\n#if defined(P1_STD_THREAD)\n// A reusable barrier (C++17 has none): each thread waits here until all have\n// finished the current gene block. The party count starts at the number of\n// threads planned and is lowered if creating one of them fails.\n//\n// A waiter first spins for up to 50 us: the other threads are usually only\n// microseconds behind, and a sleep plus wake-up through the kernel costs tens\n// of microseconds on macOS -- with 27 blocks at k = 256, that adds up. After\n// 50 us it sleeps, so an oversubscribed machine does not burn cores.\nclass Barrier {\n public:\n  explicit Barrier(unsigned parties) : parties_(parties) {}\n  void set_parties(unsigned parties) {\n    std::lock_guard<std::mutex> lock(m_);\n    parties_ = parties;\n    if (arrived_ > 0 && arrived_ >= parties_) release();\n  }\n  void wait() {\n    std::unique_lock<std::mutex> lock(m_);\n    const unsigned gen = generation_.load(std::memory_order_relaxed);\n    if (++arrived_ >= parties_) {\n      release();\n      return;\n    }\n    lock.unlock();\n    const auto until = std::chrono::steady_clock::now() + std::chrono::microseconds(50);\n    while (std::chrono::steady_clock::now() < until)\n      if (generation_.load(std::memory_order_acquire) != gen) return;\n    lock.lock();\n    cv_.wait(lock, [&] { return generation_.load(std::memory_order_relaxed) != gen; });\n  }\n\n private:\n  void release() {  // called with m_ held\n    arrived_ = 0;\n    generation_.fetch_add(1, std::memory_order_release);\n    cv_.notify_all();\n  }\n  std::mutex m_;\n  std::condition_variable cv_;\n  unsigned parties_, arrived_ = 0;\n  std::atomic<unsigned> generation_{0};\n};\n#endif\n\ntemplate <class Rows>\nvoid run_rows(const Rows& rows, const std::uint32_t* row_nnz, std::uint64_t n_cells,\n              std::uint64_t k, std::uint64_t scratch_cap, std::uint32_t n_blocks,\n              bool do_repack, std::uint64_t n_genes) {\n  const std::uint64_t nnz = row_nnz[n_cells];\n  const std::uint64_t floor_threads = std::max<std::uint64_t>(1, nnz * k / kMinWorkPerThread);\n  const unsigned nt =\n      static_cast<unsigned>(std::min<std::uint64_t>(threads_granted(), floor_threads));\n  if (nt <= 1) {\n    Scratch s(scratch_cap);\n    if (do_repack) rows.repack(0, n_genes);  // rung 10\n    for (std::uint32_t b = 0; b < n_blocks; ++b) rows(0, n_cells, b, s);\n    return;\n  }\n\n  std::vector<std::uint32_t> bounds;\n  bounds.reserve(static_cast<std::size_t>(nt * kChunksPerThread + 2));\n  bounds.push_back(0);\n  const std::uint64_t target = std::max<std::uint64_t>(1, nnz / (nt * kChunksPerThread));\n  std::uint64_t chunk_start = row_nnz[0];\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    if (row_nnz[i + 1] - chunk_start >= target) {\n      bounds.push_back(static_cast<std::uint32_t>(i + 1));\n      chunk_start = row_nnz[i + 1];\n    }\n  if (bounds.back() != n_cells) bounds.push_back(static_cast<std::uint32_t>(n_cells));\n  const std::size_t n_chunks = bounds.size() - 1;\n\n  // Rung 10: the threads build W's tiles first and meet at a barrier; nothing\n  // reads a tile before every thread has finished writing its genes.\n  const std::size_t n_gchunks = static_cast<std::size_t>(nt) * 4;\n  const auto gene_lo = [&](std::size_t j) { return n_genes * j / n_gchunks; };\n\n#if defined(P1_OPENMP)\n  const long nc = static_cast<long>(n_chunks);\n#pragma omp parallel num_threads(nt)\n  {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n#pragma omp for schedule(static)\n      for (long gg = 0; gg < static_cast<long>(n_gchunks); ++gg)\n        rows.repack(gene_lo(static_cast<std::size_t>(gg)),\n                    gene_lo(static_cast<std::size_t>(gg) + 1));\n    }  // implicit barrier\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      // The implicit barrier at the end of this loop keeps all threads on the\n      // same block of W: nobody starts block b + 1 before every row has done b.\n#pragma omp for schedule(dynamic, 1)\n      for (long c = 0; c < nc; ++c) rows(bounds[c], bounds[c + 1], b, s);\n    }\n  }\n#elif defined(P1_STD_THREAD)\n  // One chunk counter per block, so a block's chunks are handed out afresh.\n  std::unique_ptr<std::atomic<std::size_t>[]> next(new std::atomic<std::size_t>[n_blocks]);\n  for (std::uint32_t b = 0; b < n_blocks; ++b) next[b].store(0, std::memory_order_relaxed);\n  Barrier barrier(nt);\n  std::atomic<std::size_t> next_gene{0};\n  auto worker = [&]() {\n    Scratch s(scratch_cap);  // one per thread, allocated once\n    if (do_repack) {\n      for (;;) {\n        const std::size_t gc = next_gene.fetch_add(1, std::memory_order_relaxed);\n        if (gc >= n_gchunks) break;\n        rows.repack(gene_lo(gc), gene_lo(gc + 1));\n      }\n      barrier.wait();\n    }\n    for (std::uint32_t b = 0; b < n_blocks; ++b) {\n      for (;;) {\n        const std::size_t c = next[b].fetch_add(1, std::memory_order_relaxed);\n        if (c >= n_chunks) break;\n        rows(bounds[c], bounds[c + 1], b, s);\n      }\n      if (b + 1 < n_blocks) barrier.wait();  // one block: never waits, as rung 6\n    }\n  };\n  std::vector<std::thread> helpers;\n  helpers.reserve(nt - 1);\n  for (unsigned t = 1; t < nt; ++t) {\n    try {\n      helpers.emplace_back(worker);\n    } catch (...) {\n      break;\n    }\n  }\n  if (helpers.size() + 1 < nt) barrier.set_parties(static_cast<unsigned>(helpers.size() + 1));\n  worker();\n  for (std::thread& h : helpers) h.join();\n#endif\n}\n\n}  // namespace\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  const Mapped m = w.map(\"csr_packed.bin\");\n  if (m.bytes() < 128) fatal(\"csr_packed.bin is truncated; re-run prepare\");\n  const std::uint64_t* hdr = m.as<std::uint64_t>(0);\n  const std::uint64_t n_cells = hdr[0], n_genes = hdr[1], format = hdr[3];\n  if (n_cells != p.n_cells || n_genes != p.n_genes)\n    fatal(\"csr_packed.bin dimensions do not match the harness; re-run prepare\");\n  if (m.bytes() != hdr[10]) fatal(\"csr_packed.bin has the wrong size; re-run prepare\");\n  const std::uint32_t* row_nnz = m.as<std::uint32_t>(hdr[5]);\n\n  const Caches caches = query_caches();\n  const Blocks blocks = plan_blocks(n_genes, p.k, caches);\n  // Prefetch when the working set -- now one block of W -- outgrows L2.\n  const std::uint32_t dist = prefetch_distance(blocks.genes, p.k, caches);\n  std::unique_ptr<Cursor[]> cursors(blocks.count > 1 ? new Cursor[n_cells] : nullptr);\n\n  // Rung 9: the decoded rows hold gene * k, unless that would overflow a u32.\n  const bool pre = n_genes * p.k <= 0xFFFFFFFFull;\n  const std::uint32_t scale = pre ? static_cast<std::uint32_t>(p.k) : 1u;\n\n  // Rung 10: W repacked into (gene block x panel) tiles. P1_REPACK=0 keeps\n  // rung 9a's layout, which is the A/B for this rung.\n  bool tiled = format == 3 && p.k > 0 && blocks.genes > 0;\n  if (const char* e = std::getenv(\"P1_REPACK\")) {\n    if (std::strtoll(e, nullptr, 10) == 0) tiled = false;\n  }\n  std::unique_ptr<double[]> tiles(\n      tiled ? new double[std::uint64_t(blocks.count) * blocks.genes * p.k] : nullptr);\n\n  if (format == 3) {\n    const PackedRows rows{m.as<std::uint32_t>(hdr[6]), m.as<std::uint32_t>(hdr[7]),\n                          m.as<std::uint8_t>(hdr[8]),  m.as<std::uint8_t>(hdr[9]),\n                          p.W, y.data, p.k, dist, n_genes, blocks, cursors.get(), scale, pre,\n                          tiles.get(), tiled};\n    run_rows(rows, row_nnz, n_cells, p.k, hdr[4], blocks.count, tiled, n_genes);\n  } else if (format == 2) {\n    const WideRows rows{row_nnz, m.as<std::uint32_t>(hdr[6]), m.as<double>(hdr[7]),\n                        p.W, y.data, p.k, dist, n_genes, blocks, cursors.get()};\n    run_rows(rows, row_nnz, n_cells, p.k, 0, blocks.count, false, n_genes);\n  } else {\n    fatal(\"csr_packed.bin has an unknown format; re-run prepare\");\n  }\n}",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    },
    {
      "id": "c83c3780-9ab1-4b06-99f1-34ad712c3f7c",
      "name": "baseline",
      "is_baseline": true,
      "version": 2,
      "owner_handle": "zachdebruine",
      "updated_at": "2026-09-10T02:08:44.181+00:00",
      "reason": "baseline \u2014 required for same-session scoring",
      "files": {
        "harness.cpp": "// ============================================================================\n// hpcbench fixed harness -- CIS 677 P1. HOST-OWNED. DO NOT EDIT: every entry is\n// compiled and linked against this file exactly as written.\n//\n// One binary, two modes:\n//\n//     ./build/task prepare <raw_input> <workdir>      UNTIMED\n//     ./build/task solve   <workdir> <result.bin> <k> TIMED\n//\n// `prepare` reads the raw corpus and calls the entry's `hpcbench::prepare`,\n// which writes whatever representation it wants into the workdir.\n//\n// `solve` generates the weights, allocates and zero-fills the output, then\n// starts a clock, calls the entry's `hpcbench::solve`, and stops it. Reading the\n// entry's own representation happens inside `solve` and is therefore on the\n// clock -- that is the point of the design. Everything the host does is outside\n// it: process start-up, generating W, allocating Y, writing result.bin and\n// digesting it are no longer charged to anyone.\n//\n// The timed region is announced on stdout, always, around the call:\n//\n//     HPCBENCH_SOLVE_BEGIN\n//     HPCBENCH_SOLVE_END\n//     HPCBENCH_TIME_MS <milliseconds>\n//\n// The last line is printed only where the platform has a monotonic clock. The\n// browser's wasm sysroot does not implement clock_time_get, so a `steady_clock`\n// call there traps; the in-tab runner instead timestamps the two markers as it\n// receives them, which measures the same region from outside the program.\n//\n// DIMENSIONS. The workdir belongs to the entry and the host does not parse it,\n// so `prepare` mode records the corpus dimensions in a host-owned sidecar,\n// <workdir>/.hpcbench-dims (two little-endian u64: n_cells, n_genes). `solve`\n// reads that, not the entry's files, to size W and Y. Reading it is untimed.\n// ============================================================================\n#define HPCBENCH_HARNESS_TU 1\n\n#include \"canon.hpp\"\n#include \"task.hpp\"\n\n#if defined(__wasm__) || defined(__EMSCRIPTEN__)\n// No clock_time_get in this sysroot: calling steady_clock::now() traps.\n#define HPCBENCH_NO_STEADY_CLOCK 1\n#else\n#include <chrono>\n#endif\n\n#include <cstdint>\n#include <cstdio>\n#include <cstdlib>\n#include <cstring>\n#include <string>\n#include <vector>\n\nnamespace {\n\nconst char* kDimsFile = \"/.hpcbench-dims\";\n\nvoid write_dims(const std::string& workdir, std::uint64_t n_cells, std::uint64_t n_genes) {\n  hpcbench::Writer w(workdir + kDimsFile);\n  w.write_value(n_cells);\n  w.write_value(n_genes);\n  w.close();\n}\n\nvoid read_dims(const std::string& workdir, std::uint64_t& n_cells, std::uint64_t& n_genes) {\n  hpcbench::Reader r(workdir + kDimsFile);\n  n_cells = r.read_value<std::uint64_t>();\n  n_genes = r.read_value<std::uint64_t>();\n  if (n_cells == 0 || n_genes == 0) hpcbench::fatal(\"workdir dimensions are empty; re-run prepare\");\n}\n\nint usage() {\n  std::fprintf(stderr,\n               \"usage: task prepare <raw_input> <workdir>\\n\"\n               \"       task solve   <workdir> <result.bin> <k>\\n\");\n  return 2;\n}\n\n}  // namespace\n\nint main(int argc, char** argv) {\n  if (argc < 2) return usage();\n  const std::string mode = argv[1];\n\n  if (mode == \"prepare\") {\n    if (argc < 4) return usage();\n    const std::string workdir = argv[3];\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    std::vector<double> X;\n    if (!hpcbench::read_raw_input(argv[2], n_cells, n_genes, X)) return 1;\n\n    hpcbench::Input in{n_cells, n_genes, X.data()};\n    hpcbench::Sink sink{workdir};\n    hpcbench::prepare(in, sink);\n    write_dims(workdir, n_cells, n_genes);\n\n    std::fprintf(stderr, \"prepare: %llu x %llu\\n\", (unsigned long long)n_cells,\n                 (unsigned long long)n_genes);\n    return 0;\n  }\n\n  if (mode == \"solve\") {\n    if (argc < 5) return usage();\n    const std::string workdir = argv[2];\n    const std::string result_path = argv[3];\n    const std::uint64_t k = std::strtoull(argv[4], nullptr, 10);\n    if (k == 0) hpcbench::fatal(\"k must be a positive integer\");\n\n    std::uint64_t n_cells = 0, n_genes = 0;\n    read_dims(workdir, n_cells, n_genes);\n\n    // Untimed: the problem definition and the output buffer.\n    const std::vector<double> W = hpcbench::gen_W(n_genes, k);\n    std::vector<double> Y(n_cells * k, 0.0);\n\n    hpcbench::Work work{workdir};\n    hpcbench::Params params{k, W.data(), n_cells, n_genes};\n    hpcbench::Result out{Y.data(), n_cells, k};\n\n    // The markers bracket the timed region for a runner with no clock inside\n    // the program. They are flushed so the timestamps are the real boundaries.\n    std::printf(\"HPCBENCH_SOLVE_BEGIN\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t0 = std::chrono::steady_clock::now();\n#endif\n    hpcbench::solve(work, params, out);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const auto t1 = std::chrono::steady_clock::now();\n#endif\n    std::printf(\"HPCBENCH_SOLVE_END\\n\");\n    std::fflush(stdout);\n#ifndef HPCBENCH_NO_STEADY_CLOCK\n    const double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();\n    std::printf(\"HPCBENCH_TIME_MS %.6f\\n\", ms);\n    std::fflush(stdout);\n#endif\n\n    // Untimed: writing and digesting the answer.\n    hpcbench::write_result(result_path.c_str(), Y, {n_cells, k});\n    return 0;\n  }\n\n  return usage();\n}\n",
        "src/main.cpp": "// The fixed harness now lives in harness.cpp, which is linked into every entry.\n// Nothing is compiled from this file; it is kept only so an older tool that\n// writes src/main.cpp does not write a stale harness there.\n",
        "src/prepare.cpp": "// src/prepare.cpp -- YOURS. UNTIMED.\n//\n// One function, no main(), no argv, no file format you have to guess:\n//\n//     void hpcbench::prepare(const Input& in, Sink& out);\n//\n// `in` is the raw corpus, already read for you: `in.X` is row-major\n// n_cells x n_genes doubles. `out` is your workdir; `out.create(\"name\")` hands\n// you a Writer. Whatever you write here is what `solve` has to read, and that\n// decision is most of the project.\n//\n// Nothing here is on the clock. Sort it, compress it, quantise it, shard it,\n// build an index. Note what is NOT in `Input`: `k`. The sweep value does not\n// exist yet, so it cannot be tabled.\n//\n// The baseline writes the dense matrix straight back out, which is the worst\n// possible answer and is the point.\n#include \"task.hpp\"\n\n#include <cstdint>\n#include <cstdio>\n\nvoid hpcbench::prepare(const Input& in, Sink& out) {\n  Writer w = out.create(\"matrix.bin\");\n  w.write_value(in.n_cells);\n  w.write_value(in.n_genes);\n  w.write(in.X, static_cast<std::size_t>(in.n_cells * in.n_genes) * sizeof(double));\n  w.close();\n\n  std::uint64_t nnz = 0;\n  const std::uint64_t n = in.n_cells * in.n_genes;\n  for (std::uint64_t i = 0; i < n; ++i)\n    if (in.X[i] != 0.0) ++nnz;\n  std::fprintf(stderr, \"prepare: %llu nonzeros of %llu (%.2f%% dense)\\n\",\n               (unsigned long long)nnz, (unsigned long long)n,\n               100.0 * double(nnz) / double(n));\n}\n",
        "src/solution.cpp": "// src/solution.cpp -- YOURS, and the ONLY thing timed.\n//\n//     void hpcbench::solve(const Work& w, const Params& p, Result& y);\n//\n// The clock starts on entry to this function and stops on return. Everything\n// else -- process start-up, generating the weights, allocating and zero-filling\n// `y`, writing result.bin, digesting it -- happens outside the timed region and\n// costs you nothing.\n//\n// What that means in practice: reading your own representation out of the\n// workdir is INSIDE the clock. `w.open(name)` streams it, `w.map(name)` gives\n// you the whole file, `w.read_all(name)` slurps it. Choosing between them, and\n// choosing how little you need to read at all, is part of the optimisation.\n//\n// `p.W` is row-major n_genes x k, already generated by the host: there is no\n// gen_W in an entry any more. `y.data` is row-major n_cells x k and arrives\n// zero-filled. Compute Y = X * W.\n//\n// The baseline reads the dense matrix the baseline `prepare` wrote and runs the\n// textbook triple loop. Both halves are yours to replace.\n#include \"task.hpp\"\n\n#include <cstdint>\n#include <vector>\n\nvoid hpcbench::solve(const Work& w, const Params& p, Result& y) {\n  // On the clock: the baseline pays to read 296 MB of dense doubles.\n  Reader r = w.open(\"matrix.bin\");\n  const std::uint64_t n_cells = r.read_value<std::uint64_t>();\n  const std::uint64_t n_genes = r.read_value<std::uint64_t>();\n  std::vector<double> X(static_cast<std::size_t>(n_cells * n_genes));\n  r.read(X.data(), X.size() * sizeof(double));\n  r.close();\n\n  const std::uint64_t k = p.k;\n  for (std::uint64_t i = 0; i < n_cells; ++i)\n    for (std::uint64_t g = 0; g < n_genes; ++g)\n      for (std::uint64_t c = 0; c < k; ++c)\n        y.data[i * k + c] += X[i * n_genes + g] * p.W[g * k + c];\n}\n",
        "build.sh": "#!/bin/sh\n# build.sh -- YOURS to edit. Flags, vectorisation, OpenMP, LTO: all fair game.\n#\n# It must produce ONE binary at build/task, linking the host harness with your\n# two source files:\n#\n#     harness.cpp        host-owned: main, argv, the timing boundary\n#     src/prepare.cpp    yours, untimed\n#     src/solution.cpp   yours, timed\n#\n# Your files must not define main(); the harness owns the process. That is\n# checked below, and again by the compiler.\nset -eu\n\nCXX=${CXX:-c++}\nCXXFLAGS=${CXXFLAGS:--O3 -std=c++17 -DNDEBUG}\n\nfor f in src/prepare.cpp src/solution.cpp; do\n  [ -f \"$f\" ] || { echo \"build.sh: missing $f\" >&2; exit 1; }\n  if grep -Eq '^[[:space:]]*(int|auto)[[:space:]]+main[[:space:]]*\\(' \"$f\"; then\n    echo \"build.sh: $f defines main(); the harness owns main. Your file provides\" >&2\n    echo \"          only hpcbench::prepare or hpcbench::solve.\" >&2\n    exit 1\n  fi\ndone\n\nmkdir -p build\n\nprobe() {\n  printf 'int main(){return 0;}\\n' > build/.probe.cpp\n  # shellcheck disable=SC2086\n  $CXX $1 build/.probe.cpp -o build/.probe 2>/dev/null && echo \"$1\" || true\n  rm -f build/.probe.cpp build/.probe\n}\n\nEXTRA=\"$(probe -march=native) $(probe -fopenmp)\"\n\n# shellcheck disable=SC2086\n$CXX $CXXFLAGS $EXTRA -Iinclude \\\n  harness.cpp src/prepare.cpp src/solution.cpp \\\n  -o build/task\n\necho \"built build/task with $CXX $CXXFLAGS $EXTRA\"\n"
      }
    }
  ],
  "skipped": [
    {
      "id": "c175c542-67e6-45ec-b40c-dc38563692df",
      "name": "ahsan-00",
      "owner_handle": "kowshid",
      "reason": "last run on your account was build_failed and the source has not changed since"
    },
    {
      "id": "abe122eb-ceea-492c-841a-d003785506b0",
      "name": "ahsan-01",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "4e142fc2-adba-470a-8c79-046bc03ba291",
      "name": "ahsan-01b",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "d804526d-d34f-41b1-947e-fd67d05a4323",
      "name": "ahsan-02",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "86551f34-faed-4bdc-b6a4-cc6ea9852eed",
      "name": "ahsan-03",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "24e3a06c-f742-4472-86e1-358be1b641b0",
      "name": "ahsan-04",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "d12f7654-db3b-4cc2-b8dd-83f4c2303ace",
      "name": "ahsan-05",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "394df29c-b172-476e-bf9d-f59fb2b59ac9",
      "name": "ahsan-06",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "92b22f20-b058-4730-9055-c38be49a2cb4",
      "name": "ahsan-07",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "9a43e071-0a76-412e-8ff6-a21f128af7e5",
      "name": "ahsan-07x",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "2fe06387-fe6f-46b7-80f5-9628c8ad9fa2",
      "name": "ahsan-08",
      "owner_handle": "kowshid",
      "reason": "not in owner's top 3 and unchanged since the last run on your account"
    },
    {
      "id": "0b651029-b529-4b34-9526-458228b170d6",
      "name": "bitshift",
      "owner_handle": "zachdebruine",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "8d427dbc-80f0-4606-b4d5-80c9c4d01510",
      "name": "dbaine-v3",
      "owner_handle": "bained",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "f63558d1-3f3c-4d22-803f-bac95091357e",
      "name": "entry-1",
      "owner_handle": "nancy",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "6374ccdc-027d-46e2-ad51-bfc3504b1cae",
      "name": "entry-10",
      "owner_handle": "ghnimim",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "3e06aaef-be26-4c49-8ef1-00112992d0b4",
      "name": "entry-11",
      "owner_handle": "ghnimim",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "e88b4943-4e7a-45ef-9e46-f602f9ec4aad",
      "name": "entry-12",
      "owner_handle": "ghnimim",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "db87879a-50f4-461b-8e9c-f566bc0beb85",
      "name": "entry-13",
      "owner_handle": "ghnimim",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "c72d079a-daee-4b77-961c-889131737483",
      "name": "entry-14",
      "owner_handle": "alaoua mohamed",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "d3602ad7-7418-49e3-9a10-e8ca6e80b98d",
      "name": "entry-15",
      "owner_handle": "castelsa",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "3d1f51b3-c60e-4e5f-a46f-43a090c380d4",
      "name": "entry-16",
      "owner_handle": "castelsa",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "b2f3b155-71d9-4c57-9d87-d6175defeae7",
      "name": "entry-2",
      "owner_handle": "castelsa",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "36c12ff6-383d-4c92-977b-91f57db48d5e",
      "name": "entry-3",
      "owner_handle": "alaoua mohamed",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "bdba590b-0aae-4a7c-af20-beaeecc343ac",
      "name": "entry-4",
      "owner_handle": "alaoua mohamed",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "130a206c-62b9-4205-a730-8906f529e9da",
      "name": "entry-5",
      "owner_handle": "alaoua mohamed",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "d5870f1d-91ef-4c97-8d64-3d10c1353b0f",
      "name": "entry-6",
      "owner_handle": "nancy",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "316c4bb0-4096-4332-ac40-c5947ad863cf",
      "name": "entry-7",
      "owner_handle": "nancy",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "ccbf86de-267a-4bb6-ad0b-3b7ae18a03d2",
      "name": "entry-8",
      "owner_handle": "nancy",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "2a3aa290-bd92-40bd-a901-6cbf4c730f24",
      "name": "entry-9",
      "owner_handle": "ghnimim",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "b55aa922-a345-49fb-a498-c7612974f91c",
      "name": "panel-csc",
      "owner_handle": "zachdebruine",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "91d82f97-e664-4407-958c-df892330f887",
      "name": "qa-browser-check",
      "owner_handle": null,
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "f2d50038-8f98-4443-8965-f016f81073df",
      "name": "qa-browser-check2",
      "owner_handle": null,
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    },
    {
      "id": "71ad3e10-32d1-4ac1-9642-4b3333567bed",
      "name": "sparse-csr",
      "owner_handle": "zachdebruine",
      "reason": "another participant's entry \u2014 its source stays private until the competition closes"
    }
  ]
}